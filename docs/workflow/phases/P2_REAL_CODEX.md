# P2: real Codex runtime and explicit API providers

Task IDs: `P2-01` through `P2-10`

Use `codex-app-server`, `security-privacy`, `native-app-testing`, and `spec-sync`. Integrate the official Codex app-server as a long-running child process over stdio JSONL. Generate or validate protocol types against the installed binary. Implement ChatGPT-managed sign-in, account state, rate limits, runtime model discovery, skill discovery, durable project threads, streamed events, interruption, restart recovery, and server approval requests.

The packaged Codex path uses eight guarded host-defined tools for new project conversations and retains guarded MCP for existing MCP-bound conversations. Store each thread's route and fail closed on mismatch. For new dynamic threads, bind the `project_id` property of each model-visible tool schema to the main-owned active project with JSON Schema `const`; retain independent main-process active-project and draft-freshness checks. Stored thread definitions remain authoritative on resume. The shared main-owned transaction service handles both routes. Packaged native edit, Undo, reopen, legacy-thread compatibility and a bounded forbidden-tool probe must be distinguished from the still-open complete effective-tool boundary. A host call before the `turn/start` response requires a submitted request, a validated owned start notification and a later matching response; unknown, completed or contradictory turns cannot dispatch. The AI selector offers Codex, OpenAI API, DeepSeek and Gemini API with runtime-validated models and provider-specific capabilities. API-provider output must pass the same main-owned guarded transaction engine; it has no direct MCP, file or export authority. Use fixed provider endpoints, main-owned secure keys, explicit paid-turn initiation and provider-specific context disclosure. Gemini function-call thought signatures must be validated, replayed within the current main-process turn and never exposed or persisted. Fake transports are allowed only in tests. Apply ADRs 0014 and 0015.

Apply ADR 0013: use P1's real project foundation and implement the shared validated transaction core in P2-05 before the authenticated P2-06 edit. Require expected draft sequence, durable journaling, atomic persistence before notification, deterministic inverse/undo, stack-ordered redo and recovery of complete committed transactions. Redo is a new hash-chained transaction, survives reopen, and is cleared by a newly applied edit. Manual and later Magic Wand tools must reuse this engine; no AI-only mutation store or UI-only edit is acceptable. Persisted navigation state is separate from draft sequence and operation history. Full P3/P6 acceptance remains required.

The partial manual Edit surface uses the same main-owned transaction service for playhead-based clip-edge trim, interior split, a marked ripple range cut, newest undo and stack-ordered redo. Its IPC carries a strict committed draft head and bounded intent, while main supplies the trusted manual origin and generated journal IDs. A split keeps the left fragment identity and creates a deterministic right fragment identity from the trusted operation. A half-open range cut may cross source joins or remove a source's complete visible span; it reflows surviving fragments without deleting immutable source inventory or baseline. The projected fragment map and Undo/Redo targets must update after manual and assistant commits before further tool targeting. Guarded `cut.split`, `cut.delete_range`, and `cut.restore_range` tools expose those same validated reducers to Codex and the fixed API-provider adapters; their availability does not satisfy the remaining guarded-tool policy, provider access, or P2 phase gates.

The guarded `cut.restore_range` operation restores one confirmed missing half-open source-time interval from exactly one immutable baseline source clip. It rejects unknown or ambiguous sources, invalid bounds, overlapping visible material, and mixed-operation requests before persistence. Main inserts by baseline source order and source-time position, persists the full before/after maps and inverse in the shared journal, and projects the committed draft to controls, timeline, and preview. Codex and API-provider calls use the same freshness validation and Undo history. This bounded exact restoration does not infer missing speech or authorize source mutation or cleanup.

For every `thread/start` and `thread/resume`, derive one validated feature policy from the stored route and runtime model. MCP-bound threads keep `code_mode_only` false and native collaboration disabled. Dynamic-bound Codex threads enable code mode and its packaged host with in-process fallback disabled. V1-capable models may use the supported V1 child path at depth 1; GPT-6-Luna V2 uses the six native V2 functions as direct-only tools under `codex_video_edit_agents`, excluded from nested code-mode. Set V2 concurrent capacity to two because the root occupies one slot, leaving at most one child. Correlate `spawn_agent` by call ID to the server-owned `subAgentActivity.started` child ID, then allow only child summary reads if they are available. V2 children receive only the main thread's path-free project and timeline summary snapshots; they do not inherit the per-thread dynamic editor tools. Preserve the child's empty environment, read-only/no-network sandbox and never-approval policy. Explicitly disable supported unrelated browser, app, connector, plugin, shell, search, image, memory, hook, remote-control and commit features on both routes. Also set `features.sleep_tool=false` and `features.view_image=false` in process and thread configuration; code-mode-only does not suppress every direct tool. Keep the full effective upstream tool catalog as an open gate; exact request flags and one-turn inventories are bounded evidence only.

Acceptance:

- parser, recovery, stale protocol, and tool validation tests pass
- a failed Codex turn exposes an explicit Retry request control only after the server confirms terminal failure; it resubmits the most recent user request as a new turn, while interrupted or uncertain turns cannot be replayed automatically
- packaged recovery must identify and stop only the direct App Server child owned by the test Electron main, reconnect through the existing Settings action, and restore the same project-thread identity/history without adding a transaction. Test idle child loss and loss after a guarded mutation commits during an active turn separately; require no duplicate journal transaction and shared Undo after recovery. State that this does not test loss during the transaction persistence boundary or imply every uncertain edit is safe.
- packaged nested-tool surface probes must correlate the exact diagnostic turn to an account-contained private rollout and verify the `exec` `custom_tool_call` with its matching output; an assistant's final text alone is not authoritative. Require eight guarded editor functions and zero connected-app functions. For V1-capable models, require five supported V1 collaboration functions on the dynamic route. For GPT-6-Luna V2, require the six child functions to stay out of nested code-mode, leaving only read-only `clock__curr_time` outside the eight editor tools; separately require packaged direct `spawn_agent`/`wait_agent` calls, exact spawn/activity correlation, one completed child, parent-validated project/timeline summary reads before spawn, a matching child report, inherited read-only policy, and unchanged source/baseline/journal. Repeat the dynamic probe with synthetic `[apps._default].enabled = true` and `[apps.adobe].enabled = true` configuration.
- a real authenticated Codex smoke test discovers models and skills
- a fresh subscription preference selects runtime-listed GPT-6-Luna/high, preserves a valid explicit choice, and reports an actionable empty selection when unavailable; while Settings is open, a new local skill becomes visible from refreshed runtime metadata without an app restart
- OpenAI API, DeepSeek and Gemini API key paths pass fixed-endpoint, secure-storage/session-fallback, model-discovery, key-redaction and provider-failure tests; Gemini 3 function-call signature replay passes authenticated native verification
- a remembered API model choice survives restart only with its protected key and live catalog membership; replacement, removal and session-only use clear it
- authenticated API-provider fixture turns commit only guarded reversible draft operations after explicit user initiation and pass packaged isolated native tests
- one real turn applies a reversible draft edit through a guarded tool
- the edit survives reopen, rejects stale sequence requests, and can be undone through the shared engine; interrupted/incomplete transactions do not corrupt the committed draft
- verified pass checkpoints bind to the exact current pass/head and reject passed checks without concrete evidence IDs; the main-only structural helper derives evidence from validated state, accepts only current main-owned manual-origin groups, and does not claim editorial or A/V review
- raw protocol text stays out of normal UI
- `docs/workflow/results/P2.json` validates
