# Active phase

- Phase ID: `P2`
- Task IDs: `P2-01` through `P2-10`
- Prompt: `docs/workflow/phases/P2_REAL_CODEX.md`
- Required skills: `codex-app-server`, `security-privacy`, `native-app-testing`, `spec-sync`; apply `timeline-editor` to the shared transaction foundation and `open-source-development` to publication. Apply ADRs 0014 and 0015's fixed API-key provider boundary to P2-08 through P2-10.
- Required result: `docs/workflow/results/P2.json`

P0 and P1 are accepted in their result records. P1 binds to verified public main revision `33d3fdc1d87787322c658fad88a1c4fa9787f2d1` after PR #10 Windows/Linux checks passed. Its private evidence covers the actual packaged local Electron shell, immutable synthetic import, project/baseline identity, persisted five-stage navigation, failed-save preservation, bounded lifecycle recovery, 100-200% scale, keyboard and guest-only visual inspection. The real Orca 50.2/AT-SPI 2.56.8 smoke passed seven controls under natural activation. Retained failures and Linux-only limitations are explicit in `docs/workflow/results/P1.json`; no later feature is accepted by navigation alone.

Implement real Codex app-server over stdio with the supported ChatGPT-managed sign-in and exact generated protocol contract. Discover models and skills at runtime; preserve actual project threads, streaming, interruption and supported server approvals/subagents. No fake production transport or hardcoded model catalog. Official Codex 0.142.3 was installed in the isolated guest for the earlier checks described here. Its experimental TypeScript/JSON schemas were generated there, and the reviewed thread/turn/MCP dependency closure is pinned alongside the prior account/settings types. Real signed-out bootstrap/skill discovery and packaged account/settings login-initiation/cancellation/reopen passed with external browser launching suppressed. The packaged owned MCP inventory also negotiates while signed out. Subsequently, the user explicitly authorized seeding the private app-owned guest account with only the official runtime credential file, mode 600, under ADR 0006. A packaged authenticated probe discovered runtime models, skills and usage; this is not browser-login completion. Production onboarding remains managed sign-in; no credential-import feature is added. A later fully passing private synthetic packaged native run discovered one model and five skills, applied one real Codex-origin start trim at sequence 1 (1.5 to 1.0 seconds), observed committed controls and exact retained pixels while the turn was running, reopened the same thread with matching history, and used a real Codex undo at sequence 2 to restore 1.5 seconds. A separate read-only turn was stopped while running; it left the journal unchanged and reopened on the same thread. Guest-only visual evidence confirmed the drawer-to-Source-inspector switch. An offline guarded-service continuation against a later real Codex-edited native fixture rejected stale sequence/hash and preserved journal, draft, source and baseline; a later packaged native turn proved the live App Server/MCP stale-error relay with unchanged journal, source and baseline. Explicit native-agent flags and child-event filtering let a parent turn complete; its first history-only audit missed v1 spawn evidence. A later rollout and child-history audit established the bounded spawn/read described below. Earlier failed attempts remain failures under their original checks. Complete effective forbidden-tool restriction still needs stronger authenticated native evidence; a later strict native child pass is recorded below. P2 remains incomplete.

After that published slice, a further `gpt-5.5` xhigh native turn explicitly requested discovery of the supported spawn tool but the persisted parent turn page saved no collaboration item; the turn-history API cannot represent tool-search calls or reconstruct filtered v1 spawn events. A later private audit found a linked child for that run. A separate authenticated negative command probe passed a narrower behavioral check: the completed turn invoked no command/file/dynamic or unrequested tool, and the synthetic project did not change. It does not prove full built-in-tool unavailability. A separate signed-in packaged Settings run matched the current runtime plan, model, usage window/reset time and skills across restart, with guest-only visual input. Managed browser-login completion and complete forbidden-tool restriction remain open; a later strict native child pass is recorded below. No P2 result or task checkbox is recorded.

Apply ADR 0013 before the real fixture edit: implement one shared transaction engine with expected sequence/hash checks, durable journal, atomic persistence before notification, deterministic undo and complete-transaction recovery. Manual tools, Codex and later Magic Wand reuse it. Preserve sources and the immutable baseline; stage navigation is separate from draft history. Apply the editorial pass-group and truthful checkpoint requirements in `docs/47_EDITORIAL_FIRST_CUT.md` and its prompt. The supplied editorial reference was adapted as requirements; its runtime is not implemented yet.

Current P2 continuation: the strict packaged native child test passed twice on fresh synthetic projects after correcting its evidence source. Pinned rollout policy retains the v1 `spawn_agent` function call/output but filters collaboration events from persisted history. Private parent rollout evidence, App Server `thread/read` child parent/source, child-owned summary calls and matching saved read-only/never-approval turn contexts verified one supported native child per run; source, baseline and journal stayed unchanged. Guest screenshot inspection found the completed single-drawer state. A separate browser-equipped isolated guest launched the packaged Electron Settings sign-in action, opened the real OpenAI login page in guest Chromium, cancelled sign-in and returned to signed out; guest-only visual inspection verified the browser page and native app behind it. That proves opening and cancellation, not managed browser-login completion. Complete effective restriction of unapproved built-in tools also remains open. P2 remains incomplete with no result record or task checkbox.

An optional official ChatGPT device-code route is now implemented in Settings. A fresh-account packaged guest passed initiation, transient one-time code, fixed verification-page opening in guest Chromium, cancellation and cleanup. In a separate live attempt the user completed verification externally; the same packaged native window received the official completion, reconciled to a signed-in ChatGPT plan and cleared its code. A separate packaged reopen with that private app-owned account confirmed authoritative signed-in state, plan, live model catalog and skills. The first interactive test expecting cancellation failed when the account completed during inspection; its failure is retained, and the regular cancellation run remains a distinct pass. This proves managed device-code completion, not the browser OAuth callback route. Complete effective restriction of unapproved built-in tools remains open, so P2 has no result record or task checkbox.

ADR 0014 records the later user request for optional OpenAI API and DeepSeek API-key providers alongside the unchanged Codex App Server. P2-08 through P2-10 require fixed main-owned endpoints, protected key storage or session-only fallback, live provider model validation, explicit initiation of paid turns, provider-specific capability labels, and a real guarded edit through the shared transaction engine. The provider connections, shared guarded adapter and packaged Settings are implemented; an existing OpenAI API key authenticated live model discovery in isolated packaged Electron. A remembered model is encrypted with its key and revalidated on restart in unit tests. A separate packaged Linux guest with real GNOME Secret Service passed protected OpenAI key/model restoration after live model discovery and explicit removal; this does not establish another OS keyring. Packaged native OpenAI and DeepSeek draft tests with deterministic main-only transports passed committed trim, stale rejection, shared undo, provider failure and project reopen. Their model text was synthetic; no paid authenticated provider edit or authenticated DeepSeek catalog has been tested. ChatGPT subscription sign-in and API billing remain distinct. P2 stays incomplete.

A later exact-main packaged OpenAI-key fixture passed live catalog selection but two explicitly sent edit turns returned HTTP 429. Both left the journal at zero; there is no authenticated paid edit acceptance or known account-side rate/quota diagnosis. A follow-up product slice maps 429 to a fixed redacted rate-or-quota message. Packaged OpenAI and DeepSeek synthetic native runs passed the updated drawer, unchanged-journal failure, shared edit/undo and project reopen; guest-only visual inspection saw the actual native message. P2-08 through P2-10 and P2-07 remain unchecked; no P2 result is written.

Generation-time API connection rejection now has its own redacted 401/403 recovery message. The packaged deterministic OpenAI and DeepSeek native fixtures passed that state without a journal mutation, and an additional OpenAI 1366×768 run verified the alert fully inside the single drawer with a usable message scroll area. Visual inspection caught an earlier clipped alert; the final package was rerun after correcting the layout and viewport assertions. The live authenticated fixture harness can target either provider when a private authorized key exists, but DeepSeek authentication and successful paid editing remain unverified. No P2 task or result is accepted by these synthetic passes.

A further packaged OpenAI 200%-interface-scale run passed after reserving minimum conversation height; guest-only inspection saw the changed alert after scrolling within the selected drawer. At that scale in the test window, preview and full drawer are reviewed separately, not claimed simultaneously visible. Earlier failed visual runs remain failures. The final default-size OpenAI/DeepSeek and compact OpenAI native runs passed on the corrected package.

Product builds, launches, screenshots and input stay inside isolated Docker guests. Never launch or control the app on the user's host. Use Playwright Electron and the guarded guest X11/XTEST/x11grab helper, with no host mounts, devices or viewer. The narrow credential seed grants no configuration/history transfer or secret output; the host credential file was unchanged at transfer. Keep all credentials, private media, projects and evidence out of Git. A tagged H.264/BT.709 display-only still-frame path passed synthetic unit, project-runtime and packaged native fixture tests, including guest-only visual seek. The two supplied recordings were then privately imported through that packaged native picker in order, each with preserved source bytes, its own project and a first-frame preview; they remain separate projects. This is not the requested appended editable draft, full-length review or export, and no private media or evidence is published. A fresh packaged authenticated negative command probe correlated the exact turn to its private account-confined rollout: zero raw tool-search, function, other call or unknown response items; source, baseline and journal were unchanged. Guest-only capture showed the completed native drawer. Two earlier setup runs failed before the probe and remain failures. This proves only no invocation in that turn; complete effective forbidden-tool unavailability remains open. The bounded synthetic Codex edit, managed device-code completion and OpenAI API catalog check above passed; browser OAuth callback completion, authenticated DeepSeek catalog, paid provider edit, user-media edit, audio listening, continuous A/V playback, Windows capture/device/installer and full export workflow are not accepted. P2-P10 remain required.

The later partial P3-04 slice combined those two supplied recordings into one ordered editable draft in packaged Electron, with verified source hashes, join-frame mapping, reopen and native visual inspection. The preceding paragraph describes the earlier separate-project state. At that point the combined draft had not received Codex or Magic Wand edits, audio review or an export. P2 remains the active incomplete phase; P3-04 remains unchecked pending full media and render acceptance.

The subsequent partial P2-05/P6-01 slice added native Edit-stage playhead trim and shared newest Undo through main-owned typed IPC. Synthetic packaged Electron and the supplied two-source recordings passed manual trim/undo, committed clip reflow, reopen and immutable source/baseline checks in the isolated guest; guest-only visual inspection saw the real Edit window. At that point the supplied footage had not received Codex or Magic Wand edits, an audio watch-through, or an export. Complete model-visible tool restriction, authenticated DeepSeek catalog/paid edit, broader manual tools and phase acceptance remain open.

The next partial P2-05/P3-04/P6-01 slice adds interior playhead Split through the same manual IPC and hash-chained journal. It retains the left fragment ID, creates a deterministic right ID from the trusted operation, and projects the committed fragment map so subsequent edits target current boundaries. A packaged Linux Electron synthetic two-source run passed Split, fragment trim, two undos, reopen, decoded-display checks at the fragment and source joins, source/baseline integrity and guest-only 200% window inspection. After that fixture, the two supplied recordings passed the same split/trim/undo/reopen and integrity path in the isolated guest, with actual native-window inspection. They have not received a full A/V review, Codex or Magic Wand edit, or export. P2 remains active and unchecked; P3-04 and P6-01 also remain partial, with no phase result.

The preceding partial P2-05/P3-04/P6-01 work added `projects:manual-range-cut`: Edit marks two positions on the current committed draft, then cuts their nonempty half-open output interval and ripples surviving fragments. The range can cross a fragment/source join or remove one source's entire visible span while retaining immutable source inventory and baseline. Main owns manual authority and journal IDs; the shared newest Undo restores the prior fragment map. `npm run check` passed 42 foundation and 255 media tests with two documented skips. A packaged Linux Electron synthetic two-source fixture passed cross-source marked cut, exact surviving decoded frame at the new join, Undo/reopen and source/baseline integrity. An initial guest visual inspection found Cut range below the default window fold; the Edit preview reserved 70 px, then a final packaged rerun passed Cut range in view, exact restored pixels after Undo, 200% bounds and guest-only visual inspection. After that fixture passed, both supplied recordings were imported in order in the isolated no-mount guest and passed a cross-source two-second cut, exact second-source join frame, Undo/reopen and original/managed source and baseline integrity, with guest-only native inspection. Continuous synchronized A/V playback, a full watch-through, Magic Wand and master export remain open; P2/P3/P6 checkboxes and result records stay open.

The preceding partial P2-05/P2-06/P2-10 slice exposes that same half-open `ripple_delete` reducer as guarded `cut.delete_range` to real Codex App Server and the fixed OpenAI API/DeepSeek adapters. The complete local check passed 42 foundation and 259 media tests with two documented skips. A packaged Linux Electron synthetic two-source fixture passed a real signed-in Codex cross-source cut, committed-frame inspection while the turn ran, manual Undo, reopen and immutable-source checks. After that gate, the two supplied recordings were privately imported in order into a combined draft and passed a real Codex two-second cross-source cut, exact decoded join frame, manual Undo, reopen, original/managed source and baseline integrity, and guest-only native visual inspection. Two earlier private test attempts failed from full-resolution test-harness memory pressure and a premature reopened-frame assertion; the final bounded hash-and-wait harness passed. The resulting private draft is restored, not editorially complete. The current read tools provide no transcript or audio evidence for semantic speech selection; continuous A/V review, Magic Wand, lossless master export, authenticated paid API edits, DeepSeek catalog and P2-07 effective tool restriction remain open. P2/P3/P6 checkboxes and results stay open.

The current partial P2-05/P2-06/P2-10 slice exposes the existing reversible split reducer as guarded `cut.split`, beside the two summary reads, trim, range cut and newest Undo. The complete local check passed 42 foundation and 262 media tests with two documented skips. A packaged Linux Electron synthetic two-source fixture passed a real signed-in Codex interior split, committed three-fragment preview during the turn, exact start/split/join pixels, manual Undo, reopen and immutable-source checks, with guest-only visual inspection. An earlier native attempt checked asynchronous Undo too early and remains a failed harness attempt. The two supplied recordings previously passed real Codex range-cut and manual split paths, but were not run through this Codex split fixture. The current reads provide no transcript or audio evidence for semantic speech selection. Official Codex 0.155.1 still exposes built-in MCP resource helpers without a public exact effective-tool allowlist; the then-pinned runtime was 0.142.3 and P2-07 stayed open at that checkpoint. Authenticated paid API editing, Magic Wand editorial selection, synchronized A/V review and master export remain separate gates. No P2 task checkbox or result record follows from this partial tool slice.

The next partial P2-08 through P2-10 slice adds Gemini API as a third fixed key provider under ADR 0015. Main owns its separate protected/session key, live-catalog model filter and turn-local thought-signature replay. Packaged isolated Linux Electron passed disconnected/rejected-key Settings, a main-only synthetic Gemini tool-chain edit/preview/stale/Undo/reopen flow, and guest-only visual inspection; OpenAI and DeepSeek synthetic native regressions also passed. No authorized Gemini key or live completion was tested. P2-07 effective-tool confinement, authenticated DeepSeek/Gemini and successful paid provider edits, semantic A/V review, Magic Wand and lossless master export remain open. Do not write P2.json or mark tasks complete from this partial slice.

The partial P2 subscription-default slice pins official Codex 0.155.1 and its matching code-mode host, regenerates the reviewed protocol dependency closure, and applies ADR 0016. With no saved preference, the live catalog must offer `gpt-5.6-luna` with `high`; otherwise Settings keeps the choice unset. At the initial policy checkpoint, unrelated app/tool namespaces and native child agents were disabled pending stronger evidence. The later P2-07 boundary below permits only correlated read-only child summaries on dynamic-bound threads; MCP-bound threads keep child collaboration disabled. Do not spawn Astra agents. Require packaged signed-in native Settings, a bounded tool-surface probe, a committed Codex edit, shared Undo and reopen on both synthetic media and the two private supplied recordings before describing this slice as passed. Preserve failed runs separately. P2-07 and full P2 acceptance remain open.

At the earlier P2-04/P2-07 restriction checkpoint, the product explicitly disabled supported plan, user-input and native-agent tools at App Server launch and thread create/resume. A rebuilt isolated package passed Luna/high nested-tool probing on the then-restricted route, real guarded split, exact frames, shared Undo/reopen and guest-only inspection. A final-package Luna/high trim/Undo/Stop/history rerun and a separately audited forbidden-command turn passed; the latter observed no raw tool invocation in that exact turn. Later route-aware tests permit the bounded dynamic-route child summary path described below. These observations do not establish a permanent upstream effective-tool allowlist. Do not mark P2 complete.

The user supplied a DeepSeek key for one isolated live fixture turn. In the packaged Electron window, live model discovery and one explicit Send produced three HTTP 200 completion responses, exactly one guarded start trim, committed-state preview pixels and duration, project reopen, unchanged source/baseline bytes and no restored session-only key. The key was passed through a mode-600 guest file that the harness deleted before app launch; private evidence remains ignored. A Gemini 3 live harness now checks transient thought-signature replay without recording signature bytes, but no Gemini key or live turn exists. OpenAI's 429, P2-07 effective confinement, semantic editing, A/V review and lossless export still prevent P2 acceptance.

A keyless isolated native continuation then used manual Undo on that same live DeepSeek draft, reopened it, and verified the exact original frame after decode. The reusable authenticated fixture now includes shared Undo and another reopen. The first continuation attempt sought the Edit-only control on Record or Import and failed before mutation; the corrected run passed. This is additional P2-10 evidence, not a completed phase result.

The strengthened authenticated DeepSeek fixture subsequently passed a complete second packaged native run: live catalog, one explicit paid Send, one guarded provider trim, exact changed preview, reopen, shared manual Undo, exact restored preview after decode, second reopen, immutable source/baseline and session-only key removal. A preceding run reached the final reopen but failed because the test read a zero-sized decoding canvas before its wait could retry; the canvas guard was corrected and that failed run remains separate. Three completion responses in the passing run returned HTTP 200. This still does not prove semantic editing, full A/V review, Gemini live access, master export or P2 acceptance.

For P2-07, a separate direct official App Server probe in the isolated guest tested experimental host-defined dynamic tools with no MCP server. A live-catalog Luna/high turn executed one code-mode cell computing `ALL_TOOLS` and called one validated namespaced read-only host tool; the bounded inventory contained only that tool. Earlier plain-function and mismatched bare-name attempts failed and remain separate. This is a transport feasibility result outside packaged Electron, not a replacement for existing MCP-backed project threads, a guarded draft edit, or complete effective-tool confinement. See `docs/research/P2_DYNAMIC_TOOLS.md`; P2 stays open.

A second direct probe restarted App Server and resumed that kind of dynamic-tool thread by exact thread ID. It completed another owned host-tool call with zero foreign host requests despite `thread/resume` having no `dynamicTools` field. This is one observed persistence path, not migration of the app's MCP-backed project threads or a packaged native edit. P2 stays open.

The next P2-07 internal boundary has pinned generated host-call types, a six-tool schema/name adapter, request/output bounds, correlated optional project-thread dispatch and redacted owned stream/history projection. Focused unit tests pass, but the packaged app does not register or route host-defined tools yet. Live early-call timing, mixed-route product selection and native committed host edit/Undo/reopen still block this route. P2 stays open.

One direct isolated live Luna/high timing probe using that adapter observed `turn/start` settle before one correlated read-only host call. Two older-binary setup attempts made no host call because their package lacked the matching code-mode host; failures remain recorded. The successful scheduling order is one observation, not a guarantee for every turn. Packaged product routing and the remaining P2-07 gates stay open.

The internal registry now tags new project threads with an MCP or dynamic route and treats all legacy v1 bindings as MCP. Unit tests prove later atomic migration, dynamic start/resume and cross-route rejection. A packaged Luna/high MCP edit/Undo/reopen/interruption regression passed with guest-only window inspection. Another packaged run reopened that real conversation from a synthetic v1 file, completed an owned read and left the file unchanged; a later product-created binding wrote v2 while retaining the old thread ID. Its broader second edit harness failed a post-turn assertion after a correct committed trim and remains failed evidence. Packaged main still selects MCP; guarded dynamic edit and effective tool confinement remain open. P2 stays open.

The optional dynamic client now permits a host call during `turn/start` only after a validated owned `turn/started` notification provisionally identifies its turn. It still buffers projection until the RPC response agrees; unknown, completed or contradictory turns quarantine. Focused tests cover those orders and a direct isolated official 0.155.1 client passed one Luna/high owned read without MCP. The live run did not establish that an early call occurred, and packaged main still uses MCP. Native mixed-route editing, effective tool confinement and P2 acceptance remain open.

The packaged product now routes new Codex project conversations through the six guarded host-defined tools and preserves MCP for stored MCP/v1 bindings. Both reach the same main-owned active-project service and draft-refresh path. An isolated packaged Luna/high synthetic run passed an actual dynamic-route trim during the turn, committed preview, same-thread/history reopen, real Codex Undo, restored preview, Stop and immutable source/baseline; the registry confirmed a dynamic binding and guest-only screenshot showed the actual native window. The same package separately reopened an existing v1 MCP conversation for an owned read without rewriting its binding. A first copy-based compatibility attempt failed because the project baseline embeds an absolute local root; the valid in-place guest test restored its registry backup. A dynamic-route negative command probe passed with no forbidden invocation in that exact turn. Complete effective upstream-tool confinement, safe native-child inheritance and the remaining P2 acceptance gates remain open; no P2 result is written.

A follow-up native continuation used the real dynamic edit/Undo fixture to send a stale sequence/hash to `codex_video_edit__cut_trim_edge`. Offline service rejection and the correlated completed App Server host call both returned a safe stale-draft failure. The draft, both journal records, original and managed source and baseline stayed byte-identical. This closes the narrow host-route stale-error relay check, not the complete effective-tool or P2 gates.

A further packaged Luna/high run on an earlier dynamic route revision checked the nested code-mode inventory and an actual two-source split. Its inventory answer reported six owned editor tools and zero other nested tools in that turn; the answer was not correlated to the raw rollout and its count predates the current supported child surface. The synthetic fixture and the two private user-supplied recordings, imported in their requested order through the native app, both passed committed split during the turn, exact split/join frame checks, shared Undo, reopen, and original/managed source and baseline integrity. Guest-only visual inspection of both native windows was completed. This is bounded editing evidence for those turns; complete effective upstream-tool confinement, semantic editing, full review and lossless master export remain open. No P2 result is written.

The current reviewed catalog now has seven tools, adding `cut.delete_ranges` for 2–16 confirmed disjoint half-open output ranges in descending order. Focused reducer/service and schema parity tests passed one atomic transaction, replay, stale and overlap rejection, shared Undo and immutable sources. Packaged Luna/high native runs passed a real dynamic batch on synthetic and both privately staged supplied recordings in order, with committed preview at the edited source join, manual Undo, reopen, unchanged original/managed source and baseline and guest-only visual inspection of the supplied native window. A separate fresh packaged Luna/high synthetic split turn on the seven-tool build reported seven owned and zero unowned nested names before the rollout-correlated fixture was added. The current fixture verifies the exact call and output from the private rollout and includes five supported v1 child functions on the dynamic route. Meaning-preserving selection, listened A/V joins, full review, effective confinement and lossless master export remain open. P2 remains incomplete and has no result record.

A final rebuild from formatted source repeated the supplied-footage batch successfully after the native harness explicitly selected the combined project through Home and waited for the large-media Edit stage. Three preceding harness attempts failed before the Codex edit on project focus or premature stage visibility; they remain failed evidence, not product passes. The final run passed the live two-operation commit, exact edited join, shared Undo, reopen, immutable input checks and guest-only native window inspection. P2 remains incomplete.

The latest partial P2-05/P6-01 slice exposes manual Redo in Edit through strict typed IPC and the same durable hash-chained journal as Undo. Replay restores the latest undone edit in LIFO order, survives project reopen, and clears the redo stack after a new edit. `npm run check` passed 42 foundation tests and 301 media tests (two documented skips), plus typecheck, lint, formatting and contract/delivery validation. A packaged Linux Electron synthetic two-source run passed Redo before and after reopen, exact decoded preview, follow-up Undo and source/baseline integrity; guest-only visual inspection saw the actual Edit window. No P2 task checkbox or phase result is recorded; effective tool confinement, wider editing, semantic Magic Wand work, full A/V review and lossless export remain open.

The latest P2-07 child-read checkpoint passes the packaged Linux Electron Luna/high fixture against official Codex 0.155.1. The real dynamic-bound parent completed one `spawn_agent`/wait cycle; `thread/read` linked the child to that parent and its two owned project/timeline summary reads completed under the inherited read-only, no-network, empty-environment and never-approval policy. The client retains an early child-turn candidate without authority until a validated server-owned parent spawn item supplies its receiver ID; in this run, the ID arrived with the completed item. It ignores unrelated `thread/started` and filters child stream events from the parent projection. Source, baseline and journal stayed unchanged. The guest-only native display screenshot was visually reviewed: it shows the packaged Electron window, one Codex drawer, the synthetic preview and no browser chrome or persistent Ready badge. Focused route/protocol/MCP tests pass 42 cases and typecheck passes. This is bounded child-read evidence only: complete upstream tool confinement, all remaining P2 gates and the P2 phase result remain open.
The latest partial P2-07 boundary run packages the changed Codex bridge and uses a synthetic hostile external-MCP entry in an isolated Codex profile. The native authenticated Luna/high child test passed after the app enumerated configured names, disabled each server, verified the app-server inventory, and disabled Apps/plugins/remote plugins on thread start/resume. The native fixture confirmed the external server remained disabled on the active thread, then completed the correlated child summary read with unchanged project, source, baseline and journal. Focused unit tests and repository checks are being rerun for this slice. This does not establish a complete effective upstream tool catalog, and P2 remains incomplete.

The boundary slice is now verified end to end. In isolated Docker, `npm run check` passed typecheck, lint, formatting, 42 foundation tests, 307 media tests, package/schema validation, and delivery/reference validation; `npm run check:protocol` verified the pinned 0.155.1 generated protocol closure (437 schemas, consumed method set). The staged-publication audit cleared 700 indexed files. The packaged authenticated synthetic-hostile-config child test passed on the actual Electron window, confirmed the configured server remained disabled before the correlated child read, and left project/source/baseline/journal unchanged. Its screenshot was reviewed inside the guest. This is partial P2-07 evidence only; no P2 result or task completion is recorded.
The follow-up MCP isolation hardening adds bounded validation inside the main-owned App Server argument builder and direct unit coverage of the full two-pass CLI operation. Eleven focused MCP tests passed, including exact argument order and fail-closed retained-enabled, added/removed inventory, malformed/duplicate name and over-limit cases. The full isolated Docker check passed again with 42 foundation tests, 310 media tests, typecheck, lint, formatting, package/schema validation and delivery/reference validation. This does not alter the earlier packaged native result: that run covers the valid hostile-config path; the new tests cover deterministic policy failures. P2-07 and P2 remain open.

The 2026-09-25 review of the newer official Codex 0.156.0 release found no complete model-visible-tool allowlist in its public `thread/start` or configuration types. Its request shape remains based on dynamic host tools; the release notes do not advertise an app-server allowlist. The product stays on its previously packaged and tested 0.155.1 contract pending protocol regeneration and native regression review. This is source evidence only and does not close P2-07 or prove the runtime's effective registry.

The pinned 0.155.1 configuration types state that `apps._default.enabled=false` can be overridden by per-app settings. The packaged Luna/high fixture now seeds a fresh app-owned Codex home with both `[apps._default].enabled = true` and `[apps.adobe].enabled = true`. Baseline and hostile-config dynamic turns passed a live code-mode inventory, committed split, shared Undo, reopen and immutable-source/baseline checks. The exact inventory was verified from its account-contained rollout: one `exec` custom-tool call and correlated output, seven guarded editor functions, five v1 collaboration functions, zero connected-app functions and zero other nested functions. Earlier attempts that required zero dynamic-agent functions or disconnected remain recorded as failures. This is bounded to the tested 0.155.1 thread surface; complete upstream tool confinement, the other P2 gates and the phase result remain open.

The latest partial P2-05/P2-06 range-batch fixture passes twice in packaged Linux Electron with real Luna/high Codex on synthetic two-source media. Its exact completed App Server history proves successful active-project summary reads before one guarded batch edit, committed state during the turn, shared Undo, reopen, exact preview and unchanged sources/baseline; the second run also received guest-only visual inspection. A separate earlier run passed the same structural batch on the two supplied recordings. One reused-profile attempt failed before mutation on a mismatched model-supplied project ID and remains a failure. The harness now supplies the exact active ID and validates direct `dynamicToolCall` history with its bounded nested output. Commit `e4ef13a` is on the open PR #61 branch; both push and PR CI runs passed on Linux and Windows. These checks do not prove semantic speech edits, audio listening, Magic Wand, complete tool confinement or final export. P2 stays incomplete; no result or task checkbox is recorded.
