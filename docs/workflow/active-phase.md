# Active phase

- Phase ID: `P2`
- Task IDs: `P2-01` through `P2-10`
- Prompt: `docs/workflow/phases/P2_REAL_CODEX.md`
- Required skills: `codex-app-server`, `security-privacy`, `native-app-testing`, `spec-sync`; apply `timeline-editor` to the shared transaction foundation and `open-source-development` to publication. Apply ADR 0014's API-key provider boundary to P2-08 through P2-10.
- Required result: `docs/workflow/results/P2.json`

P0 and P1 are accepted in their result records. P1 binds to verified public main revision `33d3fdc1d87787322c658fad88a1c4fa9787f2d1` after PR #10 Windows/Linux checks passed. Its private evidence covers the actual packaged local Electron shell, immutable synthetic import, project/baseline identity, persisted five-stage navigation, failed-save preservation, bounded lifecycle recovery, 100-200% scale, keyboard and guest-only visual inspection. The real Orca 50.2/AT-SPI 2.56.8 smoke passed seven controls under natural activation. Retained failures and Linux-only limitations are explicit in `docs/workflow/results/P1.json`; no later feature is accepted by navigation alone.

Implement real Codex app-server over stdio with the supported ChatGPT-managed sign-in and exact generated protocol contract. Discover models and skills at runtime; preserve actual project threads, streaming, interruption and supported server approvals/subagents. No fake production transport or hardcoded model catalog. Official Codex 0.142.3 is installed in the isolated guest. Its experimental TypeScript/JSON schemas were generated there, and the reviewed thread/turn/MCP dependency closure is pinned alongside the prior account/settings types. Real signed-out bootstrap/skill discovery and packaged account/settings login-initiation/cancellation/reopen passed with external browser launching suppressed. The packaged owned MCP inventory also negotiates while signed out. Subsequently, the user explicitly authorized seeding the private app-owned guest account with only the official runtime credential file, mode 600, under ADR 0006. A packaged authenticated probe discovered runtime models, skills and usage; this is not browser-login completion. Production onboarding remains managed sign-in; no credential-import feature is added. A later fully passing private synthetic packaged native run discovered one model and five skills, applied one real Codex-origin start trim at sequence 1 (1.5 to 1.0 seconds), observed committed controls and exact retained pixels while the turn was running, reopened the same thread with matching history, and used a real Codex undo at sequence 2 to restore 1.5 seconds. A separate read-only turn was stopped while running; it left the journal unchanged and reopened on the same thread. Guest-only visual evidence confirmed the drawer-to-Source-inspector switch. An offline guarded-service continuation against a later real Codex-edited native fixture rejected stale sequence/hash and preserved journal, draft, source and baseline; a later packaged native turn proved the live App Server/MCP stale-error relay with unchanged journal, source and baseline. Explicit native-agent flags and child-event filtering let a parent turn complete; its first history-only audit missed v1 spawn evidence. A later rollout and child-history audit established the bounded spawn/read described below. Earlier failed attempts remain failures under their original checks. Complete effective forbidden-tool restriction still needs stronger authenticated native evidence; a later strict native child pass is recorded below. P2 remains incomplete.

After that published slice, a further `gpt-5.5` xhigh native turn explicitly requested discovery of the supported spawn tool but the persisted parent turn page saved no collaboration item; the turn-history API cannot represent tool-search calls or reconstruct filtered v1 spawn events. A later private audit found a linked child for that run. A separate authenticated negative command probe passed a narrower behavioral check: the completed turn invoked no command/file/dynamic or unrequested tool, and the synthetic project did not change. It does not prove full built-in-tool unavailability. A separate signed-in packaged Settings run matched the current runtime plan, model, usage window/reset time and skills across restart, with guest-only visual input. Managed browser-login completion and complete forbidden-tool restriction remain open; a later strict native child pass is recorded below. No P2 result or task checkbox is recorded.

Apply ADR 0013 before the real fixture edit: implement one shared transaction engine with expected sequence/hash checks, durable journal, atomic persistence before notification, deterministic undo and complete-transaction recovery. Manual tools, Codex and later Magic Wand reuse it. Preserve sources and the immutable baseline; stage navigation is separate from draft history. Apply the editorial pass-group and truthful checkpoint requirements in `docs/47_EDITORIAL_FIRST_CUT.md` and its prompt. The supplied editorial reference was adapted as requirements; its runtime is not implemented yet.

Current P2 continuation: the strict packaged native child test passed twice on fresh synthetic projects after correcting its evidence source. Pinned rollout policy retains the v1 `spawn_agent` function call/output but filters collaboration events from persisted history. Private parent rollout evidence, App Server `thread/read` child parent/source, child-owned summary calls and matching saved read-only/never-approval turn contexts verified one supported native child per run; source, baseline and journal stayed unchanged. Guest screenshot inspection found the completed single-drawer state. A separate browser-equipped isolated guest launched the packaged Electron Settings sign-in action, opened the real OpenAI login page in guest Chromium, cancelled sign-in and returned to signed out; guest-only visual inspection verified the browser page and native app behind it. That proves opening and cancellation, not managed browser-login completion. Complete effective restriction of unapproved built-in tools also remains open. P2 remains incomplete with no result record or task checkbox.

An optional official ChatGPT device-code route is now implemented in Settings. A fresh-account packaged guest passed initiation, transient one-time code, fixed verification-page opening in guest Chromium, cancellation and cleanup. In a separate live attempt the user completed verification externally; the same packaged native window received the official completion, reconciled to a signed-in ChatGPT plan and cleared its code. A separate packaged reopen with that private app-owned account confirmed authoritative signed-in state, plan, live model catalog and skills. The first interactive test expecting cancellation failed when the account completed during inspection; its failure is retained, and the regular cancellation run remains a distinct pass. This proves managed device-code completion, not the browser OAuth callback route. Complete effective restriction of unapproved built-in tools remains open, so P2 has no result record or task checkbox.

ADR 0014 records the later user request for optional OpenAI API and DeepSeek API-key providers alongside the unchanged Codex App Server. P2-08 through P2-10 require fixed main-owned endpoints, protected key storage or session-only fallback, live provider model validation, explicit initiation of paid turns, provider-specific capability labels, and a real guarded edit through the shared transaction engine. The provider connections, shared guarded adapter and packaged Settings are implemented; an existing OpenAI API key authenticated live model discovery in isolated packaged Electron. A remembered model is encrypted with its key and revalidated on restart in unit tests. A separate packaged Linux guest with real GNOME Secret Service passed protected OpenAI key/model restoration after live model discovery and explicit removal; this does not establish another OS keyring. A further packaged native OpenAI-path test with a deterministic main-only transport passed committed trim, stale rejection, shared undo, provider failure and project reopen. That model text was synthetic; no paid authenticated provider edit or DeepSeek catalog has been tested. ChatGPT subscription sign-in and API billing remain distinct. P2 stays incomplete.

Product builds, launches, screenshots and input stay inside isolated Docker guests. Never launch or control the app on the user's host. Use Playwright Electron and the guarded guest X11/XTEST/x11grab helper, with no host mounts, devices or viewer. The narrow credential seed grants no configuration/history transfer or secret output; the host credential file was unchanged at transfer. Keep all credentials, private media, projects and evidence out of Git. Both user-example sources remain unimported and unedited; their required append order is recorded only in ignored local data. A fresh packaged authenticated negative command probe correlated the exact turn to its private account-confined rollout: zero raw tool-search, function, other call or unknown response items; source, baseline and journal were unchanged. Guest-only capture showed the completed native drawer. Two earlier setup runs failed before the probe and remain failures. This proves only no invocation in that turn; complete effective forbidden-tool unavailability remains open. The bounded synthetic Codex edit, managed device-code completion and OpenAI API catalog check above passed; browser OAuth callback completion, authenticated DeepSeek catalog, paid provider edit, user-media edit, audio listening, continuous A/V playback, Windows capture/device/installer and full export workflow are not accepted. P2-P10 remain required.
