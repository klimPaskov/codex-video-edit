# P3 progress: presentation-time and ordered two-source draft

P3-04 has two partial foundations while P2 remains the active incomplete phase. The first verified source presentation timestamps for display-only seeks, including B-frame order and variable-cadence gaps. The current slice measures the final displayed packet duration, stores two compatible immutable sources in a versioned initial baseline, maps adjacent half-open draft clips into the correct source, and supports trim/undo reflow through the existing common journal. The native Add footage control imports a second source from an unedited first-source project, creates a new combined project, and leaves the first project intact. The path-free IPC view, Source inspector, project list and bounded Codex summary show ordered source identity without private paths.

Synthetic media/domain tests cover timing evidence, source compatibility, exact join mapping, trim, undo, reopen and tamper rejection. A packaged Linux Electron test imported two generated H.264 sources through the native picker, crossed the join, reopened and verified pixels and immutable data. After that fixture passed, both user-supplied recordings were imported in order through the packaged native app in the isolated no-mount guest. The native preview reached both sides of the join and reopened; decoded-display bytes and original/managed source hashes matched the expected inputs. Guest-only visual inspection confirmed the real window changed at the join. The first two private harness attempts failed by seeking before asynchronous reopen completed; the corrected run passed. All private media, hashes, paths, projects and screenshots remain outside Git.

That initial two-source slice did not provide continuous synchronized A/V playback, multiple-source render/export, a full watch-through, Codex/Magic Wand edits on the user footage, mixed-format conversion, or a complete P3 fixture matrix. P3-04 and P3 remain unchecked. No phase result is written.

An additional partial P3-04 operation permits an interior split of a committed source fragment. It keeps the left ID, derives the right ID from the trusted operation, preserves half-open source order and total duration, and bounds the fragment count. A packaged Linux Electron synthetic two-source fixture passed Split, fragment trim, two undos, reopen, exact decoded-display checks at the new fragment boundary and source join, and immutable source/baseline checks. Guest-only visual inspection covered the actual 200% native window. After that fixture, the two supplied recordings were privately imported in order into one draft and passed native Split, fragment trim, two undos, reopen and source/baseline integrity checks, with guest-only native visual inspection. That split slice did not establish synchronized A/V playback, full watch-through, canonical multi-source rendering, Codex/Magic Wand editing of those recordings or export. P3-04 remains unchecked.

Spec sync: architecture, import, screen, media, lossless, Codex tool summary, P3 phase prompt, tasks, traceability, progress, typed IPC, two-source baseline schema/examples, reference implementation notes, media-engine/timeline-editor/native-app-testing skills and tests reflect the slice. Existing single-source contracts remain version 1.0. AI prompts, subagent routing and reference images are unchanged because this slice introduces no new AI instruction, delegated role or screenshot reference. Export and capture contracts are unchanged.

The current partial P3-04 range-cut work uses a half-open output-time interval to remove selected fragments and reflow survivors. The interval may span the two-source join; removing one source's entire visible span does not remove its immutable inventory entry or baseline. The committed fragment projection drives subsequent native seeks and manual edits after the cut or newest Undo. A packaged Linux Electron synthetic two-source fixture passed cross-source mark/cut, exact decoded surviving frame at the new join, Undo, reopen and source/baseline integrity. Guest-only visual inspection first found Cut range below the default viewport; the Edit preview reserved 70 px, and the final packaged synthetic rerun passed viewport visibility, exact restored pixels, 200% bounds and visual inspection. After that fixture, both supplied recordings were imported in order into the isolated no-mount guest and passed a two-second cross-source cut, exact second-source decoded join frame, Undo/reopen and original/managed hash and baseline checks, with native guest-only inspection. All private footage and evidence remain ignored. P3-04 and P3 remain unchecked; continuous synchronized A/V playback, full review and canonical multi-source rendering are still absent.

The same reducer is now callable as guarded `cut.delete_range`. A real signed-in Codex turn passed a cross-source cut on a packaged synthetic two-source draft, followed by exact surviving frame, manual Undo and reopen. After that fixture passed, a real Codex turn applied a specified two-second cross-source cut to the combined supplied recordings. The native committed frame, restored join frame after manual Undo, reopen and immutable source/baseline checks passed; guest-only visual inspection confirmed the packaged window. Earlier private harness memory and premature seek-assertion failures are retained in P2 progress. This demonstrates a bounded AI-requested time edit on the P3 draft model, not automatic content selection, synchronized A/V review, Magic Wand or canonical render/export. P3 remains incomplete.
