# P3 progress — presentation-timestamp source seeking

P3-04 has a partial, dependency-safe foundation while P2 remains the active incomplete phase. The native still-frame preview now probes presentation timestamps from the verified source, sorts B-frame packets from decode order into presentation order, and resolves a requested source microsecond to the frame already displayed at that time. It uses rational time-base comparisons and keeps the timestamp index separate from canonical render or master export. The immutable source and existing project/baseline contracts are unchanged.

Focused tests cover malformed/missing timestamps, duplicate PTS, frame-count mismatch, B-frame ordering, fractional tick boundaries, and a generated H.264 variable-cadence gap. The full source/media unit suite and contract checks passed locally. A packaged Linux Electron fixture in the isolated no-mount Docker guest passed native import, in-between-frame seek/canvas equality, project reopen, and source/baseline integrity. Guest-only screenshot inspection and a mouse-driven previous-frame action showed the actual native window updating after decode. Evidence is retained privately under ignored `test-results/`; no user footage or private capture is published.

This slice does not resolve the final presentation boundary of a source, provide synchronized audio playback, append two sources into one editable draft, or implement a canonical multi-source render. P3-04 and P3 remain unchecked. The two supplied recordings remain separate projects until that further work passes fixture and native acceptance.

Spec sync: the product architecture, import, screen, media, lossless, P3 phase prompt, task dependency, traceability row, and reusable media-engine skill now describe the verified source seek. IPC, project, source and timeline schemas/examples, Codex prompts/tools, agent routing, and reference screenshots are intentionally unchanged because this slice adds no new field, edit operation, assistant action or screen layout.
