# P2 progress

Published bootstrap task IDs: P2-01, P2-03 and P2-07; P2-01 through P2-07 remain incomplete until the full phase gate passes.

Implemented an actual bounded stdio JSONL transport and pinned official Codex 0.142.3 bootstrap client. The client verifies the executable version, initializes once, checks its dedicated account-directory identity, reads ChatGPT account type and discovers local skills. Model selection data comes from the runtime and requires a ChatGPT account response; there is no hardcoded catalog. The client exports no generic RPC or editing method to the renderer. No native UI/IPC changes are included in this slice.

The transport bounds UTF-8 lines, pending work and output queues, validates RPC envelopes/IDs, drains stderr without returning private payloads, rejects unexpected server requests and terminates uncertain connections without replay. Main-owned executable, context and account paths plus a minimal environment prevent implicit host credential inheritance. Review found and fixed a close/reconnect overlap race: close now waits for startup and process shutdown before another connect. Model pagination stays on one transport. Descendant-process containment and full tool confinement still require the later runtime policy and negative tests.

Installed the unmodified official package only in the isolated Docker guest and generated its TypeScript/JSON schemas. A reviewed 28-type dependency closure is pinned by original generation hashes; the sync script changes only import extensions and formatting. Upstream Apache-2.0 licensing is preserved. Its regeneration check and two generator-boundary tests passed. Research identifies the experimental no-environment policy and native child inheritance, with pinned source links and explicit unproven runtime/MCP boundaries.

The real signed-out bootstrap test passed two connections, fixture-skill discovery, model-account gating, shutdown/reconnect exclusion and unchanged guidance bytes. Runtime/source/test hashes are recorded before startup. The first attempt failed when saving evidence because a contract transfer target was missing; it remains a setup failure. A later reviewed run passed after the transfer and lifecycle fixes. Account type does not prove managed-token provenance. No credentials were copied, login performed, model turn started, Electron window launched by this test or media edited.

Full checks passed 39 Python and 79 TypeScript tests, 28 schema/example pairs, 20 reference images and 10 invalid quality-contract rejections. Publication review and remote verification follow this record. The final native evidence key wording was corrected to signed-out account and ChatGPT-account gating; this does not claim authentication.

Spec sync updates Codex integration, the bridge README, test-environment guidance, the Codex skill, protocol generation contract, dependency notices, research and traceability. Renderer/IPC, project/edit/media schemas and examples, editing prompts, reference images and transaction routes are intentionally unchanged: none of their behaviors is implemented by this bootstrap. No P2 result or task checkbox is asserted.

At this initial bootstrap checkpoint, the next continuation was durable threads/streaming and the guarded MCP connection to the common transaction foundation. The isolated account was signed out and authenticated model/skill/turn/edit acceptance was still required. Verify the exact experimental no-environment policy, including MCP and native subagents. Both user examples remained private, unimported and unedited; their append order was recorded only in ignored local data.

## Native account/settings continuation

The bootstrap source PR #12 merged at main revision `b144220` after all four Windows/Linux push/PR checks passed. The long Linux check completed successfully; a later cancellation request arrived after completion and did not cause a retry. This is bootstrap publication evidence, not P2 acceptance.

The next source slice packages the official pinned runtime outside ASAR with its Apache licence and hash manifest, resolves only fixed main-owned resources, and adds Appearance/Codex settings sections. Managed browser-sign-in, cancel, sign-out, reconnect, actual runtime model/reasoning selection with application-level persistence, supplied usage and discovered skills are implemented in source. The exact generated protocol closure is now 45 types. Twenty auth tests, four synthetic packaged-resolver tests and twelve controller race/recovery tests passed; packaged signed-out native checks also passed. No successful managed login, authenticated model catalog, thread, turn, shared edit transaction or media edit is claimed.

Spec synchronization covers screens, AI settings, integration, traceability, dependency notices, bridge/test guidance and the Codex skill. Parent-owned Codex view/IPC contracts and tests accompany these account/settings changes. Project/edit/media schemas, editing prompts, transaction routes and reference images remain intentionally unchanged because this slice grants no editing authority. P2 remains incomplete and no result or task checkbox is written.

The integrated account/settings checks now pass 40 Python and 112 TypeScript tests. A real packaged Linux native run passed signed-out connection, managed browser-login initiation/cancellation and reopen; its external browser launcher was explicitly suppressed in the test, so no browser or authentication success is claimed. Guest-only input and full-display inspection verified sections, available skill names, 100% and 200% scale, preference save and Escape focus restoration. The first native test failed because its whole-response text heuristic matched the generic term CODEX_HOME inside a bundled skill description. The corrected check verifies forbidden keys; the unchanged app returned no forbidden keys and passed. Retain that initial failure. The final rebuild uses the clearer Available skills label and passed native login-initiation/cancellation/reopen, 200% scale, guest-only inspection and the existing synthetic import/project/native regression suite. PR #13 merged at verified main revision `8f6b7f65f11f505ce037aa6b3ced76ebc0647055` after all four Windows/Linux push and pull-request checks passed.

## Shared transaction continuation

The shared transaction source slice was published through PR #14 at verified main revision `6bde7c4889984fa1184d2553aab2efa56679e501`. It adds the first common draft transaction reducer required by P2-05. Manual, Codex, and Magic Wand adapters inject trusted authority into the same engine; untrusted requests contain only bounded trim intent and exact project/draft/base/sequence/hash freshness. Complete records are file-synced and atomically promoted into one hash-chained journal. Reopen verifies and deterministically replays every record, ignores recognized incomplete staging records, reports uncertain postcommit outcomes, deduplicates retries by request hash, and restores the newest applicable change through a new undo transaction. Navigation and draft writes share one root queue, and fixture tests verify unchanged project baseline and source bytes. The queue coordinates one application process; the MCP child forwards to this main-owned store instead of becoming a second writer. Directory persistence across power loss remains unproven.

Verified pass checkpoints are stored separately and bind only passed check records to the exact current head and all still-applied transactions in the newest pass. They do not change draft sequence, clear undo, create a revision, or prove the external check was actually performed. The main-owned active-project tool service now exposes path-free project/timeline summaries, the bounded trim and newest-transaction undo to its future MCP adapter; it rejects caller authority, inactive scope, stale heads, forbidden tool names and backend detail. Fourteen FFmpeg-backed transaction and tool tests pass, including exact start/end boundaries and uncertain undo retry. Thirteen thread foundation tests cover application-owned experimental requests, project-thread binding, stream correlation, interruption and raw-data redaction. The current reducer supports one imported main clip's start/end trim only. At that published checkpoint, stdio MCP exposure, client thread wiring, live experimental validation, native committed-state updates, authenticated use, multi-clip editing, full group undo/redo and P3 recovery remained incomplete. No P2 result or task checkbox was asserted.

Repository workflow controls moved from the opaque `.astra` tree into `docs/workflow/`. Ignored generated evidence now lives under `test-results/`, and ignored machine-local inputs live under `local-data/`. Validators, publication guards, tests, prompts, results and skills use the new paths. The two private acceptance sources remain outside Git and are represented only by the ordered local locator.

## Thread, drawer and guarded MCP continuation

The thread, drawer and guarded MCP slice was published through PR #15 at verified main revision `caa647ccc2f51ebdb21311892463b02b24e6af18`. It wires the durable project-thread runtime into the production Codex client. Application-owned requests compile against the pinned experimental protocol. Create/resume identity comes from the main-owned project registry; every turn repeats the empty-environment policy; racing notifications are reduced in order; authoritative terminal state wins; and idle project close performs `thread/unsubscribe`. A known remote rejection may retry with a fresh main-owned message identity. An uncertain transport outcome stays visible and is never replayed. Command, file-change, legacy approval and MCP-elicitation requests are declined or cancelled and quarantine the connection.

The experimental generator was run with official Codex 0.142.3 in the isolated guest. Its complete output remains private, while a reviewed 205-type dependency closure is retained in source with original hashes. Request builders use the generated inputs, including the distinct request-side `"read-only"` mode. Response decoding requires the generated `{ type: "readOnly", networkAccess: false }` sandbox and the fixed OpenAI provider before state reaches the renderer.

Electron main now owns the active project tool service and a token-authenticated local broker. The packaged fixed-hash MCP child exposes exactly `project.get_summary`, `timeline.get_summary`, `cut.trim_edge`, and `timeline.undo`, then forwards bounded name/input only. The process-only random credential is absent from app-server arguments, thread configuration, renderer state and persistent data. App-server startup fails unless `mcpServerStatus/list` confirms the expected owned server version, all four tool names and input schemas, and no resources/templates. The app-server's fixed configuration disables shell and unified execution, JavaScript, web search, browser/computer use, apps/connectors/plugins, remote plugins and image generation.

The project view adds a compact Codex drawer beside the preview. The drawer and source inspector are mutually exclusive. It sends only a bounded user request and renders projected user/Codex text, generic activity and actionable failure/uncertainty; no sample conversation or unconfirmed edit is inserted. Stop maps to the real interrupt path. Timeline, preview and controls continue to come from main-owned committed project state.

Sixteen focused thread-view, production client and MCP tests cover the new request/response boundary, stream races, retry, interrupt, idle unsubscribe, approval denial/quarantine, child protocol, broker authentication/bounds, deep POSIX application-data paths, package tamper, exact tool-schema validation and secret-free app-server configuration. Packaged Linux guest checks passed the product drawer, keyboard/accessibility regression and real signed-out Codex startup with the exact owned MCP name/schema inventory. The focused final-package lifecycle suite passed in Docker after increasing only the fatal-startup observation bound. Earlier missing-asset-dialog timeout failures remain retained and are not rewritten as passes. At this published checkpoint the guest account was signed out, so no model turn, model-side tool invocation, native subagent behavior or committed Codex edit was claimed. P2 remained incomplete, and no P2 result or task checkbox was written.

Spec synchronization changes the Codex integration, simple UI and screen specs, security boundary, tool catalog, AI settings, runtime-policy research, protocol contract, thread view and desktop IPC schemas/examples, fixed in-app prompt, Codex skill, bridge/tool/test guidance, traceability and routing. Project/media/timeline persistence schemas, later editing prompts, reference images/manifest, lossless policy and capture/export routes remain unchanged because this slice changes neither source media nor the master pipeline and does not implement those later workflows.

## Committed draft preview continuation

The committed draft preview slice was published through PR #16 at verified main revision `81eef1b6d85daffc5db44fd81d90a0750d69e3f7`. It makes the project preview, timeline duration and seek controls follow the main-owned committed draft after a bounded trim or undo. Main reads the project and draft under the same serialized store operation, attaches the exact draft identity, base revision, sequence and timeline hash to every renderer view, and binds frame requests and decoded pixels to that head. It checks the head both before and after decoding. A stale request returns only the newer path-free draft state, so the renderer never paints pixels from a superseded edit.

Codex mutation settlement always rereads committed state, including an uncertain postcommit transport outcome, and publishes a validated main-to-renderer update without replacing the original tool result if UI delivery fails. The renderer accepts only a strictly newer matching head, keeps navigation and panel state, clamps the playhead to the committed duration and requests a fresh frame. Source-library playback continues to use the immutable source baseline.

Real FFV1/PCM fixture tests cover start/end trim, undo, source-to-timeline frame mapping, stale-before-decode and mutation-during-decode rejection, detached atomic snapshots, navigation serialization, uncertain postcommit refresh, and observer failure. A packaged Linux Electron run in the isolated Docker desktop verifies that a production manual trim survives reopen, shortens the project timeline, maps output time zero to the retained source frame, advances through committed frames, and leaves the source-library preview and baseline hashes unchanged. The first cold packaged attempt persisted its import and project but exceeded the 30-second first-frame observation; that failed evidence is retained, and an unchanged immediate retry completed. Accessibility, process-lifecycle and signed-out Codex regression checks pass on the same final package. Guest-only inspection verifies the committed duration, source-offset first frame, next retained frame and single-line current/total time without using the host display or input.

This slice remains limited to one enabled main clip without speed changes. It does not establish authenticated Codex invocation, a model-side edit, visible manual trim/undo controls, transcript editing, multi-clip playback, smooth P3 playback or phase acceptance. The project examples remain private and unimported. Prompts, routing, reference images, media/export contracts and later timeline schemas are unchanged because this continuation changes committed preview synchronization only. P2 remains incomplete, and no P2 result or task checkbox is written.

## Recent project-thread history continuation

The recent project-thread history slice was published through PR #17 at verified main revision `c555aa41e06b6239d6bb6501dee62f6148a589da`. It restores a bounded recent projection when the real project thread resumes. It consumes the inline initial full-turn page or exactly one newest-first `thread/turns/list` fallback using the trusted registry thread ID. The reviewed pinned protocol closure grows from 205 to 207 generated types with source hashes for the list request and response. Main validates at most 100 turns and 1,000 unique items, reverses them into chronological order, redacts and retains at most 200 user/Codex messages and 32 generic activities, and replaces all server item identities before typed IPC. Pagination cursors, skill paths, raw tools, arguments, results and reasoning never reach the drawer.

Only the newest turn may be in progress. It and every known item state are seeded before buffered notifications are reduced, reopens the drawer as running, rejects an overlapping turn and remains interruptible. Resume requires a supported thread status, and an inline page must agree with that same response. A separate fallback page is the later authority, so completion between resume and list is absorbed without a false quarantine. Forbidden command, file, web, image, dynamic-tool, foreign-MCP, unknown, approval-waiting, duplicate or contradictory inline history quarantines the unopened conversation. A failed history fallback also leaves the conversation unopened.

Focused protocol, stream, client, runtime and desktop-controller checks pass for inline and fallback pages, omitted wire-default fields, history bounds, chronological redaction, active-turn continuation, contradiction rejection, fallback completion races, transport failure, forbidden-content quarantine and fresh renderer IDs. The metadata audit now generates the same pinned experimental schema used by production and requires `thread/resume` and `thread/turns/list` request definitions. A fresh packaged Linux build in the no-mount Docker guest passed the real signed-out Codex settings/start-cancel/reopen flow, synthetic project and drawer suite, and process-lifecycle suite. Guest-only display capture and review confirmed the actual native drawer beside the project preview with one selected workflow step, no sample messages, and the actionable signed-out error. Browser launch was suppressed and no host display or input was used. A delegated read-only security/protocol review reported no remaining findings after the default-field, fallback-race, required-status, active-item and explicit-bound fixes.

At that published checkpoint, real managed authentication was unavailable in the isolated guest, so authenticated persisted-history resume and subsequent model streaming remained unverified. This code changes no renderer schema, project/media/timeline persistence schema, prompt, routing rule, reference asset or lossless/export boundary. P2-01, P2-03, P2-04 and P2-07 remain partial; no P2 result or task checkbox is written.

## Explicitly authorized authenticated test setup

On 2026-09-12 the user explicitly authorized copying the official runtime credential file into the private app-owned Docker account for this session. Only those bytes were transferred directly, with guest mode 600; the host file was unchanged at transfer. No account directory, configuration, history or mounts were copied, and no secrets were emitted. ADR 0006 records the narrow exception under the current instruction's precedence. Production onboarding remains managed ChatGPT sign-in; this adds no credential-import feature or renderer token input.

The packaged account probe now reports connected/signed_in and runtime model, skill and usage discovery. These are authenticated discovery outcomes, not managed browser-login completion. Earlier signed-out tests and their limitations remain historical evidence. Credential bytes, account identifiers and private locations remain excluded from public records and screenshots.

At this discovery checkpoint, private synthetic-fixture native acceptance was pending: real project turns and guarded reversible edits, committed state/preview with unchanged source and baseline, interruption, history reopen, stale rejection and shared undo. Effective tool restrictions and supported native subagent inheritance required separate checks. Discovery alone proved none of those gates. Both user examples remained private and unimported; no P2 result or task checkbox was written.

This spec-sync pass changes only test setup authorization, current account-access status and the pending authenticated validation plan in ADR 0006, active-phase/progress, native testing guidance, runtime-policy research and the two relevant skills. Production login/IPC, project/media/timeline schemas and examples, prompts, tool routes, references and lossless/export contracts are intentionally unchanged because this exception implements no product behavior.

## Active-project read bootstrap correction

The real authenticated native attempt exposed a missing context input: the app did not supply the active project identifier, so the model could not bootstrap the project/timeline read tools. Main now appends only validated `project_id` and `schema_version: "1.0"` JSON to its developer instructions on create and resume. It supplies no paths, titles or draft snapshot. The read tools remain responsible for current draft identity, sequence and hash, and all active-project and transaction checks remain authoritative.

The desktop Codex regression was adjusted for that context. The failed native evidence is retained; at this checkpoint a corrected packaged build and rerun were pending. This correction alone was not a successful model edit or P2 acceptance. Spec sync updated the fixed in-app prompt documentation, Codex skill, P2-06 acceptance wording and traceability; tool/IPC/persistence schemas, tool inventory, references and lossless/export contracts were unchanged.

## Runtime resource-helper and shutdown findings

The authenticated attempt without project context invoked built-in MCP resource listing and was quarantined under the current editor policy. Pinned upstream source confirms that any configured MCP server brings resource listing/template/read helpers independently of empty environments and the server's four-tool allowlist. Empty resources/templates do not remove those built-ins. No supported disabling switch was found in the reviewed schema. The policy continues quarantining unreviewed items; no permissive parser change or claim of complete built-in-tool absence is made.

The shutdown correction preserves projector poison while allowing disconnect to publish active-turn uncertainty at most once. Exceptions from notification callbacks cannot bypass the transport shutdown barrier. At this checkpoint the corrected native repeat was pending, with failed evidence retained. Account access was available through the explicitly authorized seed and packaged connected/signed_in catalog/usage probe; browser-login completion, a successful authenticated edit and full P2 acceptance were not claimed then. This sync changed lifecycle guidance, the runtime limitation, skill guidance and traceability only; tool schemas, prompts, media precision and export boundaries were unchanged.

## First authenticated trim and projection correction

The third native run completed a real authenticated trim turn. Exactly one Codex-origin trim journal record at sequence 1 reduced the synthetic draft from 1.5 seconds to 1.0 second, and preview matched retained source frame 1 exactly. Offline verification reopened the draft and project frame and confirmed the immutable baseline. These are bounded successful edit outcomes within an otherwise failed run, not a full native-flow pass.

The resumed thread reached ready, then projected-history equality failed. The live projector had emitted a user-message completion as an empty Codex reply. It now tracks and validates the echo without emitting a message because main already owns the sent user text. Owned summary tools now display Reading the project; trim/undo display Applying an edit, without implying a successful commit from activity alone. At this checkpoint the native test was being extended for live update timing and Stop. Preserve this failed evidence. Full history equality, Stop and undo were unverified at that point; native subagent behavior and P2 acceptance remain unverified. Schemas, tool inventory and references were unchanged.

## Passing authenticated synthetic native flow

A later private run passed the expanded `tests/native/codex-authenticated.test.ts` flow against packaged Electron and real signed-in Codex 0.142.3 in the isolated Linux guest. Runtime discovery returned one model and five skills in this run, and the runtime-validated selection persisted. The synthetic project's original duration was 1.5 seconds. A real Codex turn called the guarded tools and committed exactly one Codex-origin start trim at sequence 1, reducing the draft to 1.0 second. The test observed committed duration/seek controls and exact retained source-frame pixels while that turn was still running. It then reopened the actual thread, retained thread identity and matched the projected history. A second real Codex turn used the shared undo tool, appending sequence 2 and restoring the original 1.5-second timeline and first-frame pixels. The source and immutable baseline were unchanged, the journal verified, and offline project/draft reopen agreed with the native view.

A separate real read-only turn was interrupted through Stop while running. It appended no journal transaction, and the same thread reopened with the interrupted turn's history and the restored draft intact. Guest-only native capture separately passed for the packaged window: a click switched the Codex drawer to the Source inspector, leaving one selected panel; no host input was used. Private result, package/runtime/MCP/test provenance, source/baseline/journal and visual screenshot records include hashes. These artifacts stay ignored and are summarized here without private paths, account details, transcripts or raw evidence.

This complete bounded flow supersedes the earlier *current* claims that authenticated model invocation, history equality, Stop, undo or a real Codex edit were still pending. The missing-project-context, resource-helper quarantine and failed history-projection attempts above remain recorded as failures at their respective checkpoints. The successful run does not prove managed browser-login completion, effective denial of every unapproved built-in tool under a live model, native subagent inheritance, or authenticated native stale-protocol rejection. Those P2 gates remain open, along with full phase acceptance. It also does not test user-supplied media, audio listening, Windows hardware/capture/installer or export. No P2 result or task checkbox is written.

Spec sync for this evidence update changes only `docs/08_CODEX_INTEGRATION.md`, `docs/27_TRACEABILITY.md`, `docs/workflow/active-phase.md`, this progress record and `tests/desktop/README.md`. It changes the reported validation status, not production behavior or workflow. Product/screen specs, persisted and IPC schemas/examples, tests, prompts, skill instructions, subagent routing, reference images/manifest, lossless/media contracts and export routes are intentionally unchanged.
