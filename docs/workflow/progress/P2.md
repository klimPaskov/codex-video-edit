# P2 progress

Published bootstrap task IDs: P2-01, P2-03 and P2-07; P2-01 through P2-07 remain incomplete until the full phase gate passes.

Implemented an actual bounded stdio JSONL transport and pinned official Codex 0.142.3 bootstrap client. The client verifies the executable version, initializes once, checks its dedicated account-directory identity, reads ChatGPT account type and discovers local skills. Model selection data comes from the runtime and requires a ChatGPT account response; there is no hardcoded catalog. The client exports no generic RPC or editing method to the renderer. No native UI/IPC changes are included in this slice.

The transport bounds UTF-8 lines, pending work and output queues, validates RPC envelopes/IDs, drains stderr without returning private payloads, rejects unexpected server requests and terminates uncertain connections without replay. Main-owned executable, context and account paths plus a minimal environment prevent implicit host credential inheritance. Review found and fixed a close/reconnect overlap race: close now waits for startup and process shutdown before another connect. Model pagination stays on one transport. Descendant-process containment and full tool confinement still require the later runtime policy and negative tests.

Installed the unmodified official package only in the isolated Docker guest and generated its TypeScript/JSON schemas. A reviewed 28-type dependency closure is pinned by original generation hashes; the sync script changes only import extensions and formatting. Upstream Apache-2.0 licensing is preserved. Its regeneration check and two generator-boundary tests passed. Research identifies the experimental no-environment policy and native child inheritance, with pinned source links and explicit unproven runtime/MCP boundaries.

The real signed-out bootstrap test passed two connections, fixture-skill discovery, model-account gating, shutdown/reconnect exclusion and unchanged guidance bytes. Runtime/source/test hashes are recorded before startup. The first attempt failed when saving evidence because a contract transfer target was missing; it remains a setup failure. A later reviewed run passed after the transfer and lifecycle fixes. Account type does not prove managed-token provenance. No credentials were copied, login performed, model turn started, Electron window launched by this test or media edited.

Full checks passed 39 Python and 79 TypeScript tests, 28 schema/example pairs, 20 reference images and 10 invalid quality-contract rejections. Publication review and remote verification follow this record. The final native evidence key wording was corrected to signed-out account and ChatGPT-account gating; this does not claim authentication.

Spec sync updates Codex integration, the bridge README, test-environment guidance, the Codex skill, protocol generation contract, dependency notices, research and traceability. Renderer/IPC, project/edit/media schemas and examples, editing prompts, reference images and transaction routes are intentionally unchanged: none of their behaviors is implemented by this bootstrap. No P2 result or task checkbox is asserted.

At this initial bootstrap checkpoint, the next continuation was durable threads/streaming and the guarded MCP connection to the common transaction foundation. The isolated account was signed out and authenticated model/skill/turn/edit acceptance was still required. Verify the exact experimental no-environment policy, including MCP and native subagents. Both user examples remained private, unimported and unedited; their append order was recorded only in ignored local data.

## Native account/settings continuation

The bootstrap source PR #12 merged at main revision `b144220` after all four Windows/Linux push/PR checks passed. The long Linux check completed successfully; a later cancellation request arrived after completion and did not cause a retry. This is bootstrap publication evidence, not P2 acceptance.

The next source slice packages the official pinned runtime outside ASAR with its Apache licence and hash manifest, resolves only fixed main-owned resources, and adds Appearance/Codex settings sections. Managed browser-sign-in, cancel, sign-out, reconnect, actual runtime model/reasoning selection with application-level persistence, supplied usage and discovered skills are implemented in source. The exact generated protocol closure is now 45 types. Twenty auth tests, four synthetic packaged-resolver tests and twelve controller race/recovery tests passed; packaged signed-out native checks also passed. No successful managed login, authenticated model catalog, thread, turn, shared edit transaction or media edit is claimed.

Spec synchronization covers screens, AI settings, integration, traceability, dependency notices, bridge/test guidance and the Codex skill. Parent-owned Codex view/IPC contracts and tests accompany these account/settings changes. Project/edit/media schemas, editing prompts, transaction routes and reference images remain intentionally unchanged because this slice grants no editing authority. P2 remains incomplete and no result or task checkbox is written.

The integrated account/settings checks now pass 40 Python and 112 TypeScript tests. A real packaged Linux native run passed signed-out connection, managed browser-login initiation/cancellation and reopen; its external browser launcher was explicitly suppressed in the test, so no browser or authentication success is claimed. Guest-only input and full-display inspection verified sections, available skill names, 100% and 200% scale, preference save and Escape focus restoration. The first native test failed because its whole-response text heuristic matched the generic term CODEX_HOME inside a bundled skill description. The corrected check verifies forbidden keys; the unchanged app returned no forbidden keys and passed. Retain that initial failure. The final rebuild uses the clearer Available skills label and passed native login-initiation/cancellation/reopen, 200% scale, guest-only inspection and the existing synthetic import/project/native regression suite. PR #13 merged at verified main revision `8f6b7f65f11f505ce037aa6b3ced76ebc0647055` after all four Windows/Linux push and pull-request checks passed.

## Shared transaction continuation

The shared transaction source slice was published through PR #14 at verified main revision `6bde7c4889984fa1184d2553aab2efa56679e501`. It adds the first common draft transaction reducer required by P2-05. Manual, Codex, and Magic Wand adapters inject trusted authority into the same engine; untrusted requests contain only bounded trim intent and exact project/draft/base/sequence/hash freshness. Complete records are file-synced and atomically promoted into one hash-chained journal. Reopen verifies and deterministically replays every record, ignores recognized incomplete staging records, reports uncertain postcommit outcomes, deduplicates retries by request hash, and restores the newest applicable change through a new undo transaction. Navigation and draft writes share one root queue, and fixture tests verify unchanged project baseline and source bytes. The queue coordinates one application process; the MCP child forwards to this main-owned store instead of becoming a second writer. Directory persistence across power loss remains unproven.

Verified pass checkpoints are stored separately and bind only passed check records to the exact current head and all still-applied transactions in the newest pass. They do not change draft sequence, clear undo, create a revision, or prove the external check was actually performed. The main-owned active-project tool service now exposes path-free project/timeline summaries, the bounded trim and newest-transaction undo to its future MCP adapter; it rejects caller authority, inactive scope, stale heads, forbidden tool names and backend detail. Fourteen FFmpeg-backed transaction and tool tests pass, including exact start/end boundaries and uncertain undo retry. Thirteen thread foundation tests cover application-owned experimental requests, project-thread binding, stream correlation, interruption and raw-data redaction. The current reducer supports one imported main clip's start/end trim only. At that published checkpoint, stdio MCP exposure, client thread wiring, live experimental validation, native committed-state updates, authenticated use, multi-clip editing, full group undo/redo and P3 recovery remained incomplete. No P2 result or task checkbox was asserted.

Repository workflow controls moved from the opaque `.astra` tree into `docs/workflow/`. Ignored generated evidence now lives under `test-results/`, and ignored machine-local inputs live under `local-data/`. Validators, publication guards, tests, prompts, results and skills use the new paths. The two private acceptance sources remain outside Git and are represented only by the ordered local locator.

## Thread, drawer and guarded MCP continuation

The thread, drawer and guarded MCP slice was published through PR #15 at verified main revision `caa647ccc2f51ebdb21311892463b02b24e6af18`. It wires the durable project-thread runtime into the production Codex client. Application-owned requests compile against the pinned experimental protocol. Create/resume identity comes from the main-owned project registry; every turn repeats the empty-environment policy; racing notifications are reduced in order; authoritative terminal state wins; and idle project close performs `thread/unsubscribe`. A known remote rejection may retry with a fresh main-owned message identity. An uncertain transport outcome stays visible and is never replayed. Command, file-change, legacy approval and MCP-elicitation requests are declined or cancelled and quarantine the connection.

The experimental generator was run with official Codex 0.142.3 in the isolated guest. Its complete output remains private, while a reviewed 205-type dependency closure is retained in source with original hashes. Request builders use the generated inputs, including the distinct request-side `"read-only"` mode. Response decoding requires the generated `{ type: "readOnly", networkAccess: false }` sandbox and the fixed OpenAI provider before state reaches the renderer.

Electron main now owns the active project tool service and a token-authenticated local broker. The packaged fixed-hash MCP child exposes exactly `project.get_summary`, `timeline.get_summary`, `cut.trim_edge`, and `timeline.undo`, then forwards bounded name/input only. The process-only random credential is absent from app-server arguments, thread configuration, renderer state and persistent data. App-server startup fails unless `mcpServerStatus/list` confirms the expected owned server version, all four tool names and input schemas, and no resources/templates. The app-server's fixed configuration disables shell and unified execution, JavaScript, web search, browser/computer use, apps/connectors/plugins, remote plugins and image generation.

The project view adds a compact Codex drawer beside the preview. The drawer and source inspector are mutually exclusive. It sends only a bounded user request and renders projected user/Codex text, generic activity and actionable failure/uncertainty; no sample conversation or unconfirmed edit is inserted. Stop maps to the real interrupt path. Timeline, preview and controls continue to come from main-owned committed project state.

Sixteen focused thread-view, production client and MCP tests cover the new request/response boundary, stream races, retry, interrupt, idle unsubscribe, approval denial/quarantine, child protocol, broker authentication/bounds, deep POSIX application-data paths, package tamper, exact tool-schema validation and secret-free app-server configuration. Packaged Linux guest checks passed the product drawer, keyboard/accessibility regression and real signed-out Codex startup with the exact owned MCP name/schema inventory. The focused final-package lifecycle suite passed in Docker after increasing only the fatal-startup observation bound. Earlier missing-asset-dialog timeout failures remain retained and are not rewritten as passes. At this published checkpoint the guest account was signed out, so no model turn, model-side tool invocation, native subagent behavior or committed Codex edit was claimed. P2 remained incomplete, and no P2 result or task checkbox was written.

Spec synchronization changes the Codex integration, simple UI and screen specs, security boundary, tool catalog, AI settings, runtime-policy research, protocol contract, thread view and desktop IPC schemas/examples, fixed in-app prompt, Codex skill, bridge/tool/test guidance, traceability and routing. Project/media/timeline persistence schemas, later editing prompts, reference images/manifest, lossless policy and capture/export routes remain unchanged because this slice changes neither source media nor the master pipeline and does not implement those later workflows.

## Committed draft preview continuation

The committed draft preview slice was published through PR #16 at verified main revision `81eef1b6d85daffc5db44fd81d90a0750d69e3f7`. It makes the project preview, timeline duration and seek controls follow the main-owned committed draft after a bounded trim or undo. Main reads the project and draft under the same serialized store operation, attaches the exact draft identity, base revision, sequence and timeline hash to every renderer view, and binds frame requests and decoded pixels to that head. It checks the head both before and after decoding. A stale request returns only the newer path-free draft state, so the renderer never paints pixels from a superseded edit.

Codex mutation settlement always rereads committed state, including an uncertain postcommit transport outcome, and publishes a validated main-to-renderer update without replacing the original tool result if UI delivery fails. The renderer accepts only a strictly newer matching head, keeps navigation and panel state, clamps the playhead to the committed duration and requests a fresh frame. Source-library playback continues to use the immutable source baseline.

Real FFV1/PCM fixture tests cover start/end trim, undo, source-to-timeline frame mapping, stale-before-decode and mutation-during-decode rejection, detached atomic snapshots, navigation serialization, uncertain postcommit refresh, and observer failure. A packaged Linux Electron run in the isolated Docker desktop verifies that a production manual trim survives reopen, shortens the project timeline, maps output time zero to the retained source frame, advances through committed frames, and leaves the source-library preview and baseline hashes unchanged. The first cold packaged attempt persisted its import and project but exceeded the 30-second first-frame observation; that failed evidence is retained, and an unchanged immediate retry completed. Accessibility, process-lifecycle and signed-out Codex regression checks pass on the same final package. Guest-only inspection verifies the committed duration, source-offset first frame, next retained frame and single-line current/total time without using the host display or input.

This slice remains limited to one enabled main clip without speed changes. It does not establish authenticated Codex invocation, a model-side edit, visible manual trim/undo controls, transcript editing, multi-clip playback, smooth P3 playback or phase acceptance. The project examples remain private and unimported. Prompts, routing, reference images, media/export contracts and later timeline schemas are unchanged because this continuation changes committed preview synchronization only. P2 remains incomplete, and no P2 result or task checkbox is written.

## Recent project-thread history continuation

The recent project-thread history slice was published through PR #17 at verified main revision `c555aa41e06b6239d6bb6501dee62f6148a589da`. It restores a bounded recent projection when the real project thread resumes. It consumes the inline initial full-turn page or exactly one newest-first `thread/turns/list` fallback using the trusted registry thread ID. The reviewed pinned protocol closure grows from 205 to 207 generated types with source hashes for the list request and response. Main validates at most 100 turns and 1,000 unique items, reverses them into chronological order, redacts and retains at most 200 user/Codex messages and 32 generic activities, and replaces all server item identities before typed IPC. Pagination cursors, skill paths, raw tools, arguments, results and reasoning never reach the drawer.

Only the newest turn may be in progress. It and every known item state are seeded before buffered notifications are reduced, reopens the drawer as running, rejects an overlapping turn and remains interruptible. Resume requires a supported thread status, and an inline page must agree with that same response. A separate fallback page is the later authority, so completion between resume and list is absorbed without a false quarantine. Forbidden command, file, web, image, dynamic-tool, foreign-MCP, unknown, approval-waiting, duplicate or contradictory inline history quarantines the unopened conversation. A failed history fallback also leaves the conversation unopened.

Focused protocol, stream, client, runtime and desktop-controller checks pass for inline and fallback pages, omitted wire-default fields, history bounds, chronological redaction, active-turn continuation, contradiction rejection, fallback completion races, transport failure, forbidden-content quarantine and fresh renderer IDs. The metadata audit now generates the same pinned experimental schema used by production and requires `thread/resume` and `thread/turns/list` request definitions. A fresh packaged Linux build in the no-mount Docker guest passed the real signed-out Codex settings/start-cancel/reopen flow, synthetic project and drawer suite, and process-lifecycle suite. Guest-only display capture and review confirmed the actual native drawer beside the project preview with one selected workflow step, no sample messages, and the actionable signed-out error. Browser launch was suppressed and no host display or input was used. A delegated read-only security/protocol review reported no remaining findings after the default-field, fallback-race, required-status, active-item and explicit-bound fixes.

At that published checkpoint, real managed authentication was unavailable in the isolated guest, so authenticated persisted-history resume and subsequent model streaming remained unverified. This code changes no renderer schema, project/media/timeline persistence schema, prompt, routing rule, reference asset or lossless/export boundary. P2-01, P2-03, P2-04 and P2-07 remain partial; no P2 result or task checkbox is written.

## Explicitly authorized authenticated test setup

On 2026-09-12 the user explicitly authorized copying the official runtime credential file into the private app-owned Docker account for this session. Only those bytes were transferred directly, with guest mode 600; the host file was unchanged at transfer. No account directory, configuration, history or mounts were copied, and no secrets were emitted. ADR 0006 records the narrow exception under the current instruction's precedence. Production onboarding remains managed ChatGPT sign-in; this adds no credential-import feature or renderer token input.

The packaged account probe now reports connected/signed_in and runtime model, skill and usage discovery. These are authenticated discovery outcomes, not managed browser-login completion. Earlier signed-out tests and their limitations remain historical evidence. Credential bytes, account identifiers and private locations remain excluded from public records and screenshots.

At this discovery checkpoint, private synthetic-fixture native acceptance was pending: real project turns and guarded reversible edits, committed state/preview with unchanged source and baseline, interruption, history reopen, stale rejection and shared undo. Effective tool restrictions and supported native subagent inheritance required separate checks. Discovery alone proved none of those gates. Both user examples remained private and unimported; no P2 result or task checkbox was written.

This spec-sync pass changes only test setup authorization, current account-access status and the pending authenticated validation plan in ADR 0006, active-phase/progress, native testing guidance, runtime-policy research and the two relevant skills. Production login/IPC, project/media/timeline schemas and examples, prompts, tool routes, references and lossless/export contracts are intentionally unchanged because this exception implements no product behavior.

## Active-project read bootstrap correction

The real authenticated native attempt exposed a missing context input: the app did not supply the active project identifier, so the model could not bootstrap the project/timeline read tools. Main now appends only validated `project_id` and `schema_version: "1.0"` JSON to its developer instructions on create and resume. It supplies no paths, titles or draft snapshot. The read tools remain responsible for current draft identity, sequence and hash, and all active-project and transaction checks remain authoritative.

The desktop Codex regression was adjusted for that context. The failed native evidence is retained; at this checkpoint a corrected packaged build and rerun were pending. This correction alone was not a successful model edit or P2 acceptance. Spec sync updated the fixed in-app prompt documentation, Codex skill, P2-06 acceptance wording and traceability; tool/IPC/persistence schemas, tool inventory, references and lossless/export contracts were unchanged.

## Runtime resource-helper and shutdown findings

The authenticated attempt without project context invoked built-in MCP resource listing and was quarantined under the current editor policy. Pinned upstream source confirms that any configured MCP server brings resource listing/template/read helpers independently of empty environments and the server's four-tool allowlist. Empty resources/templates do not remove those built-ins. No supported disabling switch was found in the reviewed schema. The policy continues quarantining unreviewed items; no permissive parser change or claim of complete built-in-tool absence is made.

The shutdown correction preserves projector poison while allowing disconnect to publish active-turn uncertainty at most once. Exceptions from notification callbacks cannot bypass the transport shutdown barrier. At this checkpoint the corrected native repeat was pending, with failed evidence retained. Account access was available through the explicitly authorized seed and packaged connected/signed_in catalog/usage probe; browser-login completion, a successful authenticated edit and full P2 acceptance were not claimed then. This sync changed lifecycle guidance, the runtime limitation, skill guidance and traceability only; tool schemas, prompts, media precision and export boundaries were unchanged.

## First authenticated trim and projection correction

The third native run completed a real authenticated trim turn. Exactly one Codex-origin trim journal record at sequence 1 reduced the synthetic draft from 1.5 seconds to 1.0 second, and preview matched retained source frame 1 exactly. Offline verification reopened the draft and project frame and confirmed the immutable baseline. These are bounded successful edit outcomes within an otherwise failed run, not a full native-flow pass.

The resumed thread reached ready, then projected-history equality failed. The live projector had emitted a user-message completion as an empty Codex reply. It now tracks and validates the echo without emitting a message because main already owns the sent user text. Owned summary tools now display Reading the project; trim/undo display Applying an edit, without implying a successful commit from activity alone. At this checkpoint the native test was being extended for live update timing and Stop. Preserve this failed evidence. Full history equality, Stop and undo were unverified at that point; native subagent behavior and P2 acceptance remain unverified. Schemas, tool inventory and references were unchanged.

## Passing authenticated synthetic native flow

A later private run passed the expanded `tests/native/codex-authenticated.test.ts` flow against packaged Electron and real signed-in Codex 0.142.3 in the isolated Linux guest. Runtime discovery returned one model and five skills in this run, and the runtime-validated selection persisted. The synthetic project's original duration was 1.5 seconds. A real Codex turn called the guarded tools and committed exactly one Codex-origin start trim at sequence 1, reducing the draft to 1.0 second. The test observed committed duration/seek controls and exact retained source-frame pixels while that turn was still running. It then reopened the actual thread, retained thread identity and matched the projected history. A second real Codex turn used the shared undo tool, appending sequence 2 and restoring the original 1.5-second timeline and first-frame pixels. The source and immutable baseline were unchanged, the journal verified, and offline project/draft reopen agreed with the native view.

A separate real read-only turn was interrupted through Stop while running. It appended no journal transaction, and the same thread reopened with the interrupted turn's history and the restored draft intact. Guest-only native capture separately passed for the packaged window: a click switched the Codex drawer to the Source inspector, leaving one selected panel; no host input was used. Private result, package/runtime/MCP/test provenance, source/baseline/journal and visual screenshot records include hashes. These artifacts stay ignored and are summarized here without private paths, account details, transcripts or raw evidence.

This complete bounded flow supersedes the earlier *current* claims that authenticated model invocation, history equality, Stop, undo or a real Codex edit were still pending. The missing-project-context, resource-helper quarantine and failed history-projection attempts above remain recorded as failures at their respective checkpoints. The successful run does not prove managed browser-login completion, effective denial of every unapproved built-in tool under a live model, native subagent inheritance, or authenticated native stale-protocol rejection. Those P2 gates remain open, along with full phase acceptance. It also does not test user-supplied media, audio listening, Windows hardware/capture/installer or export. No P2 result or task checkbox is written.

Spec sync for this evidence update changes only `docs/08_CODEX_INTEGRATION.md`, `docs/27_TRACEABILITY.md`, `docs/workflow/active-phase.md`, this progress record and `tests/desktop/README.md`. It changes the reported validation status, not production behavior or workflow. Product/screen specs, persisted and IPC schemas/examples, tests, prompts, skill instructions, subagent routing, reference images/manifest, lossless/media contracts and export routes are intentionally unchanged.

## Published authenticated edit slice and native-child continuation

The authenticated trim/undo, history-echo and shutdown slice was published through PR #19. All four Linux/Windows push and pull-request checks passed, and public main was verified at `65369861a8c6c482d9db6251550383f91bedbcd3`. The passing synthetic guest evidence stays private. P2 is still open.

The next source slice explicitly requests native v1 collaboration as a fallback in app-server and thread configuration. Pinned Codex 0.142.3 can override that fallback from persisted thread state or model metadata; the generated model catalog does not reveal the resolved version. A first native child request ended uncertain before a persisted spawn. A later request briefly projected a generic subagent activity but also ended uncertain; neither produced authoritative child proof. The app-server can broadcast child-thread events on the parent connection, so the project client now ignores ordinary notifications bearing a different validated thread ID before buffering the parent's stream. Malformed identities and forbidden notification methods still fail closed. A focused unit test exercises the filter, and packaged parent turns completed with this code, although none had an actual child to exercise it natively. The completed medium-effort turn had no raw `spawnAgent` item or child thread: model text claiming a spawn was not counted. A planned higher-effort attempt stopped at account startup before a turn and is a setup failure, not native-agent evidence. All failures remain private.

A new strict authenticated native child test requires the parent `spawnAgent` item, one receiver child thread, a completed owned child read call, unchanged source/baseline/journal, and exposed child policy fields. Its final bounded run selected the runtime-discovered `gpt-5.5` model at `xhigh` in packaged Settings and completed the parent turn, but the persisted parent turn page held only user and agent messages and zero collaboration items. A later rollout audit found a successful native spawn and linked child that this first test could not detect. The native window's child-start wording was model text, not a spawn. The test failed its then-current history-only real-spawn gate and is not retroactively counted as a pass. Guest-only visual inspection observed the packaged Codex drawer and Source-inspector switch. The account startup failure and live-timing-sensitive trim regressions are retained as failures; the previously passing trim/undo run remains valid for its exact build. At that checkpoint native-child inheritance, complete forbidden-tool restriction and the full P2 phase remained unverified.

The separate `tests/native/codex-stale-committed.test.ts` continuation passed against a one-transaction draft produced by a real authenticated packaged Codex turn on the current build. With the app closed, the production guarded tool service rejected an otherwise valid request carrying the pre-edit sequence and hash and a fresh request ID as `stale_draft`. Offline replay still matched the committed draft; journal bytes/count, original and managed source bytes, and baseline bytes were unchanged. This proves the shared guard on a real Codex-created revision, not a live App Server/MCP stale-error relay. A later full authenticated trim/undo rerun missed the strict live-timing observation and remains a failed run; the earlier complete pass is preserved for its exact build.

This continuation changes the runtime policy, filtered stream routing, protocol/request tests, strict native child and stale-continuation tests, Codex integration guidance, runtime-policy research and relevant skills. Project/media/draft persistence schemas, editor tool inventory, lossless and export contracts, screen layout and reference images remain unchanged. P2 remains incomplete with no result record or task checkbox.

After PR #20 merged with all four Linux/Windows checks passing, a further bounded packaged-native child attempt tested explicit discovery. The pinned `gpt-5.5` metadata declares search-tool support, and upstream v1 planning conditionally defers native spawn behind tool search when namespace tools are available. The app selected `gpt-5.5` at `xhigh` through Settings; the fresh turn asked Codex to discover and use the supported native spawn tool. The parent turn completed, but the persisted parent turn page contained only the user and three agent messages, with no collaboration item; the history API cannot represent tool-search calls or reconstruct filtered v1 spawn events. A later rollout audit found a successful native spawn and linked child for this run. Guest-only visual review again verified the native window and single-inspector switch. This is another strict test failure, not a supported-subagent claim; effective model tool availability remains unknown.

A new authenticated packaged-native negative tool-policy probe on a fresh synthetic fixture passed its bounded behavioral gate. The app projected ready with a completed Codex reply after a harmless request to list the current directory using a built-in command. Official server history recorded a completed turn with zero command, file, dynamic or unrequested MCP/collaboration items. The journal remained empty and original/managed source and baseline bytes were unchanged; guest-only visual inspection switched from the real Codex drawer to Source details at 1.5 seconds. The model's refusal was classified as a claim, not proof of the tool catalog: the result explicitly records `effectiveToolUnavailabilityProven: false`. An initial audit failed because its separate bare `thread/resume` reapplied defaults; the corrected audit used the production policy request and passed. Neither audit changes the original turn's history. Complete effective forbidden-tool restriction, native-child inheritance, live stale-error relay and managed browser-login completion remain open. This evidence and its failed predecessor stay ignored/private; no P2 result or task checkbox is written.

## Live stale-draft relay through packaged Codex

The strict `tests/native/codex-stale-committed.test.ts` continuation now also reopens a real Codex-edited synthetic project in packaged Electron in the isolated guest and sends a deliberately obsolete sequence/hash through the guarded `cut.trim_edge` MCP tool. Official App Server turn history recorded one real tool call with the old sequence/hash and a failed tool status containing the stale-draft response. The native turn completed; the existing draft, journal bytes/count, original and managed source bytes, and baseline remained unchanged. This closes the narrower live stdio/MCP stale-error relay evidence gap for this fixture. It does not establish full forbidden-tool absence, native child inheritance, managed browser-login completion or P2 acceptance.

The first new native run reached the authoritative audit but failed because the test expected a completed MCP item for a rejected tool call. A private read-only history audit showed the correct failed item and stale response. The corrected strict rerun passed. Both runs and the packaged screenshot remain ignored/private; the screenshot was inspected without host input. A separate read-only inspection of saved guest rollout metadata found native collaboration resolved to v1 in the tested turns, so the prior lack of spawn items is not explained by a disabled collaboration version. It still does not prove the model-visible tool catalog or a child. No P2 result or task checkbox is written.

Spec sync for this evidence update changes the native stale continuation, Codex integration/traceability/current phase status, guest test guidance, and Codex skill validation note. Product behavior, IPC/persistence schemas and examples, guarded tool inventory, prompts, subagent routing, reference images/manifest, media precision and export contracts are unchanged. The accepted lossless/open-source/reference notes remain applicable; no workflow or visual contract changed.

## Published stale relay and signed-in Settings acceptance

PR #22 merged after all four Linux/Windows checks passed; public main was verified at `d3fe3b08f56ef0edc0e90a9e82201d1c4fe23ae4`. The earlier live stale-draft result remains bounded to its synthetic packaged fixture. No phase result or checkbox was written.

A new `tests/native/codex-authenticated-settings.test.ts` run passed against packaged Electron and the seeded signed-in account in the isolated Linux guest. Across two launches it compared the visible ChatGPT plan, runtime-discovered model, remaining-usage window and supplied reset time, and Available skills list to the current bounded IPC state. The second launch remained open for guest-only display capture and an XTEST click that collapsed Available skills; visual inspection confirmed the native modal state change. The test suppressed external browser opening and started no model turn. Its passing evidence and screenshots are ignored/private. It proves signed-in Settings presentation for this runtime response, not managed browser-login completion, Windows acceptance or a stable allowance across future account states.

Review of pinned `ResponseItem` and `v2/ThreadItem` corrected an earlier evidence inference: `thread/turns/list` cannot represent tool-search calls. At that checkpoint, persisted turn history showed no collaboration item; a later private rollout audit found native child threads. The absence of search items in persisted turn history does not establish that search was unused. At that checkpoint the effective model-visible tool catalog and native child inheritance remained unverified. That code/test change was the native signed-in Settings check; product UI, IPC/persistence schemas, prompts, tool inventory, subagent routing, reference images/manifest, media and export contracts are unchanged. Status wording was synchronized in the Codex feature/settings and screen specs, runtime-policy research, active phase, traceability, guest testing guide and relevant skills. P2 remains incomplete.

## Native child rollout correction and strict pass

PR #23 merged after all four Linux/Windows checks passed; public main was verified at `c21c93cee5e2e4baeb00dc4c1ce565df620502b4`. A later private read-only audit of the isolated app-owned Codex account corrected the child inference above. Pinned rollout policy retains v1 function calls and outputs but filters collaboration spawn begin/end events, so parent `thread/turns/list` pages cannot reconstruct those child items. Five persisted successful `spawn_agent` outputs each matched a child rollout and parent link; three children completed the two approved summary reads under matching read-only, never-approval parent/child turn contexts. Earlier failed tests remain failures under their then-current assertions, not retroactive passes.

The strict native test now correlates one fresh parent rollout spawn call/output with the returned child ID, `thread/read` child parent/source, completed child-owned summary calls, and exact inherited turn policy. It reads only bounded official rollout files beneath the isolated account path supplied by App Server, never exposes raw content, and no longer uses a bare audit resume that could reapply defaults. Two fresh runs passed against packaged Electron and separate synthetic fixtures. The first captured screenshot was taken before the renderer finished its ready refresh; the test now waits for the visible working status to clear and input to reenable. The second passing screenshot was visually inspected and shows the settled single Codex drawer. Original and managed source, baseline and journal remained unchanged. No user-supplied video, real hardware, Windows native window or export was involved. All private records/screenshots stay ignored.

This slice updates the strict native test, Codex integration/security research, current screen/phase status, traceability, guest testing guidance and relevant skills. No product behavior, IPC/persistence schema, generated protocol, prompt, guarded tool inventory, reference image/manifest, lossless boundary or export contract changed. Native child read-only inheritance is now verified for the bounded pinned runtime. Managed browser-login completion and complete effective forbidden-tool restriction remain open; P2 has no result record or task checkbox.

## Guest-local managed-login opener

A separate isolated desktop guest was rebuilt with Debian Chromium and `xdg-utils` while retaining UID 1000, dropped capabilities, restricted seccomp, no host mounts/devices/ports, and the existing virtual display. The original authenticated guest stayed untouched. `tests/native/codex-login-opener.test.ts` launched an already reviewed packaged Electron build from a fresh signed-out account, used the real Settings Sign in action and `shell.openExternal`, detected guest Chromium starting on the validated OpenAI authorization route, then cancelled and returned to signed out. A first run reached those assertions but hung during teardown while the browser opener was still running; terminating those test-owned guest processes released it. The corrected test terminates only newly started opener/browser processes before closing Electron and writes a pass record only after teardown. Its next run passed and exited cleanly with no Chromium left; a further run with explicit browser/opener exit assertions also passed. Guest-only X11 capture was visually inspected: the loaded OpenAI Welcome back page appeared in Chromium with the native editor behind it. No host browser or input was used. Private screenshot, package/test provenance and bounded result remain ignored.

This closes the real browser-opener and cancellation evidence gap, not managed browser-login completion: no callback or reconciled signed-in state followed this fresh login attempt. The separate authorized credential seed proves authenticated runtime access but not this browser flow. Complete effective restriction of unapproved built-in tools also remains open. P2 remains incomplete; no phase result or task checkbox is written.

Spec sync for this slice updates the isolated desktop image and strict native opener test, Codex integration/Settings and screen-spec status, active-phase/traceability records, runtime-policy research, guest testing guidance and the Codex/native-testing skills. Product behavior, renderer/IPC/persistence schemas and examples, generated protocol, prompts, subagent routing, reference images/manifest, lossless/media and export contracts remain unchanged because the change only provisions an isolated test browser and records bounded evidence.

## Official device-code sign-in and native completion

The optional Sign in from another device action now starts the official App Server `chatgptDeviceCode` mode, displays only its bounded one-time code and fixed verification page in the selected Settings attempt, and opens that page through a fixed main-owned browser action when clicked. The renderer cannot choose a URL or navigate itself. Cancel, completion, failure, reconnect and closing the panel clear transient code state. A direct pinned-runtime probe confirmed start/cancel support. A fresh-account packaged Linux guest run passed sign-in initiation, guest Chromium page opening, cancellation back to signed out and test-owned browser cleanup; guest-only screenshot inspection confirmed the native Settings code state and browser page. No host window was launched or controlled.

During a separate inspected packaged run, the user completed official verification externally. The same native window received completion, reconciled to a signed-in ChatGPT plan and cleared the one-time code. A separate packaged reopen using that private app-owned account passed authoritative signed-in account and plan reads plus live model and skill discovery. The interactive test was originally written to cancel after inspection; its cancel assertion failed when completion arrived first. That failed result is retained, while the ordinary cancellation run remains a separate pass. The completion and reopen establish this managed device-code route for the tested pinned runtime; they do not prove the browser OAuth callback route, Windows product behavior or complete P2 acceptance. Private code, account data, screenshots and guest artifacts stay outside Git.

This spec-sync pass updates the Codex feature and Settings specs, screen behavior, P2 task wording/status, active-phase and traceability records, runtime research, native testing guide and relevant Codex/native-testing skills. The main implementation also changes typed account/device-login domain, IPC and example contracts, the auth bridge, Settings UI, and focused/native tests. Project/media/draft schemas, guarded edit tool inventory, prompts, subagent routing, reference screenshots/manifest, lossless/media policy and export contracts are intentionally unchanged because sign-in changes no draft or media behavior. Complete effective restriction of unapproved built-in tools remains unverified; leave every P2 task checkbox and the phase result absent until full acceptance.

## API-key provider requirement change

The user later requested API-key AI providers, naming DeepSeek, in addition to Codex. ADR 0014 supersedes ADR 0002's exclusivity while preserving official ChatGPT-managed Codex and all prior bounded evidence. P2-08 through P2-10 cover explicit OpenAI API and DeepSeek fixed-endpoint connections, main-owned protected keys or session-only fallback, provider-specific model discovery, explicit paid-turn initiation, and guarded edits through the same durable draft transaction/undo history. Source implementation and fake-transport tests exercise those boundaries; a packaged isolated native test verifies disconnected provider Settings and rejects an invalid key without retaining it. PR #27 merged this slice after Linux and Windows CI passed. P2 remains incomplete; private native screenshots and test data stay outside Git.

An existing OpenAI API credential was transferred through standard input into the isolated guest for one packaged native catalog test, then the guest file was deleted before the app opened. The app authenticated a real OpenAI `GET /v1/models`, cleared the key input, selected a compatible model, and reopened disconnected after session-only use. The raw catalog also contained base models such as `babbage-002`, which the editor's Chat Completions tool path cannot use. A conservative reviewed-family intersection now presents only live GPT-4.1/GPT-4o text chat models. The private screenshot was visually inspected with no key visible. Account-specific catalog details remain private. No paid generation call, authenticated DeepSeek catalog, or provider-origin draft edit occurred. P2-09 and P2-10 therefore remain open and no phase result is written.

The spec-sync scope includes AGENTS, goal/planning/workflow/tasks/active phase, ADR index and historical supersession, provider/security/architecture/editorial/Settings/screen/QA specs, skills, prompts, routing, traceability, out-of-scope/glossary, risks and acceptance fixtures. Current reference images/manifest, media codec and lossless contracts, existing Codex generated protocol, user-media sequence, and accepted P0/P1 result records remain unchanged. Provider domain/IPC schemas, examples and implementation tests must be synchronized with the actual provider code before a working slice is published.

## Exact-turn private rollout audit for P2-07

The authenticated packaged Linux negative command probe now audits the exact tested turn's bounded, account-confined official rollout after `thread/read`. It classifies raw response items that `thread/turns/list` can omit and passes only with zero tool-search, function, other call and unknown response items. One inspected native rerun passed after selecting a runtime-discovered Codex model in packaged Settings; the turn completed, the guest display showed the real drawer, and journal, original/managed source and baseline stayed unchanged. Two setup reruns failed before the probe, first at the model-selection assertion and then because this account had no saved model choice when opening the conversation; their private records remain failures. This strengthens one-turn non-invocation evidence but does not prove complete effective tool unavailability: pinned MCP resource helpers remain registered. P2-07 and the phase stay open.

This evidence-only slice changes the native test, test guide, Codex/native-testing skills, runtime-policy research, active-phase status, Settings status correction and traceability. Product code, IPC and persistence schemas/examples, Codex prompts and subagent routing, reference images/manifest, lossless/media/export contracts and task dependencies are unchanged. No private rollout text, account identifier, screenshot or credential is published; the complete evidence remains in ignored guest/host test directories.

## Remembered API-provider model selection

The provider key store now encrypts a versioned key-plus-model envelope through Electron safeStorage when a secure backend is available. Legacy key-only ciphertext remains readable and migrates on the next model selection. A new key clears its predecessor's model even if both catalogs contain the same ID; a session-only connection removes protected storage and keeps its model in memory only. On restart, the selected model is restored only after a fresh live catalog intersects the reviewed chat-capable family. A failed protected-model write keeps the previous selection and returns a fixed retry message without exposing key material. Focused unit tests pass for restart, migration, replacement, catalog mismatch and failure. A fresh packaged isolated native Settings run passed the disconnected/rejected-key path and guest-only visual inspection on this build; the current Linux safeStorage backend correctly offered session-only use. Native protected-key restoration remains unverified because that guest backend is not approved for persistence. No paid generation call or user media was involved; P2-09/P2-10 and the phase remain open.

This workflow slice changes the protected internal key format, provider selection runtime, typed IPC issue enum/schema and example, Settings/security/architecture/AI integration and screen specs, P2 task/acceptance text, relevant Codex/security skills, provider research, traceability and focused tests. Codex protocol generation, edit prompts, subagent routing, project/draft/media schemas, reference images/manifest, lossless/export contracts and accepted phase records remain unchanged. The packaged isolated native Settings smoke passed, but it cannot substitute for the missing secure-backend restart proof.

## Packaged protected API model restoration

Task IDs P2-08 and P2-09 gained a bounded packaged native acceptance check. A separate unprivileged, no-mount Linux desktop guest supplied real GNOME Secret Service instead of the ordinary guest's insecure `basic_text` fallback. The packaged Electron window connected an already authorized OpenAI API test key, discovered a compatible live model, saved the model in the protected key record, closed, reopened and revalidated that model against a fresh live catalog. The reopened renderer key input was empty, the key/model plaintext was absent from the protected file, and Remove key deleted the protected record and selection. Guest-only X11 capture showed the actual reopened Settings state. The credential, account data, keyring, screenshot and test output remain ignored/private. No paid completion or user-media operation occurred.

The reproducible Dockerfile, guest wrapper and Playwright Electron test are now source-controlled. The earlier ad hoc keyring pass and the later wrapper pass are separate passing runs; this record describes the latter. Scope is Linux GNOME Secret Service and OpenAI model discovery, not Windows/macOS keyring behavior, DeepSeek authentication or a provider edit. P2-08 through P2-10 remain unchecked and no P2 result record is written. This testing-only slice updates the native test and guest instructions, API settings/security/Codex specs, research, current phase and traceability, plus the Codex, security and native-testing skills. Product behavior, IPC/persistence schemas and examples, model adapters, prompts, subagent routing, media/export contracts and reference images/manifest are unchanged.

## Packaged synthetic API-provider draft turn

P2-10 gained a packaged Linux Electron integration test with a synthetic main-process OpenAI transport. The native UI imported an FFV1/PCM fixture, selected a session-only provider model, showed the adjacent billing/context notice and sent a request through the real bounded conversation and shared draft service. The first turn committed one `api_provider` start trim; the journal and seek controls updated while the response was held. A guest-only X11 capture showed the retained frame in the native preview before that response settled, and exact canvas pixels matched the retained frame afterward. An obsolete-head call failed without another transaction. A later API-provider undo restored original draft pixels, a provider rejection returned a fixed redacted error without key/raw response in the local conversation, and the project reopened with the unchanged original and managed source and baseline. The test permits only the fixed provider URLs and uses no external completion or real paid credential. Guest-only screenshots and full results remain ignored/private. Early timing-sensitive preview attempts failed; two later complete runs passed with native capture and preview-settlement checks. The failed records remain failures.

The provider client now resolves the main-process `fetch` binding at call time so an isolated packaged test can install its deterministic transport after launch without a renderer-visible override or production endpoint switch. Unit/contract checks and source review must still pass. This evidence does not satisfy authenticated OpenAI or DeepSeek edit acceptance, real model compliance, Windows native behavior or any user-media workflow. A concurrent read-only P2-07 source audit found the pinned Codex runtime registers MCP resource helpers and a plan tool alongside the four owned MCP tools whenever an MCP server exists; no supported exact built-in allowlist was found. Full effective model-visible tool restriction therefore remains open. P2 has no accepted result or task checkbox.

Spec sync for this slice changes the provider transport binding, packaged native test and guest route, API integration/settings/screen/security descriptions, research, active-phase and traceability records, and relevant Codex/security/native-testing skills. Typed IPC, project/draft persistence schemas/examples, provider endpoints and model-family policy, editing prompts, subagent routing, reference images/manifest, lossless/media/export contracts, and accepted phase records are intentionally unchanged.

The first PR CI pass found one Windows-only transient `EBUSY` while a Codex-client test removed its temporary directory immediately after child-process shutdown; the other Windows job and both Linux jobs passed. That test now uses Node's bounded retry for temporary-directory cleanup. This changes test cleanup only, not Codex transport behavior or the phase acceptance claim. Rerun CI before merging.

## DeepSeek packaged draft fixture

The same packaged native fixture now selects DeepSeek with a synthetic `deepseek-chat` catalog and a main-only transport constrained to DeepSeek's fixed model and Chat Completions routes. Two complete guest runs passed the FFV1/PCM import, explicit Send, committed live trim, stale rejection, shared undo, redacted failure, immutable source/baseline and project reopen; a fresh OpenAI regression run also passed. Guest-only X11 capture showed the DeepSeek drawer and retained frame while the response was held. Two earlier DeepSeek attempts failed the expensive full-buffer undo-preview assertion even though the captured window later showed the restored frame; the test now checks bytes directly and retains first-mismatch diagnostics. Those failed attempts remain failures. No real DeepSeek credential, external completion, paid turn or user media was used. P2-08 through P2-10 remain unchecked.

This slice extends the native fixture and its guest instructions, active-phase, API-provider screen/integration/security descriptions, traceability, provider research and relevant skills. Product routes, IPC and persistence schemas/examples, provider policy, prompts, subagent routing, references, media/export contracts and accepted phase results do not change.

## Authenticated OpenAI turn and provider 429 recovery

An exact-main packaged Electron build in the unprivileged isolated Docker guest imported a synthetic FFV1/PCM project, connected an authorized session-only OpenAI key, discovered a compatible live model and explicitly clicked Send in the native drawer. The first completion failed without a committed edit. A second instrumented run forwarded real requests unchanged and recorded only the completion HTTP status: 429. Its journal remained at sequence zero, the source and baseline stayed unchanged, and the private key file was removed before the app launched. No raw provider body, key, project or screenshot is published. These are failed authenticated-edit attempts, not accepted P2-10 evidence; the status alone cannot identify a rate versus quota cause. No further paid retry is warranted until account state changes.

The follow-up product change maps the provider's `rate_limited` condition to a fixed actionable rate-or-quota message in the local conversation and typed IPC projection. A failed turn keeps its submitted user message and commits no transaction. Focused service tests verify the message, unchanged journal and absence of raw error or key. Fresh packaged OpenAI and DeepSeek native synthetic runs passed trim, stale rejection, shared undo, provider failure, 429 failure, immutable source/baseline and project reopen. Guest-only X11 capture of the actual OpenAI window showed the fixed message in the single selected drawer. These deterministic runs do not establish a successful paid model edit. The live attempt used the earlier exact-main package; the new 429 UI was verified through the subsequent synthetic package, not the paid run.

Spec-sync scope for this behavior includes the provider error mapper, strict thread-view/IPC schema and example, screen/Codex/settings/security specs, P2 research, traceability, active phase, native and unit tests, guest instructions and Codex/native/security skills. P2-08 through P2-10 acceptance text and dependencies remain unchanged because authenticated editing is still missing. Codex prompts/subagent routing, reference images/manifest, media codec/lossless/export contracts, existing project and draft schemas, and accepted P0/P1 results are unaffected. No P2 result or task checkbox is recorded.

## API-connection recovery and drawer containment

A later main-owned provider generation call can return HTTP 401/403 after a key was accepted at connection time. The adapter already classified that result as `authentication_failed`; the conversation previously flattened it to a generic request error. It now publishes a fixed key/account-access recovery message in the strict thread view and IPC schema, retaining only the user's request and no raw provider detail. The native OpenAI and DeepSeek synthetic routes each injected 401 and passed unchanged-journal, redacted-history, shared undo, source/baseline and project-reopen checks. A focused unit test covers the persisted message. No live DeepSeek credential or paid completion was used.

Guest-only visual inspection found the first native error below the visible drawer, despite a passing text assertion. The first fixed-height revision kept it in the viewport but let it overflow the drawer and overlap transport controls. A later bounded drawer with an independently scrolling history passed default-size OpenAI and DeepSeek packaged native runs. A 1366×768 OpenAI run initially failed its screenshot pixel probe because resizing moved the preview away from that fixed absolute coordinate; the underlying live trim had visibly committed in the private screenshot. The probe now scans a bounded preview region for the retained synthetic frame. The complete compact rerun passed the native edit, recovery and reopen sequence, asserted actual window dimensions and verified that the alert is fully inside the drawer, in the viewport, and accompanied by at least 80 px of conversation history. Guest-only screenshots were inspected; all failed and passing evidence remains ignored/private.

The authenticated fixture harness now accepts provider-specific private OpenAI or DeepSeek key files, selects a live-supported reviewed model, and observes only completion status codes. It has not been rerun with a paid key in this slice: OpenAI's prior 429 remains an account-side access limit, and no DeepSeek key is available. P2-08 through P2-10 remain unchecked. This spec-sync updates architecture, screen/AI/security/acceptance specs, strict IPC schema/example, research, phase/traceability, unit/native fixtures, guest guide, and the Codex, native-testing, security, simple-desktop and reference-fidelity skills. The four guarded tool contracts, Codex prompts/subagent routing, task dependencies, current reference images/manifest, lossless/media/export contracts, and accepted P0/P1 records are unchanged. No P2 result is written.

A separate 200% scale pass first failed because the independently scrolling message history collapsed below the native test's 80 px usability gate. The final drawer reserves 90 CSS px of history, permits internal scrolling and reveals a changed error. A complete packaged 200% OpenAI synthetic run then passed edit, error, source/undo and reopen checks, with guest-only screenshot inspection. The preview and full conversation are inspected after scrolling between them at that scale; no simultaneous fit is claimed. Final default-size OpenAI/DeepSeek and 1366×768 OpenAI reruns also passed after this last CSS change. The screenshot probe needed to scroll the preview into view at 200%; the earlier failed fixed-region observation remains failed evidence.

## Tagged H.264 still-frame import and preview foundation

The media library now enables still-frame preview for a narrowly verified H.264 `yuv420p` limited-range BT.709 profile with square pixels and no display matrix. FFmpeg explicitly converts to BGRA only for the native-size renderer transport; the original H.264/AAC bytes, source metadata and project baseline remain unchanged. Higher precision/HDR, unknown color, rotation and non-square-pixel variants remain unavailable for preview. The converted pixels are not a canonical render or lossless-master input. This capability does not make compressed imports lossless.

Synthetic unit tests passed source/managed byte equality, three distinct deterministic decoded frames, preview-profile rejection, and project trim/stale mapping. A packaged Linux Electron fixture passed native picker import, exact canvas-to-decoded-display pixel comparison at three times, project reopen, and a guest-only mouse-driven visible seek. After that synthetic gate, both supplied private recordings were transferred with matching hashes into the no-mount guest and imported through the native picker in order. Each preserved source bytes, created a project and displayed its first native-size frame. An initial private harness attempt sampled the UI while the frame was still loading and failed; its screenshot showed the pending read. The corrected harness waited for decode and passed both imports. All private paths, hashes, projects and screenshots remain ignored. The two recordings are still separate projects, with no appended timeline, full A/V review, edit or export claim.

An official pinned-runtime source audit also checked experimental App Server dynamic tools as a possible replacement for MCP tool exposure. The channel exists, but the pinned planner still registers `update_plan` and fresh native children without full-history forks lose the dynamic tool specs. Switching would conflict with the accepted MCP contract and would not establish the exact four-tool model-visible restriction. No protocol migration or P2-07 acceptance follows from this audit.

Spec sync changes the media-library preview implementation, synthetic media/project/native fixtures, architecture/import/media/lossless/screen and screen-acceptance descriptions, media-library schema description, P3 task-progress note, active phase, traceability and the media/lossless/native-testing skills. The schema shape and BGRA example, typed IPC, draft transaction and Codex tools, prompts and subagent routing, current reference images/manifest, publishing policy and accepted P0/P1 results are unchanged. P2 and P3 remain incomplete, so no phase result or task checkbox is written.

## Manual Edit controls on the shared draft journal

The Edit stage now exposes playhead-based Trim start, Trim end and newest Undo for the current one- or two-source clip. Strict path-free IPC carries only clip intent and the expected committed head; Electron main binds the active project, injects the manual origin and generated IDs, commits through the same journal used by Codex and API providers, and publishes authoritative clip mapping even after a rejected or uncertain operation. The renderer disables invalid boundaries and reconciles committed clip maps from assistant turns before another manual action. No source or immutable baseline is modified.

The full local check passed 42 foundation tests and 242 media tests, with 2 documented skips. A packaged Linux Electron synthetic run imported two sources, trimmed the first clip, verified the second clip reflow and preview, undid, reopened and preserved source/baseline bytes. Guest-only visual inspection saw the real Edit controls enabled inside a clip and disabled at the join. After that fixture passed, the two supplied recordings were privately reimported in order in the isolated guest, manually trimmed by one second, undone, reopened, and checked for original/managed hash and baseline preservation. The private window was visually inspected at the join. Earlier private import-harness timing failures remain recorded in P3 progress; this run passed. The final packaged synthetic rerun passed after a minor accessibility/error-visibility adjustment. This is still-frame manual editing, not a finished audio/video review or export.

An enlarged-interface native rerun also passed after scrolling the committed Edit controls into view and checking their horizontal bounds; guest-only capture showed the actual 200% window. Two preceding test attempts failed on harness assertions: one sampled zoom before the asynchronous Settings save closed, and another assumed Playwright supplied a viewport size for an Electron window. The corrected test waits for the dialog and reads the real window width. Neither failure is counted as product acceptance.

Spec sync covers Edit/architecture/timeline/screen requirements, P2/P6 task notes, typed IPC schema/example, path-free clip projection, renderer reconciliation, unit/native fixtures, reference implementation notes and relevant timeline/native/simple-UI skills. Codex tool inventory and prompts, model routing, source/baseline schemas, reference images/manifest, recording, master-export and lossless policy are unchanged. P2-05 and P6-01 remain partial; P2-07 tool restriction and authenticated API-provider gates remain open. No P2 result or checkbox is written.

## Interior manual split on the shared draft journal

The next partial P2-05/P3-04/P6-01 slice adds a strict `projects:manual-split` request and an Edit-stage Split action at an interior playhead position. Main binds the active project and generates the manual authority and operation identity; the journal stores the two half-open fragments and deterministic inverse. The left fragment keeps its clip ID, the right ID derives from the trusted operation, source order and total duration remain unchanged, and the projected map is refreshed for the next edit. Fragment count is bounded to 4096. The source files and immutable baseline remain unchanged.

`npm run check` passed 42 foundation tests and 249 media tests, with two documented skips. The packaged Linux Electron synthetic two-source fixture passed Split, trim of the resulting fragment, two undos, reopen, exact decoded-display checks at the source join and new fragment boundary, and immutable source/baseline checks. Guest-only input and screenshots inspected the actual native window at 200% interface scale. After the synthetic fixture passed, both supplied private recordings were imported in order into one draft inside the no-mount guest; native Split, fragment trim, two undos, reopen, original/managed source hashes and baseline checks passed. The actual guest native window was visually inspected. All private footage, locations, hashes, projects and screenshots remain outside Git. This is still-frame editing evidence, not a full A/V watch-through, Codex or Magic Wand editing of the supplied footage, master rendering or export. P2-05, P3-04 and P6-01 remain unchecked, and no phase result is written.

Spec sync covers native architecture, editor and screen behavior, P2/P3/P6 task notes, active phase and progress, IPC/transaction/timeline schemas and examples, tests, traceability, reference corrections and timeline/native/simple-UI skills. Codex tool inventory and prompts, provider routing, source/baseline schema, reference images/manifest, recording, master-export and lossless policy are unchanged; the split adds no new AI authority or render path.

## Marked ripple range cut on the shared draft journal

The next partial P2-05/P3-04/P6-01 change adds `projects:manual-range-cut` and compact Mark in, Mark out, Cut range and Clear controls in Edit. The marks identify a nonempty, non-whole-draft half-open output interval at an exact committed head. Main binds the active project, supplies manual authority and generated IDs, and commits a `ripple_delete` through the same journal as trim, split, Codex and API-provider operations. The cut can cross a fragment/source join or remove one source's whole visible span while retaining all immutable source inventory. The reducer records full before/after maps and a deterministic inverse; newest Undo restores them, and committed preview mapping must refresh after settlement.

`npm run check` passed 42 foundation and 255 media tests, with two documented skips. A packaged Linux Electron synthetic two-source fixture passed a marked cut across the source join, exact surviving decoded frame at the new join, Undo, reopen and immutable source/baseline checks. Guest-only visual inspection first found the default-size Cut range controls below the fold. The Edit preview then reserved 70 px, and a final packaged synthetic rerun passed Cut range in the default viewport, exact restored frame after Undo, 200% window bounds and guest-only inspection. After that fixture gate, both supplied private recordings were imported in order inside the isolated no-mount guest. Their native two-second cross-source cut showed the exact decoded second-source frame at the new join; Undo, reopen, original/managed source hashes and immutable baseline also passed, with guest-only native visual inspection. Private footage, paths, hashes, projects and screenshots remain outside Git.

Spec sync covers native architecture, editor/live-state/screen behavior, P2/P3/P6 task notes and phase prompts, typed IPC and transaction schemas/examples, unit/native fixtures, traceability, reference correction notes and timeline/native/simple-UI skills. Codex MCP inventory and prompts, provider routing, source/baseline schema, reference images/manifest, recording, master-export and lossless policy are unchanged. This is still-frame cut evidence, not synchronized A/V review, Magic Wand or Codex editing of supplied footage, canonical rendering or export. P2-05, P3-04 and P6-01 remain unchecked, with no phase result.

## Guarded AI range-cut exposure in progress

The next partial P2-05/P2-06/P2-10 slice adds `cut.delete_range` to the exact application-owned MCP inventory and to the fixed OpenAI API/DeepSeek function adapters. The intent carries a current project/draft/baseline identity, exact sequence and timeline hash, a nonempty half-open output interval, pass group and reason. Main binds the active project, assigns trusted provider origin and operation identity, then uses the same `ripple_delete` reducer and newest Undo as the manual range cut. Reads and returned mutation state remain path-free; source inventory and baseline remain immutable. The current two read tools cannot inspect transcript, speech or joined A/V, so an AI turn can apply a user-specified time cut but cannot claim to have selected filler or verified a seamless spoken join.

`npm run check` passed 42 foundation and 259 media tests with two documented skips. One preceding full run hit a Windows `EPERM` while renaming a fresh fixture directory; the isolated failing test and the complete rerun passed without a code change. Focused service tests passed Codex and API-provider origin, stale/invalid/whole-draft rejection, journal and shared Undo; MCP tests passed the exact five-tool schema; the API adapter passed function routing. A packaged Linux Electron synthetic two-source run then used a real signed-in Codex App Server turn to commit one cross-source `cut.delete_range`, observed the transaction while the turn ran, verified the surviving decoded join frame, used manual Undo, reopened, and checked original/managed source and baseline integrity. Guest-only visual inspection confirmed the actual native window. After that fixture gate, the two supplied recordings were imported in order into another private combined project inside the no-mount guest. A real Codex turn committed the requested two-second cross-source cut; native committed preview, manual Undo, reopen, exact decoded join-frame equality and original/managed source and baseline integrity passed, with guest-only window inspection. The test leaves the private combined draft restored after Undo, not editorially finished or exported.

Two earlier private test attempts remain failed evidence: one exhausted the Node test harness heap by repeatedly transferring a full-resolution canvas, and one asserted before the reopened 4K seek label/frame settled. The corrected harness computes the canvas hash inside the secure native renderer, waits for a decoded nonzero-size frame and the completed seek, and passed the same full private path. A separate read-only reopen diagnostic also matched the canonical second-source frame hash; it was not counted as a substitute for the full run. Private footage, paths, hashes, projects, credentials, model text and screenshots remain outside Git. OpenAI's last live paid turn returned 429 and DeepSeek has no authorized live key, so neither has authenticated edit evidence. P2-07's built-in-tool confinement issue is unchanged by expanding the owned server to five tools. No transcript/audio evidence or continuous A/V watch-through exists for a meaning-preserving cut, and Magic Wand/export remain open. P2 is incomplete, with no checkbox or result record. Spec sync changes Codex integration/security/tool-catalog and live-state specs, the current tool-manifest example, two catalog descriptions (exact trim time without snapping and half-open range cut), fixed editor prompt and skill, P2 task/phase/progress/traceability, fixture guidance, native test and pinned runtime-policy wording. The catalog membership and schema shape, typed IPC, source/baseline model, reference images/manifest, lossless/media/export policies and accepted P0/P1 records are unchanged.

## Guarded AI split exposure in progress

The next partial P2-05/P2-06/P2-10 slice exposes the already journaled interior split as `cut.split`, the sixth application-owned Codex MCP tool and a fixed OpenAI API/DeepSeek function. It requires the current project/draft/baseline identity, sequence and timeline hash, clip ID, exact interior output time, pass group and reason. Electron main supplies trusted origin and operation identity. It commits through the same reversible `split` reducer and newest Undo as manual Split, preserving source bytes and draft duration. Main rereads the committed draft after the tool settles, so the three-fragment projection and preview mapping come from the journal rather than model prose. The two current reads still cannot identify a semantically useful speech boundary.

`npm run check` passed 42 foundation and 262 media tests, with two documented skips. Two preceding Windows full-suite attempts hit intermittent `EPERM` while renaming newly created fixture directories; the isolated affected test and the final complete rerun passed without a product code change. Focused Codex/API-provider service tests cover exact intent, stale and boundary rejection, trusted origin, journal replay, newest Undo and immutable bytes; the API adapter test verifies `cut_split` maps to `cut.split`, and MCP tests verify the exact six-tool schema. A packaged Linux Electron run in the isolated UID 1000/no-mount guest imported two synthetic sources and used a real signed-in Codex turn to split the first clip at 0.5 seconds. The committed three-fragment map was observed while the turn ran; exact decoded display frames at the start, split and source join matched the native canvas. Manual Undo, project reopen, journal and original/managed source and baseline integrity passed, with guest-only visual inspection of both the Codex drawer and reopened Edit window. The first native attempt asserted journal length before asynchronous Undo settlement and failed; the corrected wait-based harness passed. Private account, projects, hashes and screenshots remain ignored and unpublished.

The next partial P2-08 through P2-10 slice adds a third fixed Gemini API-key provider under ADR 0015, beside OpenAI API and DeepSeek. Settings and the single selected Assistant drawer expose separate key/model state; the main process pins Google's compatibility endpoints, intersects live catalog IDs with reviewed stable text/function models, and keeps provider generation behind explicit Send. The bounded local adapter relays Gemini 3 function-call thought signatures only inside the active main-process tool loop, rejects malformed/foreign metadata, and sends all edit intent through the existing guarded journal. Strict provider/thread IPC schema and example now enumerate all three providers. No arbitrary endpoint or renderer-held key was added.

Focused transport, domain, key-store and conversation tests passed. The first complete Windows `npm run check` found a stale two-provider fixture and an unrelated `EPERM` fixture-directory rename; the stale fixture was corrected. Two further parallel Windows checks and one serialized media run each hit a different intermittent fresh-directory rename `EPERM`; the final focused affected test passed. In the isolated Linux guest, typecheck, lint, formatting, 42 foundation and all 273 media tests passed with no skips after a test-only Python command alias was supplied. Its combined `npm run check` then stopped at contract validation because that guest lacks Python `jsonschema`; host `npm run check:contracts` passed after correcting a schema-count patch that initially applied to `projectView.sources` instead of `apiProvidersView.providers`. These failed attempts are retained as failures, not retroactive passes. Packaged Linux Electron in the isolated UID 1000/no-mount guest passed the Gemini disconnected/rejected-key Settings state and a main-only synthetic Gemini 3.1 Flash-Lite tool loop: committed trim and native preview, exact thought-signature replay, stale rejection, shared Undo, redacted provider failures, immutable source/baseline and reopen. A guest-only screenshot of the actual native window showed the selected Gemini drawer and committed preview. OpenAI and DeepSeek synthetic native regression flows passed with the same package. All synthetic model text and signatures are test data. No authorized Gemini key, live Gemini catalog, accepted completion parameters, paid tool loop or authenticated signature replay was available. OpenAI's previous live 429, DeepSeek's missing key, P2-07 effective-tool restriction, Magic Wand, full A/V review and master export remain open. No P2 task checkbox or result is written. Spec sync covers ADR, product/workflow/architecture/AI/security/screen specs, task/phase/traceability/research/progress, strict IPC schema/example, provider transport/domain/UI tests, native fixture/guest guide, master prompt and relevant skills; Codex App Server prompts and owned MCP inventory, subagent routing, references/manifest, source/timeline/lossless/export schemas and accepted P0/P1 results are unchanged.

This does not prove a meaning-preserving speech edit, synchronized A/V playback, Magic Wand, master export, authenticated paid API edit or complete forbidden-tool confinement. A review of official Codex 0.155.1 found a supported switch for its plan tool but the three MCP resource helpers remain whenever a server is configured, with no released public exact effective-tool allowlist; the pinned runtime is unchanged and P2-07 remains open. P2/P3/P6 task checkboxes and phase results stay open. Spec sync covers the current tool manifest example and foundation fixture, Codex integration/security/tool-catalog/editor/live-state/selector and editorial specs, API and Codex prompts, skill and pinned-runtime research, P2 task/phase/progress/traceability, native fixture matrix and guest guide. The conceptual tool contract already listed `cut.split` and its description now matches the implemented exact interior intent; its schema shape, typed IPC, source/baseline contract, provider routing, subagent routing, references/manifest, lossless/export policy and accepted P0/P1 records are unchanged.

## Luna subscription default and pinned-runtime update (partial)

ADR 0016 sets `gpt-5.6-luna` with `high` reasoning as the fresh ChatGPT subscription default only when both values are present in the connected official runtime catalog. A saved valid user selection still wins; an unavailable pair leaves selection unset with an actionable Settings message. Official Codex 0.155.1 and its matching `codex-code-mode-host` are packaged and hash-checked, and the exact experimental TypeScript dependency closure and protocol contract were regenerated. The packaged request and resume policy disables unrelated apps and code-mode namespaces, including external app tools and native child-agent tools. Native agents are temporarily unavailable until P2-07 can prove inherited tool confinement. No Astra agent is authorized or spawned by this slice.

The synthetic packaged Linux Electron run passed a live Luna/high model selection, a bounded read-only code-mode namespace probe with only the owned editor tool family visible, a real guarded split committed while the turn ran, exact decoded frame checks, shared manual Undo, reopen and immutable input checks. The first private two-recording rerun lost its Codex connection during that same probe and remains a failed attempt; its cause is not established. An unchanged complete rerun passed the probe, guarded split while the turn ran, exact native start/split/join frame checks, shared manual Undo, reopen and immutable originals, managed sources and baseline. Guest-only visual inspection at the joined footage showed the actual packaged native window. This probe is evidence for the tested turns, not a permanent upstream effective-tool allowlist. P2-07, native subagents, Magic Wand, continuous A/V review and lossless export remain open; no P2 result or task checkbox is written.

The fresh-preference packaged Settings test also passed two native launches against the signed-in live catalog. Both selected `GPT-5.6-Luna` and `high` and the second guest-only visual capture confirmed the actual Settings control. The same new package passed the OpenAI, DeepSeek and Gemini synthetic native draft regressions in separate isolated runs. Local typecheck, lint, formatting, 42 foundation tests, 275 of 277 media tests with two skips, and contract validation passed after the runtime and skill changes. Publication checks remain a separate final gate for this slice.

## Explicit Luna tool switches and interruption regression (partial)

The current 0.155.1 App Server launch and thread create/resume policy now explicitly disables the supported plan tool, user-input tool and native agents, alongside the existing app and code-mode namespace restrictions. This protects against those three capabilities being re-enabled by account defaults; it is not a complete upstream effective-tool allowlist. The packaged Linux Electron synthetic two-source run passed a live Luna/high code-mode probe: one owned editor tool was callable, the tested app, goal, plan, user-input, skill, spawn, image, web and shell properties were undefined, and `ALL_TOOLS` contained no unowned nested entry. The same real turn then committed a guarded split during execution, passed exact decoded start/split/join frames, shared manual Undo, reopen and immutable source/baseline checks. Guest-only visual inspection showed the real packaged Edit window. A separate Luna/high authenticated synthetic run on the final restricted package passed real trim, Undo, durable thread/history reopen, Stop on a read-only turn and unchanged journal after interruption. The old 0.142.3 native-child evidence remains historical, and P2-07 plus authenticated paid provider gates remain open. No P2 task or result is marked complete from these partial checks.

A final-package authenticated Luna/high forbidden-command probe also passed. The exact parent turn correlated to the private official rollout with zero tool-search, function, other-call or unknown raw response items; its persisted turn completed and the draft journal, source and baseline stayed unchanged. Guest-only inspection showed the settled packaged native drawer. The model's refusal text is a claim, while the raw-turn audit proves only no invocation for that turn. Original-turn complete tool-catalog introspection remains unavailable, so `effectiveToolUnavailabilityProven` is false and P2-07 remains open. Full local `npm run check` passed 42 foundation and 275 media tests with two documented skips plus typecheck, lint, formatting and contract validation; an initial formatting pass failed on one new test assertion layout and was corrected before the complete passing rerun. Spec sync covers task/phase/progress/traceability, Codex/security/tool-catalog specs, runtime research, three relevant skills, subagent routing, client/thread policy and native/unit probes. Persisted schema, IPC, media profiles, references/manifest, lossless and export contracts are unchanged.

## Authenticated DeepSeek fixture and Gemini live harness (partial)

The user supplied a DeepSeek key in response to the live-test request. The key was entered without terminal echo into a mode-600 file inside the isolated UID-1000 Docker guest; the authenticated harness read and unlinked it before launching packaged Electron. It imported a synthetic FFV1/PCM source through the native picker, discovered a live reviewed DeepSeek model, selected it in Settings, and made one explicit Send. Three completion requests returned HTTP 200. The provider called the owned summary and guarded edit tools and committed exactly one `api_provider` start trim at draft sequence 1, reducing the synthetic timeline from 1.5 to 1.0 seconds. Native canvas pixels and seek extent matched the committed edit. A packaged reopen retained the draft and preview, left source and managed-source hashes and baseline bytes unchanged, and did not restore the session-only key. The persisted conversation did not contain the key. The private screenshot was inspected and remains ignored; no media, key, project or screenshot is staged for publication. This is a real paid-provider fixture edit, not speech, Magic Wand, full A/V review or export acceptance.

The live native fixture harness now accepts a Gemini key path and selects only a reviewed Gemini 3 editing model. For a future authenticated turn it counts returned thought signatures and exact assistant-call replay in Electron main memory, recording only counts and a mismatch flag in private evidence; signature bytes are neither logged nor persisted by this audit. The harness extension typechecks, but no Gemini key was supplied and no live Gemini turn was run. OpenAI's prior 429 and the remaining P2-07 and editorial/export gates remain open. P2 tasks and `P2.json` stay incomplete. This slice changes the native test harness and its testing guidance, current screen/Codex/phase/traceability/research records and the native-testing skill; product code, IPC/persistence schemas and examples, model adapters, media/export contracts, prompts, subagent routing and screenshot references are unchanged.

## Authenticated provider edit to manual Undo (partial)

A keyless continuation in packaged isolated Electron reopened the same persisted project after the live DeepSeek trim. The first private attempt failed because Undo is visible only on Edit, while the project reopened on Record or Import; it made no mutation. The corrected run selected Edit, clicked Undo, found a second journal record with `manual` origin targeting the earlier `api_provider` transaction, restored the 1.5-second baseline duration and reopened the project. A first screenshot caught the preview while it was still decoding; a separate read-only native wait passed exact original first-frame pixels and guest-only visual inspection. Source and managed-source hashes and baseline bytes stayed unchanged. No provider key was restored and no additional paid call was made. The reusable authenticated provider harness now requires this Undo, exact restored preview and second reopen after a future live turn; this new harness path typechecks but has not itself been rerun with a paid key. P2-10 remains partial, and P2-07, Gemini live access, semantic A/V review and export remain open. The test harness, native-testing guidance, Codex spec, phase/progress and traceability are affected; schemas, examples, product code, prompts, routing, media/export contracts and reference images are unchanged.

## Complete live DeepSeek edit and Undo fixture (partial)

The strengthened authenticated DeepSeek native fixture later passed a full single-run path in packaged isolated Electron: live reviewed model discovery, explicit paid Send, one guarded `api_provider` trim, exact committed preview, reopen, manual shared Undo, exact restored preview and a second reopen. It verified immutable source/managed-source and baseline bytes and that the session-only key was absent after restart. The harness consumed and deleted a mode-600 guest key file before Electron launch; three completion responses returned HTTP 200, and private screenshots, account state and media remain ignored. The first attempt with this strengthened harness failed at the final reopen because `getImageData` was called while the canvas was still zero-sized; it had already committed and undone correctly but is not counted as a full pass. The corrected test treats zero dimensions as an assertion for its bounded decode wait and passed on a separate run. This is narrow P2-10 evidence, not meaning-preserving speech editing, Gemini live signature replay, complete tool confinement, full A/V review, export or P2 acceptance. Spec sync covers the native harness, guest guidance, Codex integration, active phase, progress, traceability and native-testing skill. Provider adapters, typed IPC/persisted contracts, prompts, subagent routing, references, media/export policies and P0/P1 results are unchanged. No P2 result or task checkbox is written.

## Host-defined App Server tool feasibility (partial)

The pinned official 0.155.1 App Server accepted one experimental namespaced dynamic tool on `thread/start` without an MCP server. The isolated guest used an app-owned private signed-in profile, verified Luna/high in the live model catalog, and completed one turn. Its code-mode cell computed `ALL_TOOLS.map(item => item.name)` and called the single host-defined tool. The callback saw one correlated `item/tool/call`, no foreign requests and no unowned inventory entry; a private rollout audit verified the code cell and no other raw custom-tool name. The first plain-function attempt completed with no call; the first namespaced attempt reached the host but failed because the callback expected a qualified name while App Server sent the bare name. The corrected and stricter runs passed. This is direct App Server protocol evidence, not a packaged Electron project edit or general effective-tool allowlist proof. The checked-in isolated native probe and `docs/research/P2_DYNAMIC_TOOLS.md` record the repeatable path without any credential, rollout or screenshot in Git. P2-07 and P2 remain open. Production code, persisted schemas, user flows, Codex prompts, subagent routing, media contracts and references are unchanged; task/phase/progress/traceability, research, the native test guide and Codex/native-testing/security skills are synchronized.

## Host-defined tool restart and resume (partial)

A second ignored isolated profile ran the official packaged 0.155.1 App Server directly. One Luna/high turn called the namespaced read-only host tool. After that process closed, a fresh App Server resumed the exact thread ID using `buildThreadResumeRequest`, whose generated schema does not take `dynamicTools`, and a second turn called the same tool. The checked-in `codex-dynamic-tools-resume.test.ts` rerun passed with one host call in each process, two completed turns and zero foreign host requests. This establishes a narrow persistence path for a newly created dynamic-tool thread. It does not test the product's existing MCP-backed threads, complete history, guarded draft transactions, Electron UI, effective upstream tool confinement or native children. No product, schema, flow, prompt, routing, media or reference change occurred; task/phase/progress/traceability, research, native test guide and relevant skills are synchronized. P2-07 and P2 remain open.

## Optional host-defined tool boundary (partial)

The pinned official 0.155.1 generated closure now includes `DynamicToolCallParams` and `DynamicToolCallResponse` with reviewed source hashes. The optional internal adapter maps the existing six MCP tool descriptions/schemas to stable names under `codex_video_edit`, decodes only bounded exact host-call payloads, and returns bounded success or fixed safe error text. A project-thread client with an internal invoker requires a known matching thread/turn ID before dispatch and keeps the connection open after an owned result; an early host call during turn-start fails closed rather than committing under an unknown turn. Its stream/history projector allows only the configured owned dynamic items as generic Reading/Applying activity, without arguments or results. Focused tests cover schema parity, foreign/stale/early quarantine, output redaction/bounds and history projection; the generated-type check, typecheck, lint, formatting, 42 foundation tests and 283 media tests passed with two media skips. The first full package validation failed because the progress note in `TASKS.md` repeated its task ID; the corrected note passed the contract and delivery validators. The packaged product still supplies MCP only and never registers these host tools. Existing MCP-thread compatibility, turn-start timing in a live run, shared transaction dispatch in packaged Electron, complete effective-tool confinement and current native-child policy remain unverified. User-facing flow, prompts, persisted project schema, media contracts, reference images and subagent routing did not change; Codex/security/tool specs, protocol contract, relevant skills, tests, task and traceability did. P2-07/P2 stay open.

## Live host-tool turn timing (partial)

The direct official 0.155.1 App Server timing probe registered the adapter's six reviewed host tools without MCP and requested a read-only project summary in Luna/high code mode. On the passing isolated run, `turn/start` returned a validated turn ID before the single owned `item/tool/call`; the turn completed with zero foreign, early or mismatched calls. Two earlier guest runs completed without a host request because the chosen older package lacked its matching code-mode host. Their failures remain private and separate. The passing package contained both matching binaries. This is one direct App Server scheduling observation, not a permanent race guarantee or a packaged native-window test. No product flow, schema, prompt, provider, UI, media, reference or subagent route changed. The native test guide, research, relevant skill, task/phase notes and traceability were synchronized. Legacy MCP-thread compatibility, guarded native draft dispatch, complete effective confinement and P2 acceptance remain open.

## Persisted thread tool route (partial)

The main-owned project-thread registry writes version 2 bindings with an explicit MCP or dynamic route. Version 1 records decode strictly as MCP and are not rewritten on read; a later binding write migrates them atomically. A new internal dynamic thread includes the reviewed six-tool definition at start. Resume follows its stored route, and an incompatible client fails before RPC; live activity and host requests are accepted only for that route. The product still constructs an MCP-only client and no packaged project is switched. Focused migration, corruption, dynamic resume and cross-route tests passed; the full local check passed 42 foundation and 288 media tests with two skips. A synthetic schema/example documents the persisted version 2 shape. In the isolated packaged Electron guest, one Luna/high MCP fixture passed a real trim during the turn, same-thread reopen, shared Undo, interruption and guest-only native-window inspection. A follow-up synthetic v1 registry fixture reopened that conversation, completed an owned read-only turn and left the old file byte-identical. A second packaged project then caused the app to write v2 with two MCP bindings; a private hash comparison verified the old thread ID survived. That second broader edit harness failed after a correct committed trim at its post-turn assertion, so its full edit path is not counted as a pass. The private failure is retained. Feature/security/tool specs, phase prompt, task/phase/progress/traceability, relevant skills and tests changed. Product UI, media behavior, provider prompts, references and subagent routing did not. Effective host-tool confinement and guarded dynamic draft commit/Undo/reopen remain open; P2-07/P2 are not accepted.

## Buffered host-call turn correlation (partial)

The optional dynamic project client now validates an owned `turn/started` notification after submitting `turn/start`, provisionally correlating a host request before the RPC response only to that thread and turn. It buffers user-facing activity until the response confirms the same ID. A call before RPC submission or any notification, after buffered completion, from a child or previously seen turn, or following a conflicting response or remote rejection cannot dispatch; a conflict quarantines the client. Seven new race tests and the existing unknown-turn test passed, followed by all thread-client tests. The first full thread-client run caught a local active-resume regression caused by quarantining a routine overlapping-turn configuration error; the catch was narrowed and the full suite passed. A direct isolated official 0.155.1 `CodexClient` with the six-tool dynamic route and no MCP completed one Luna/high owned read. The first probe printed pass but its test-only timeout held Node open; clearing the timer produced a separate clean-exit pass. Neither live run established early request timing, a native Electron window, guarded draft mutation, or effective complete tool confinement. Product main remains MCP-only. Spec sync covers Codex/security specs, P2 task/phase/progress/traceability/research, the bridge, focused and direct native tests, guest guidance and relevant Codex/security/native-testing skills. Persisted schema, typed IPC, provider flows, UI, prompts, subagent routing, references and media/export contracts are unchanged. P2 stays incomplete; no result or task checkbox is written.

## Packaged mixed Codex tool routes (partial)

New project conversations now select the reviewed six-tool host-defined route; existing registry-bound MCP conversations retain their route. Main supplies both through the same active-project tool service and committed-draft refresh. Route changes close the old App Server client and reconnect before opening the new conversation; route-specific developer instructions use actual model-visible tool names. A focused controller test covers dynamic-to-MCP switching, separate process options and instruction names. The full isolated packaged Linux Electron synthetic Luna/high fixture then passed real Codex trim during the turn, authoritative preview, reopen on the same dynamic thread with matching history, real Codex Undo, exact restored preview, Stop, second reopen and source/baseline integrity. A guest-only X11 screenshot of the actual native window was visually inspected. A separate native v1 MCP fixture on that same package passed an owned read and preserved the old registry bytes. Its first copied-profile attempt failed because project baselines embed absolute roots; the valid run used the original guest profile and restored its registry backup. A bounded dynamic-route forbidden-command run completed with no forbidden raw invocation in that exact turn. These tests do not prove full effective upstream-tool unavailability, native-child inheritance, meaning-preserving editing, supplied-media final review or lossless master export. P2-07 and P2 remain open. Spec sync changes the route prompt, Codex/security/tool specs, phase and task notes, traceability, research, relevant skills and native guide; project and draft schemas, typed IPC, API-provider flows, media contracts, UI references and subagent routing remain unchanged. Private credentials, IDs, screenshots, project media and results remain ignored in the isolated guest.

## Live host-route stale rejection (partial)

The route-aware native stale continuation now accepts a dynamic fixture with one real Codex trim followed by one real Codex Undo, while retaining the earlier one-edit MCP case. It reads the obsolete sequence/hash from the original committed edit and first proves direct guarded-service rejection. It then opens the packaged Electron project and asks the same real Luna/high thread for exactly one host-defined `cut_trim_edge` call with that stale head. The authoritative completed turn contains the correlated host call, unsuccessful bounded result and stale-draft error. The current draft, both journal files, original, managed source and baseline stayed unchanged. A separate rerun of the adapted harness against a one-edit MCP fixture also passed with one failed MCP stale call and unchanged draft, journal and immutable inputs. Both private isolated route runs passed; no new product behavior or UI changed. This verifies live stale-error relay on both routes and leaves complete effective-tool confinement, native-child inheritance and P2 acceptance open. The native test/guide, Codex/security/tool evidence, task/phase/progress/traceability, research and relevant skills are updated; prompts, routing, schemas, IPC, provider flow, media contracts, UI references and subagent routing are unchanged. No phase result or task checkbox is written.

## Dynamic split and owned inventory (partial)

The earlier route-aware packaged native split fixture read the main-owned binding and checked six owned nested tools, zero unowned entries, and absence of selected app, goal, plan, input, skill, spawn, image, web and shell handles in the tested turn. Those counts describe a previous route revision; they are not evidence for the current dynamic child surface. Its isolated Luna/high synthetic and private two-source runs passed a committed split during the turn, exact split and source-join frames, manual shared Undo, reopen and unchanged source/baseline. Earlier runs of that package and fixtures passed with `--inspect`, and their actual guest native windows were visually inspected; those runs remain separate. The screenshot, project, media, credential and raw tool response stay outside Git. The current rollout-correlated test and its route-specific counts are recorded below. This does not prove complete upstream effective-tool confinement, semantic first-cut quality, audio listening, Windows capture, final review or lossless master export. P2-05/P2-07 and the phase remain open. The native test/guide, Codex/security/tool evidence, task/phase/progress/traceability/research and relevant skills are synchronized; product prompts, persisted schemas, IPC, provider flow, media contracts, UI references and subagent routing are unchanged.

The next P2-05 slice adds a seventh reviewed guarded tool, `cut.delete_ranges`, for 2–16 confirmed disjoint half-open output ranges in descending start-time order. The reducer applies them against one exact head as one atomic durable transaction with one newest Undo, preserving original and managed source bytes. Focused reducer, service, MCP, dynamic schema and API-provider adapter tests passed replay, malformed/overlap/ascending rejection, stale head, Undo and no partial commit. An isolated packaged Luna/high dynamic turn passed on a synthetic two-source project and, after that fixture, on the two privately staged supplied recordings in the requested order. Both runs applied a two-range transaction during the turn, checked the edited join frame from the committed native preview, manually undid both cuts, reopened and verified source and baseline integrity. The supplied guest Electron window was visually inspected. A separate fresh synthetic split turn on the seven-tool catalog reported seven owned and zero unowned nested names before rollout correlation was added. These are direct requested time cuts, not automatic speech cleanup or verified audible joins. The earlier six-tool inventory is historical; current dynamic-route tool counts and hostile app-override evidence are recorded below. Complete tool confinement, semantic editing, A/V review and master export remain open. No P2 task is checked and no phase result is written. Affected domain/tool contracts, prompts, tool/example manifest, transaction schema description, tests, phase/task/progress/traceability and relevant skills are synchronized; no screen layout, IPC, project storage version, reference image or subagent routing changed.

After formatting, a final packaged rebuild initially failed its supplied-footage native harness three times before the edit: the test did not switch from the first imported project to the combined project, then attempted a hidden Home list row, then used a five-second Edit-stage wait while the large source was still loading. A guest-only diagnostic copy identified those exact assertions; no product mutation occurred in those failed attempts. The reviewed test now navigates Home, opens the combined project and waits for its committed duration and Edit controls. The final packaged build passed the real Luna/high two-range turn on the two supplied recordings, exact edited-join frame, manual Undo, reopen, original/managed source and baseline checks and guest X11 visual inspection. The private diagnostic file, media, account, screenshots and evidence stay ignored. This resolves the harness race for this slice, not the remaining P2 semantic, A/V, tool-policy or export gates.

## Native manual Redo and reopened stack (partial)

The Edit stage now exposes a committed-head-bound Redo control beside Undo. Main supplies the manual origin and new journal ID; Redo appends a hash-chained transaction for the newest undo in LIFO order, restores the original applied timeline, remains available after reopen, and is cleared by new work. The journal suite passed stacked undo/redo, reopen, exact sequence/hash advancement, stale Redo rejection and redo-stack clearing. The project DTO, typed preload/main IPC, renderer disabled state and recovery error, schema and example now include the committed Redo target.

`npm run check` passed typecheck, lint, Prettier, 42 foundation tests, 301 media tests (two documented skips), and package/delivery validation. The packaged Linux Electron two-source fixture passed manual trim, shared Undo, Redo, exact decoded preview, project reopen with Redo enabled, a repeated Redo and follow-up Undo, and immutable source/baseline checks. The actual guest Edit window was inspected at 200% using guest-only capture. This pass used synthetic fixtures; earlier supplied-footage tests cover trim/Undo only. The guest build, screenshots and synthetic media remain private and ignored. P2-05/P6-01 and the overall P2 phase remain incomplete; no task checkbox or phase result is claimed.

## Dynamic-route native child read boundary (partial)

The pinned packaged Codex 0.155.1 child fixture now passes on a fresh synthetic project with the live Luna/high subscription route. The parent performs one server-owned `spawn_agent` call and wait; `thread/read` links its returned receiver to the parent/source, and the child completes both `project.get_summary` and `timeline.get_summary`. The saved child turn retains the exact inherited read-only/no-network sandbox, empty environment and never-approval policy. The original source, managed source, baseline and journal remain unchanged.

The ordering is significant for authorization. On the tested runtime, the parent `collabAgentToolCall` can start with no receiver ID and expose receiver IDs only when the item completes; the child turn may start before that completed parent item arrives. Main therefore keeps a bounded provisional child-turn candidate that cannot authorize a call on its own, then correlates it only when a server-owned parent spawn item identifies the same child under the active parent turn. In this test the ID arrived on the completed item. `thread/started` alone grants no lineage, only the two read summaries are permitted, and ordinary child turn/item/message notifications never enter or poison the parent projection. Unit tests cover this ordering, read-only dispatch, terminal/conflicting candidates and filtering; the current focused route/protocol/MCP run passed 42 tests and `npm run typecheck` passed.

`--inspect` captured the actual Electron display inside the guest and the packaged test completed. The screenshot was then visually reviewed: it shows the native Electron window with one Codex drawer, a synthetic preview, and no browser chrome or persistent Ready badge. Keep the image private with the guest project and raw App Server evidence. This pass does not establish a complete upstream effective-tool allowlist, native behavior on Windows, user-footage A/V review, or full P2 acceptance. No task checkbox or `P2.json` result is written.
## Per-thread Apps and configured MCP isolation (partial)

App Server startup now discovers configured MCP names with the pinned Codex CLI's `mcp list --json`, validates the bounded list, applies an explicit `mcp_servers.<name>.enabled=false` override for each entry, then verifies that every discovered entry remains present and disabled. The route validates the exact app-server MCP status before opening the thread and again for its active thread. External tools, resources, templates, changed inventory, malformed names, or a collision with the owned `codex-video-edit` MCP name fail closed. The empty `mcp_servers={}` table is retained only as defense in depth because upstream merges an empty table instead of clearing existing configuration ([Codex issue #16045](https://github.com/openai/codex/issues/16045)).

Both `thread/start` and `thread/resume` now explicitly disable `features.apps`, `features.plugins`, and `features.remote_plugin`. The pinned 0.155.1 runtime registers the host-owned `codex_apps` MCP service only when Apps is enabled and ChatGPT auth is active ([tagged runtime source](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/codex-mcp/src/mcp/mod.rs)); the thread-level switches removed it in the direct isolated probe. The packaged native authenticated child test passed with a synthetic hostile `untrusted_test` config and verified the external entry stayed disabled on the active thread, then completed the correlated read-only child turn. The app was built and launched as packaged Electron in Docker; the synthetic project, source, baseline and journal were unchanged. Its reviewed guest-only screenshot showed the actual native window with the synthetic preview and one Codex drawer.

Spec sync for this boundary updates docs 08 and 21, P2-07 task/active-phase/progress evidence, traceability, P2 runtime research, and the Codex/security/native-testing skills and desktop test guide. Project and draft schemas, IPC contracts, prompt behavior, screen specs, reference manifest, provider routes, media contracts and subagent routing are unchanged. Unit/native test sources cover the protocol flags, per-server arguments, inventory validation, hostile synthetic config and active-thread boundary. Full upstream tool confinement and P2 acceptance remain open; no phase result or task checkbox is written. Private credentials, project data, paths, screenshots and raw protocol evidence remain outside Git.

## Final per-thread isolation verification

After the boundary implementation and spec sync, isolated Docker `npm run check` passed typecheck, lint, formatting, 42 foundation tests, 307 media tests, package/schema validation (42 schema/example pairs), and delivery/reference validation (20 full-page reference images and 10 invalid-quality rejection checks). The pinned protocol check passed against Codex 0.155.1 with 437 schemas and the consumed initialize/account/model/skills/thread/turn method set; its report correctly records `runtime_smoke_test: false`. The public staged-index audit passed across 700 indexed files. The earlier packaged authenticated native hostile-config run had already passed with a real correlated child summary read and unchanged project/source/baseline/journal, plus guest-only visual review. These results close this slice's checks only. P2-07, complete effective-tool confinement, all remaining P2 work, and the P2 phase result remain open.
## Deterministic configured-MCP discovery verification (partial)

The external-MCP boundary now has direct deterministic coverage for its discovery/disable/verify sequence. Tests require the exact `mcp list --json` call followed by one `mcp_servers.<name>.enabled=false` override for every configured server, including an entry that was already disabled. Verification fails closed if any server stays enabled or if the list gains or loses an entry. The App Server argument builder separately rejects malformed, duplicate or over-limit names. Focused MCP tests pass 11 cases; the full isolated Docker `npm run check` passes typecheck, lint, formatting, 42 foundation and 310 media tests, plus package/schema and reference/delivery checks. This hardening keeps the configured-MCP policy documented in docs 08/21 and the relevant skills; no persisted schema, IPC, user-facing flow, prompt, screen, media behavior, routing or reference image changed. The earlier authenticated packaged Electron hostile-config/child run proves the valid path, while these deterministic tests exercise its failure cases. Complete upstream tool confinement and P2 acceptance remain open.

## Current Gemini model filter check (partial)

The 2026-09-25 review of Google's official function-calling and OpenAI-compatibility documentation confirmed that the editor's exact-ID allowlist still matches its intended stable text/function model families. Added negative catalog cases for the current Gemini 3.8 Live, Gemini 3.8 TTS and Gemini 3.1 Pro Preview IDs; `node --test tests/media/desktop-api-providers.test.ts` passed all 10 cases. Research notes and traceability now link the official model support and September release changes. This is a model-filter regression check only: it does not prove an authenticated Gemini catalog or generation request, and P2 remains open. No product behavior, persisted schema, IPC, provider route, prompt, screen, task status, skill routing, or reference image changed.

## Official Codex 0.156.0 boundary review (partial)

On 2026-09-25, reviewed the current official 0.156.0 release, its `thread/start` protocol type, and its configuration types after the full effective-tool gate remained open on the packaged 0.155.1 runtime. The reviewed public surfaces still expose dynamic host tools, but no complete model-visible tool catalog or allowlist; release notes do not announce one. This is source review only: no runtime was installed, no app was launched, and no registry behavior is claimed. The product stays on the already verified 0.155.1 pin pending generated-protocol and packaged native regression evidence. P2-07 remains open.

The pinned 0.155.1 config source says `apps._default.enabled=false` disables apps unless overridden per app. The updated packaged `codex-split.test.ts` seeds a fresh app-owned Codex home with `[apps._default].enabled = true` and `[apps.adobe].enabled = true`, then probes the real dynamic editor thread. A successful baseline and a successful hostile-config run each completed an actual `exec` code-mode `custom_tool_call` and matching output, verified from the exact turn's account-confined rollout. Each reported seven guarded editor functions, five supported v1 collaboration functions, zero connected-app functions, and zero other nested functions; both runs committed the real guarded split, shared Undo, reopen and immutable-source/baseline checks. The dynamic native tools were previously misclassified by a stale assertion that expected spawn to be unavailable; that test failed and remains a failure. One later hostile retry disconnected before the diagnostic completed and also remains a failure. The corrected hostile run passed. This closes the named-app-override check only for this pinned nested surface and tested thread. It does not establish a complete upstream effective tool catalog. The real Codex raw rollout and all screenshot/media/account evidence remain private in the isolated guest.

The native test no longer stores the assistant's raw inventory answer; it records bounded counts only after raw call/output correlation. Spec sync updates the P2 task/prompt, Codex integration guidance, native test guide, traceability, research, desktop runbook, and relevant skills. No product behavior, IPC, persisted schema, API-provider route, UI, media contract, reference image, or task checkbox changed. In the isolated container, typecheck, lint, formatting, 42 foundation tests, and all 310 media tests passed. `npm run check` could not complete its Python package-contract stage there because the desktop image has neither `pip` nor `jsonschema`; the read-only package and delivery validators both passed separately with the installed host Python. The native app itself was built and launched only in Docker. Account files, media, and raw rollout data remain in the guest. Two synthetic guest screenshots were briefly copied to Windows Temp for visual review, outside the guest-only screenshot policy; no screenshot was staged or published, and this slice does not claim guest-only screenshot acceptance. P2-07 remains partial; no P2 result is written.
