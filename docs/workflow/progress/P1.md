# P1 implementation progress

Status: partial. P1-01 through P1-06 remain the active phase scope. This is a progress record, not an accepted phase result.

## Settings and focus slice

The current work targets P1-03, P1-05 and P1-06. Main-owned preferences store persistent interface scale at 100%, 125%, or 150% through strict typed get/set IPC. Settings opens from its control or Ctrl+,, supports Escape/Cancel without applying unsaved choices, and emits a save toast only after persistence succeeds. Load/save errors preserve the prior saved value and provide actionable feedback.

Source information presents actual imported metadata in one inspector. Settings hides the inspector while modal and restores it afterward when applicable. Keyboard focus returns to the invoking control, the selected source card on Back, or Source information when its inspector closes. This extends the working media library; it does not create projects, drafts, five-stage project navigation, or editing.

Local `npm run check` passed with 29 Python tests, 21 TypeScript tests and 27 schema/example pairs. The isolated packaged native test passed keyboard/modal focus, scale persistence through restart, exact decoded pixels, and viewport/inspector checks at 1366×768 and 1920×1080. Computer use inspected the actual native window, applied 150% sizing, and verified the rebuilt inspector stays beside the preview with its own scroll area. An initial visual failure placed the inspector below the preview and scrolled navigation away; the layout was fixed and retested. Full P1 acceptance is still incomplete. The current Home reference was inspected, but no reference image or manifest changed. Source preview retains its bounded P0 path and remains independent of master quality.

## 200% accessibility extension

The reviewed settings/focus slice merged through [PR #5](https://github.com/klimPaskov/codex-video-edit/pull/5) at `2d33af76181028762c36ab2fa20ddc23df092e40`. Its 150% native observations above remain the evidence for that slice.

Current P1-05 work extends the persisted choices to 200%, alongside 100%, 125%, and 150%, to meet the specified scaling range. Five storage and seven IPC tests passed. Isolated packaged keyboard and sizing tests passed. Current checks passed with 29 Python tests, 22 TypeScript tests and 27 schema/example pairs. The earlier host-viewer 200% attempt was stopped by the user and is not a pass. This change preserves source/frame samples and master independence. Full five-stage project navigation and P1 acceptance remain incomplete.

The extension synchronizes UI/screen/accessibility specs, scale schema/IPC traceability, desktop usage notes, active phase and this progress record. It does not change reference images, media quality, project/draft behavior, or P2-P10 requirements.

## Spec sync

Changed: interface settings/focus requirements in docs/04 and docs/05; preferences and IPC schema/example traceability in docs/27; active phase and this progress record; desktop usage notes and native testing guidance for scaled inspector bounds and focus-induced scrolling. The new preferences contract and strict get/set IPC are covered by implementation tests; this bounded native path passed, while the full phase remains incomplete.

Intentionally unchanged: five-stage project navigation requirements, P2 and later feature acceptance, source immutability, bounded preview fidelity, independent lossless master policy, AI provider/runtime and transaction routing, final export/cleanup controls, and reference images/provenance. No `docs/workflow/results/P1.json` is asserted.

Native screenshots and source settings remain private. Preference writes serialize within the single application process; tests do not establish power-loss durability or concurrent external-writer safety. Interface sizing changes presentation only and never changes master data.

## Guest-only testing boundary and recovery

The latest user instruction prohibits control of the host PC. All current native input and screenshots remain inside Docker through `tests/desktop/guest-input.py`, using guest X11/XTEST via ctypes and FFmpeg x11grab. Linux, `/.dockerenv`, UID 1000 and display `:99` guards prevent host execution. Earlier viewer evidence remains historical; future host-viewer use requires a new explicit request.

The guest-only path captured the private desktop, closed its probe, opened the actual product source, opened Settings with Ctrl+,, selected and saved 200%, opened Source details, and advanced the actual frame to 0:00.500. No host input was used. Empty-guest abrupt kill/start recovery passed while retaining the previous container; source evidence and screenshots remain private. These observations do not prove audio listening, Windows devices, full P1, or user-video acceptance.

ADR 0013 identifies the next real project prerequisite for P1-03: actual project identity, immutable source references, source-matched canonical timeline, a resolving baseline revision, reopen and persisted five-stage navigation. Navigation must not pretend to run later features. The shared draft transaction prerequisite belongs to P2; neither foundation is claimed implemented by this scaling slice.

This sync updates the current testing boundary in ADR 0006, native-testing documentation/skill, guest instructions, accessibility evidence and active phase. The real project dependency correction is tracked in ADR 0013. No phase result, image-reference change, or new completed phase is asserted.

## Actual project shell implementation

The earlier guest-only/scaling work was published through PR #6 at verified merge `8cbaf6fcc3666405f8d45b6d560c14dbf7de0503` with both CI platforms passing. The current P1-03/P1-05/P1-06 extension now implements the ADR 0013 real project prerequisite. Headless tests cover actual project creation/reopen, rational source-matched timeline and resolving immutable baseline, complete reprobe comparison, integrity and failed-save preservation. Main exposes strict list/create/open/navigate IPC and maps path-free DTOs purely from validated committed state after persistence.

Home separates Projects from the retained Source library. Import creates an actual project after ingestion; legacy sources retain an explicit Create project action. Five-stage controls persist before reflecting the selected stage, preserve source/frame position, and ignore stale replies after Home or later selection. A failed save retains the prior committed stage. Visiting Auto Edit, Edit, Review or Export does not execute unimplemented features, create edits, certify review or export media. Settings, one source inspector, keyboard focus and 100–200% sizing remain supported requirements.

The current packaged native suite passed all five stages with baseline/source unchanged, a chmod-induced save failure preserving the committed stage, reopen at Review, and 100/125/150/200% scales/security/focus. Headless checks passed with 34 Python tests, 37 TypeScript tests and 27 schema/example pairs. Guest-only X11 visual inspection separately confirmed actual Projects versus Source library, reopen at Review, Next frame at 0:00.500, Auto Edit navigation retaining that frame, Settings from 125% to 200%, all five compact-selector choices, Export selection without an export claim, and one visible scrollable source inspector. No host input was used.

The first native launch exceeded its 30-second timeout; an unchanged retry passed. The cause is unknown, and private evidence retains that limitation. Enlarged-scale Playwright PNGs were cropped, so complete guest-display screenshots were used for whole-window visual review. Current slice publication/review and the P1 result remain pending. Earlier settings/scaling evidence is not substituted for these new project-shell observations. P1 remains incomplete; no result, task checkbox or complete-project/editor claim is added. P2 real Codex/shared transactions and P3-P10 full acceptance remain required.

Spec sync: updated architecture, UI/screen, import/projects, data/revisions, traceability, native-testing skill, routing and current phase/progress. Existing project/source/timeline/revision contract shapes and reference images remain unchanged; desktop IPC is extended for the new DTO and project operations. Immutable media and baseline remain separate from future draft transactions. No private evidence is published.

## Recovery and accessibility continuation

The project-shell slice above merged through PR #7 at verified remote `94edf1d0a9e3f61904e48bfb262c86d49e38cb04`. The editorial reference adaptation and suggestion validator merged through PR #8 at `e335163284f22a3e9d58da26058f243c669f1570`, with Windows/Linux CI passing. The latter adds requirements and pure validation, not automatic editing. Its workspace checks passed with 34 Python tests, 44 TypeScript tests and 28 schema/example pairs.

A packaged lifecycle test exposed Chromium retaining 150% origin zoom despite a corrupted-settings 100% fallback. The repair passed the earlier lifecycle/full-native suites and guest visual inspection with an actual Settings read error at 100%. The subsequent candidate adds required-asset preflight and bounded explicit renderer recovery, plus contrast/non-color selection improvements and keyboard/media-preference tests. Extended acceptance is still running. The initial extended lifecycle attempt lacked its guest input helper; this test setup failure is retained and is not a product pass.

A separate test-image layer adds Orca and AT-SPI dependencies for actual screen-reader output verification inside Docker. The harness distinguishes natural activation from a separately forced run and preserves private braille/debug evidence. Dependency installation and source review alone do not establish a screen-reader pass. Missing hardware, Windows integration and full phase acceptance remain unclaimed.

This continuation synchronizes native architecture/failure screens, lifecycle and accessibility requirements, native-electron guidance, P1 prompt, traceability and test-environment documentation. Existing IPC/domain/media schemas and reference images remain unchanged because no edit operation, source transformation or new renderer endpoint is introduced. No P1 result or task completion is asserted yet.


## 2026-09-08 resumed evidence and source layout

The retained Docker guests restarted successfully after the engine was unavailable; isolation and the virtual display were verified again. Product and native input stayed inside the guests. Recovered completed extended lifecycle evidence covers normal shutdown, second instance, main-process kill/reopen, immutable project/source preservation, corrupt preferences, four missing packaged assets and the one-reopen renderer recovery limit. The complete native product suite passed again against the current packaged source. Keyboard accessibility checks passed normal/hover contrast, forced colors, reduced motion and exact fixture preview samples.

Parent guest-only visual inspection confirmed the real renderer crash dialog, explicit Reopen action, restored Home, and a working Settings shortcut with visible focused 100% selector. Playwright retains a crashed Page flag after the same WebContents successfully reloads; the lifecycle harness observes recovery through the main connection without replacing the product window. Whole-window guest inspection independently confirms visible recovery.

Real Orca acceptance remains incomplete: natural activation announced initial controls but missed Settings; a separate forced run missed Home. These are retained failures, not screen-reader passes, and the cause is still being diagnosed. No P1 result is written.

The existing workspace reorganization is preserved: documentation resources now live under docs/, reusable skills under .agents/skills/, and agent guidance under .codex/agents/. Live references, validators and catalogs follow those paths. Historical accepted result records remain unchanged. Publication/scanning permits only direct Markdown guidance and routing.json in the agent directory, excluding runtime/auth configuration. The updated foundation suite passes 37 tests; contracts validate 28 example pairs and all 20 original reference images. Reference image bytes, media samples and domain schema shapes are unchanged by this layout repair.


## Current visual and provenance audit

PR #9 merged at verified `502fda5d925e4905ab2c43966be13369e7d1e762` after both Windows/Linux push and PR CI passed. A fresh synthetic project was inspected in the actual packaged guest at 100% and 200%, with explicitly emulated forced colors, higher contrast and reduced motion. Guest-only input selected and saved 200%, opened the single source inspector, advanced the frame to 0:00.500 and reopened Settings with a clear focused selector. The inspector is vertically scrollable at 200%; this inspection covers its top section and control access, not every metadata row. The preview remained visible and controls stayed within the whole window.

Private provenance now maps retained lifecycle, keyboard/accessibility and full-native artifacts to both original guest packages and their executable/ASAR hashes. All five packaged application entries match across the two builds, and current application source matches after LF normalization. Retained lifecycle/accessibility test formatting differs but normalizes identically with Prettier. Historical test hashes were collected retrospectively and are not misrepresented as snapshots taken during those runs. Copied result/dialog records match original guest bytes.

The remaining known P1 gate is actual screen-reader output. Orca 43.1 natural, forced and native-XTEST tests retain missed-announcement failures. The inspected upstream reader focus path is a diagnostic lead, not a product exemption. A newer official Orca/AT-SPI comparison is being provisioned separately with verified source hashes; its build or installation is not a passing test. No phase result is written.


## Verified newer screen-reader runtime

A fresh guest-local build from hash-verified upstream Orca 50.2 and AT-SPI 2.56.8 sources passed the natural-activation smoke test. All seven controls produced genuine reader braille output paired with focus events from the packaged application process. The test also reads back disabled speech/physical braille and enabled monitor settings, checks immutable fixture bytes, and waits for the final Edit stage to commit in both UI and project storage. Executable, ASAR, reader entry, runtime libraries, observer and exact test hashes are retained privately. The reproducible recipe creates a fresh extraction/build/prefix on every invocation and preserves previous attempts. These dependencies are test-only and are not bundled with the app.

Parent whole-guest screenshots show clear focus and matching monitor text for Import, Interface size, Cancel and Source details. The Edit-focus screenshot displays Orca's subsequent sticky-focus-mode announcement; its actual Edit-button output is independently recorded by the test. It is not a screenshot of the later committed Edit state. Earlier Orca 43.1 failures, buffered-debug readiness setup failures and a five-second pending-save timeout remain retained. The final test uses a bounded 30-second commit wait and verifies disk state; no cause is asserted for the earlier timeout.

The full headless check passed 37 Python and 44 TypeScript tests, 28 schema/example pairs, 20 reference images and 10 invalid quality-contract rejections. Final reader-harness typecheck, lint and formatting passed after its last changes. Spec sync updates native testing, accessibility, traceability, test instructions, dependency notices and the native-testing skill. Product behavior, domain/IPC/media schemas, AI prompts/routing and reference images are intentionally unchanged by this testing slice. Public source review/publication and the final P1 audit remain pending; no phase result is claimed here.


## P1 acceptance

P1-01 through P1-06 are accepted in `docs/workflow/results/P1.json`, written by the guarded result writer only after source/index cleanliness and verified remote main `33d3fdc1d87787322c658fad88a1c4fa9787f2d1`. PR #10 published the reviewed reader test/runtime slice; Windows/Linux push and PR checks passed. The first Linux PR attempt was cancelled after a long dependency-install step; retained logs subsequently showed slow downloads rather than a proven hang. The unchanged retry passed. Private evidence and the cancelled attempt remain retained.

The final acceptance audit found no remaining functional P1 gap within its stated synthetic Linux scope. Parent inspection notes and independent evidence review distinguish real reader output, visible monitor text, focus, and later committed stage state. The result records known timeout, reader-version, retrospective provenance, emulated preference, Windows/hardware and later-feature limitations. It does not claim the user example or any Codex/editor/capture/export workflow is complete. P2 is now the first incomplete phase. Earlier progress entries above remain historical records.
