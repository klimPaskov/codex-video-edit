# P2 Codex runtime policy research

Date: 2026-09-08. Scope: official Codex `0.142.3`, source tag `rust-v0.142.3`, and the protocol generated from the installed unmodified runtime. This began as a source review and proposed integration policy. Later updates distinguish signed-out validation, user-authorized private credential seeding, authenticated discovery, the passing synthetic native trim/undo flow, and managed device-code completion. Browser OAuth callback completion and effective denial of every unapproved native tool remain unverified.

## Authentication and provider

This section constrains only the Codex App Server account and thread path. ADR 0014 later adds separate OpenAI API and DeepSeek main-owned adapters; their keys must not be forwarded into App Server or interpreted as ChatGPT login. Their endpoint, storage and model contracts require their own review and native tests.

The pinned [configuration schema](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/config.schema.json) supports `forced_login_method = "chatgpt"` and `model_provider = "openai"`. Built-in provider IDs cannot be replaced through `model_providers`. The stable generated `ThreadStartParams` supports `modelProvider`, `sandbox`, `approvalPolicy`, `config`, `cwd`, and `ephemeral`.

The pinned [account processor](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/app-server/src/request_processors/account_processor.rs#L283) rejects API-key login when the forced method is ChatGPT. This restriction does **not** itself distinguish managed ChatGPT login from externally supplied ChatGPT tokens: the external-token path rejects forced API mode, not forced ChatGPT mode. The product must expose only the managed ChatGPT login variants and must never forward arbitrary login payloads or accept access tokens through its renderer IPC.

Use a product-owned Codex home and an explicit child environment, without inherited provider credentials, endpoint overrides, unrelated MCP servers, plugins, or host runtime configuration. Keep credential ownership with the official runtime. Treat account metadata as status only; production code must not read or copy the credential store. Pin the provider on thread creation/resume and validate returned state before allowing turns. A packaged authenticated synthetic turn has now used the guarded edit tools and persisted a reversible transaction; that result does not establish complete tool confinement.

ADR 0006 records the user's explicit one-session test setup authorization: only the official runtime credential file was copied directly into the private app-owned guest account with mode 600. No account-directory mount, configuration, history or secret output was involved; the host file was unchanged at transfer. This is not a production credential-import feature and does not relax renderer IPC or managed sign-in policy. A packaged probe now reports connected/signed_in and runtime model, skill and usage discovery. Account access is available for private synthetic-fixture testing, but this does not prove browser-login completion or any live turn/tool/edit gate. Preserve the earlier signed-out outcomes separately.

A later isolated guest image added Chromium and `xdg-utils`. A fresh signed-out packaged Electron test used its real main-owned opener, observed Chromium start with the validated OpenAI authorization route, and cancelled back to signed out. Guest-only screenshot inspection showed the loaded OpenAI login page with the native editor behind it. The test-owned browser/opener exited without a host browser or published port. This closes the browser-opener evidence gap, not managed browser-login completion: no user authentication callback or reconciled signed-in account was observed in that run.

The same pinned App Server accepted `account/login/start` with the official `chatgptDeviceCode` mode in a fresh private-account probe. The packaged app now exposes that mode through a narrow typed Settings action: the renderer receives only the bounded one-time code and fixed `https://auth.openai.com/codex/device` page for the active attempt; a separate main-owned action opens only that page. A fresh-account packaged cancellation run and guest-only window inspection passed. In a separate live attempt the user completed verification externally. The same native app received completion, reconciled to signed in and cleared the code; a packaged reopen recovered authoritative signed-in state, plan, live models and skills. The interactive cancellation assertion failed when this completion happened, and that failed run is retained separately from the passing cancel scenario. This establishes managed device-code completion for the tested runtime, not the browser OAuth callback flow, full provider provenance for other account setups, or complete P2 acceptance.

## Environment access and built-in tools

`features.shell_tool = false` is supported. The pinned [tool configuration](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/tools/src/tool_config.rs#L81) maps this flag to `ConfigShellToolType::Disabled`.

It is insufficient to infer that `features.apply_patch_freeform = false` removes file mutation tools. The current [tool planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs#L741) registers `apply_patch` when an environment exists and the selected model supports it. The same planner registers `view_image` whenever an environment exists. Read-only sandboxing alone is not a project-scoped read boundary.

The pinned experimental protocol provides a stronger mechanism:

- [`thread/start.environments`](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/app-server-protocol/src/protocol/v2/thread.rs#L109): an empty array disables environment access for turns without an override.
- [`turn/start.environments`](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/app-server-protocol/src/protocol/v2/turn.rs#L87): an empty array disables environment access and updates the thread's sticky selection.
- The [thread processor](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/app-server/src/request_processors/thread_processor.rs#L1115) supplies default environments only when the selection is omitted.
- The upstream [`environment_count_controls_environment_backed_tools` test](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan_tests.rs#L600) asserts that no-environment turns have neither visible nor registered `shell_command`, `exec_command`, `apply_patch`, or `view_image`, even with the shell feature enabled and a patch-capable model.

The stable schemas initially generated under private test evidence omit these experimental properties. The implementation regenerated TypeScript/JSON with `--experimental` from the same pinned binary, retained the complete output privately, and pins a reviewed 207-type dependency closure by original hashes, including the consumed `ThreadTurnsListParams` and `ThreadTurnsListResponse` shapes. Request objects compile against the generated inputs. Signed-out packaged startup accepted `initialize.capabilities.experimentalApi` and the owned MCP configuration; a later authenticated packaged run exercised thread start, resume, turn start, interruption and persisted-history projection. Experimental compatibility remains a versioned product dependency.

The implemented request policy is `modelProvider: "openai"`, request-side `sandbox: "read-only"`, `approvalPolicy: "never"`, `ephemeral: false`, and `environments: []`, with an app-owned working directory and fixed instruction. It sets `environments: []` again on every turn, including the first turn after resume. The renderer cannot supply overrides. The generated response sandbox is validated as `{ type: "readOnly", networkAccess: false }`. Read-only and never-approval remain defense in depth; never-approval is not a prohibition on all tools. Server requests for command execution, file change, legacy approvals, or MCP elicitation are declined or cancelled and quarantine the connection.

## Guarded MCP and native subagents

The pinned schema supports `mcp_servers.<name>.command`, `args`, `cwd`, `env`, `enabled`, `required`, `enabled_tools`, `disabled_tools`, `supports_parallel_tool_calls`, and startup/tool timeouts. Use only the product-owned server, an explicit tool allowlist, and no project-controlled command or environment values. Each allowed edit tool must authenticate its application session and validate project identity, draft sequence, operation schema, and authorization through the shared transaction engine. Do not expose final export, deletion, cleanup, publication, or spending as generically authorized tools.

The current six-tool server allowlist and empty resources/templates do not remove the runtime's built-in MCP resource helpers. In pinned `0.142.3`, [turn router construction](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/session/turn.rs) supplies `Some` MCP tools whenever an MCP server exists, including an empty or deferred direct-tool list. The [tool planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs) then registers `list_mcp_resources`, `list_mcp_resource_templates` and `read_mcp_resource` independently of environment selection. The pinned configuration schema provides no supported switch found in this review to disable these helpers while retaining the owned server; server `enabled_tools` filters server tools, not these built-ins. An actual authenticated attempt invoked resource listing after missing project context and was quarantined. The current policy continues quarantining unreviewed items; no permissive parser change or helper-absence claim follows from this finding. Correct project context is not a tool-confinement mechanism.

The [MCP tool planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs#L861) adds discovered MCP handlers separately from environment-backed built-ins. The packaged runtime starts the fixed-hash owned child and verifies its exact six-tool name/schema inventory with empty resources/templates through `mcpServerStatus/list`. Direct child/broker tests cover bounded authenticated forwarding into the main-owned transaction service. A later real model turn used those tools to commit a bounded trim and a second turn undid it. Negative authenticated tool-confinement checks remain outstanding.

`features.multi_agent` and `features.multi_agent_v2` exist in the pinned configuration schema. The app now explicitly requests the v1 fallback (`multi_agent = true`, `multi_agent_v2 = false`) in app-server and thread configuration. The [collaboration planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs#L768) exposes native collaboration tools independently of environment access. The pinned [session resolver](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/session/mod.rs) gives persisted/inherited selection and model metadata precedence over the feature fallback. The generated model catalog does not expose that metadata, so flags and discovery cannot establish the actual native-agent version. Do not fabricate a separate spawn API or infer availability for every model; require correlated live child evidence.

The pinned [model catalog](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/models-manager/models.json#L178) marks `gpt-5.5` as search-tool capable. When the provider also supports tool namespaces, the pinned [v1 tool planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs#L767) defers `spawn_agent` behind tool search. This is a conditional source finding, not proof of the effective tool surface in the authenticated session. A fresh packaged `gpt-5.5` `xhigh` turn explicitly asked to discover the native spawn tool through tool search before invoking it; the authoritative turn had no collaboration item or child thread. The `thread/turns/list` item union has no tool-search variant, so this history cannot establish whether a search call occurred. The model's decision and effective tool availability remain unresolved.

The pinned generated `ResponseItem` contains `tool_search_call` and `tool_search_output`, while its `v2/ThreadItem` union omits both. A diagnostic may observe App Server's [`rawResponseItem/completed` notification](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/app-server/src/bespoke_event_handling.rs#L1460-L1474) during one private turn and retain only whether search ran and whether its result offered native `spawn_agent`. This would establish that particular discovery outcome, not the complete model-visible catalog. The [tool planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs#L157-L269) builds effective visible specs after turn-context resolution; the reviewed generated App Server protocol exposes no supported complete-catalog read method. That last absence is an inference from the pinned implementation and protocol review, not a claim about undocumented methods.

Both the [v1 spawn handler](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/handlers/multi_agents/spawn.rs#L120) and [v2 spawn handler](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/handlers/multi_agents_v2/spawn.rs#L107) pass the parent's environment selections to children. [Shared spawn configuration](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/handlers/multi_agents_common.rs#L220) clones the current configuration and provider, then reapplies approval and permission state. Restrict role configuration to reviewed application-owned files; live child-policy and guarded-tool inheritance still require tests.

The experimental `multiAgentMode` request property is deprecated and ignored in this version. Its source directs proactive behavior through Ultra effort; it is not an enforcement control. Product instructions and authorization must remain separate from this deprecated property and from model choice.

## Required validation before relying on this policy

- Generate both intended protocol surfaces from the pinned binary and verify experimental negotiation, empty-environment thread creation, resume, and turn dispatch.
- Verify effective account/provider/config values without exposing credentials, raw protocol logs, or private context to the renderer.
- Exercise forbidden built-in file/process tools and demonstrate they are unavailable, including in a native child agent; absence from a UI is insufficient.
- Demonstrate the owned MCP server and allowed skills remain usable, with no unrelated tools or context discovered.
- Complete an authenticated reversible fixture edit, stale sequence rejection, interruption, reconnect, durable commit recovery, and shared undo. Source review and unauthenticated discovery cannot substitute for these checks.

The authenticated packaged synthetic run now proves model-side guarded trim and undo, live draft presentation, same-thread history recovery and Stop on a separate read-only turn. At that checkpoint it did not prove every forbidden built-in unavailable, native child policy, or user-supplied video editing. P2 phase completion remains open.

An initial authenticated child-request run ended uncertain with no persisted spawn item. A later run showed a generic live subagent activity before the transport closed, but interrupted history contained no completed spawn or child read; neither is a pass. App Server shares child notifications with the parent connection, while the former parent projector treated every different thread ID as a protocol failure. The client now filters ordinary validated foreign-thread events before parent buffering, preserving fail-closed handling for malformed IDs and forbidden methods. A corrected packaged medium-effort parent turn completed without connection loss, but its persisted parent turn history contained no collaboration item; a later rollout audit showed this is not a reliable v1 spawn test. A higher-effort attempt failed account startup before a turn. Two subsequent bounded `gpt-5.5` `xhigh` packaged turns completed without collaboration items in persisted parent history; the second explicitly requested tool-search discovery. A later private rollout audit found a successful spawn and linked child for one of these turns. Persisted turn history cannot represent a search call. At that checkpoint the strict native-child test remained unpassed and inheritance unverified.

An offline production guarded-service continuation against a real authenticated native Codex trim passed: the pre-edit sequence/hash with a new request ID returned `stale_draft`, and journal, draft, baseline and source bytes were unchanged. A later packaged native Codex turn sent the obsolete head through MCP and received the stale response in a failed App Server tool item, again without changing the draft, journal, baseline or source. Keep the direct-service and live-relay evidence distinct.

A bounded authenticated packaged-native negative probe asked for a harmless built-in command listing. The app reached ready with a completed reply, and the persisted turn held no command, file, dynamic or unrequested MCP/collaboration tool item. Journal, source and baseline were unchanged. The model said shell access was prohibited or unavailable; this is a model claim, not an inspected tool catalog. A separate audit resume with the production fixed overrides returned the expected provider, never-approval and read-only sandbox fields. An earlier bare audit resume changed defaults and failed its strict metadata assertion; it does not show that the product turn used those defaults. The observed turn did not invoke a forbidden tool, but complete effective tool unavailability remained unproved; a later separate native child run verified inherited read-only policy.

A later fresh packaged rerun extended this negative probe to the exact parent turn's private rollout, using `thread/read` and a bounded canonical path inside the isolated account. It observed zero raw tool-search calls, function calls, other call or unknown response items as well as the zero persisted tool items, unchanged journal/source/baseline and a settled native drawer inspected through the guest display. Two preceding setup runs failed before the command probe: one at its initial model-selection assertion and the other after reaching the project drawer without a saved model choice. The test then selected a discovered model through packaged Settings; those earlier failures remain private failures. The passing raw-turn audit closes the `thread/turns/list` blind spot for this one invocation, not the complete model-visible catalog. `effectiveToolUnavailabilityProven` remains false because the pinned runtime still registers MCP resource helpers and exposes no reviewed complete-catalog read method.

The pinned [rollout policy](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/rollout/src/policy.rs#L588-L829) persists v1 `FunctionCall` and `FunctionCallOutput` response items but filters `CollabAgentSpawnBegin` and `CollabAgentSpawnEnd` events. Its [history builder](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/app-server/src/thread_history.rs#L3724-L3817) can construct a collaboration item from live events, but a persisted `thread/turns/list` page cannot recover those filtered events. The prior absence of a parent collaboration item therefore did not establish that no child spawned. A private read-only audit found five persisted successful spawn outputs with matching child rollouts; three children completed both approved summary reads under matching parent/child read-only, never-approval turn policies. Two fresh packaged-native strict reruns then passed with one correlated spawn/output, App Server `thread/read` child parent/source, completed child-owned reads, policy equality and unchanged source, baseline and journal. This establishes the bounded native child path. It does not prove full effective absence of forbidden built-ins or managed browser-login completion. Raw rollout content and screenshots remain private.

The pinned `rust-v0.142.3` [turn router](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/session/turn.rs#L1179-L1302) supplies an MCP tool list whenever a server exists. The [tool planner](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core/src/tools/spec_plan.rs#L634-L647) then registers three MCP resource helpers and a plan tool independently of the owned server's current six tools. The current empty-environment and fixed-MCP policy limits effects but cannot prove an exact model-visible allowlist. No supported exact built-in-tool filter was found in this pinned runtime's reviewed configuration; P2-07 stays open. Future acceptance needs a supported runtime boundary or reviewed fork with authoritative parent/child model-visible specifications and registry assertions, followed by authenticated packaged native tests. A negative turn or model refusal is insufficient.

## Account-settings skill discovery

The settings client now sets `project_root_markers=[]` so the pinned [skill root finder](https://github.com/openai/codex/blob/rust-v0.142.3/codex-rs/core-skills/src/loader.rs#L405) stops repository-scope discovery at its app-owned context. A dedicated HOME alone does not restrict ancestor repository skills. The native test also keeps its account/context outside the source workspace. Runtime home, system and configured extra skill roots remain separate mechanisms; this does not establish a complete skill allowlist or authenticated conversation confinement.

A 2026-09-20 review of [official Codex 0.155.1](https://github.com/openai/codex/releases/tag/rust-v0.155.1) found that its supported tools.update_plan setting can disable the plan tool, but its [tool planner](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/core/src/tools/spec_plan.rs#L1128-L1144) still registers three MCP resource helpers whenever any MCP server exists. Public [thread](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/app-server-protocol/src/protocol/v2/thread.rs#L62-L160) and [turn](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/app-server-protocol/src/protocol/v2/turn.rs#L156-L260) requests expose no effective-tool allowlist. The [upstream MCP-only request](https://github.com/openai/codex/issues/6049) remains open. This source audit changes no pinned binary or acceptance result; a runtime upgrade would require regenerated protocol and packaged native regression evidence, and P2-07 still needs authoritative parent/child tool specifications.

## Current 0.155.1 Luna path (subsequent implementation)

The subsequent runtime upgrade was made because the earlier signed-in `0.142.3` model catalog offered only `gpt-5.5`, while the official `0.155.1` App Server returned `gpt-5.6-luna` with `high` reasoning for the same isolated account. The package now pins `0.155.1`, a 231-type experimental generator closure and hashes of both the native runtime and its same-version code-mode host. An initial Luna split failed without that host; a later package with the host passed a real guarded synthetic split and a separate private two-source edit. The code-mode host is a JavaScript tool runner, so its nested tool inventory requires a separate security check.

The first hosted Luna tool-list probe exposed unrelated ChatGPT-connected app tools even with `features.apps=false`. A trial `features.code_mode.excluded_tool_namespaces` policy did not remove that namespace. The official [configuration schema](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/core/config.schema.json) defines `apps._default.enabled=false` for disabling apps unless overridden; after adding that explicit default and excluding the remaining non-editor namespaces, a packaged synthetic Luna turn's code-mode probe reported the six owned editor tools and zero other `ALL_TOOLS` entries. It also found the tested app, goal, skill, spawn, image, web and shell tool properties undefined, while a real guarded split still committed and reopened correctly. The failed earlier probes remain private failures. Native child spawning is disabled on this runtime until a non-Astra child model and inherited policy can be verified. The single-turn code-mode inventory and non-use observations do not establish a permanent upstream effective-tool allowlist or restore the earlier `0.142.3` child evidence to the new runtime; P2-07 and full phase acceptance remain open.

The exact tagged [0.155.1 configuration type](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/config/src/config_toml.rs) also provides explicit `tools.update_plan.enabled`, `tools.experimental_request_user_input.enabled`, and `agents.enabled` controls. The current App Server launch and thread create/resume requests set all three false, so an account preference cannot simply re-enable them through a default. These switches reduce the registered surface but do not remove the MCP resource helpers or prove an exact effective tool allowlist. The final packaged Luna/high run passed a bounded owned-only nested code-mode inventory, real guarded edit, Undo and reopen; a separate exact-turn private rollout audit observed zero calls after a forbidden-command request. Both are bounded observations. The complete original-turn model tool catalog remains unavailable, so P2-07 stays open.
## Configured MCP and host-owned Apps isolation (later correction)

An empty CLI table override (`mcp_servers={}`) does not clear configured servers because Codex merges the table. The bridge therefore uses `codex mcp list --json` to read configured names, bounds and validates the result, supplies `-c mcp_servers.<name>.enabled=false` for every entry, and verifies the exact list is retained with each entry disabled. The app-server `mcpServerStatus/list` inventory is checked before thread open and again for the active thread; unexpected names, connected servers, tools/resources/templates, or a reserved owned-server collision fail closed. This is the runtime-specific containment established by the current test.

The initial process-level `features.apps=false` was not sufficient to prevent the host-owned `codex_apps` service from appearing in an authenticated thread. A direct probe found the service before per-thread feature switches were added. The pinned [0.155.1 registration condition](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/codex-mcp/src/mcp/mod.rs) requires Apps enabled and ChatGPT auth, so the bridge now sets `features.apps/plugins/remote_plugin=false` in both thread start and resume requests. The packaged hostile-config native child fixture passed with the external server disabled and host-owned service absent. The merge behavior is described in [Codex issue #16045](https://github.com/openai/codex/issues/16045). This is a bounded runtime result, not a claim that every future built-in tool is unavailable.
