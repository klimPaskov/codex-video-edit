# Traceability

P0 foundation scope and synchronization audit: [46_P0_FOUNDATION.md](46_P0_FOUNDATION.md). Rows describe required checks unless supported by actual execution evidence; they are not task completion claims.

| Requirement | Main spec | Tasks | Main tests |
| --- | --- | --- | --- |
| Current Gemini model capability filter | 08, 28, 29, 35, 39; ADR 0015; P2 API-provider research | P2-09 (partial) | `tests/media/desktop-api-providers.test.ts` checks the reviewed stable text/function families and excludes current Gemini 3.8 Live/TTS model IDs plus Gemini 3.1 Pro Preview. Focused test: 10/10 passed on 2026-09-25. This checks capability filtering only; authenticated Gemini discovery and generation remain unverified |
| Current Codex App Server tool-boundary review | 08, 21; P2 runtime policy research; codex-app-server, security-privacy, native-app-testing | P2-07 (partial) | Reviewed official 0.156.0 tagged thread/config types and release notes on 2026-09-25; no complete model-visible-tool allowlist is exposed in those public surfaces. This source review does not prove the effective runtime catalog, change the packaged 0.155.1 pin, or close P2-07 |
| Codex App Server named app override boundary | 08, 21; P2 runtime policy research; codex-app-server, security-privacy, native-app-testing | P2-07 (partial) | Pinned 0.155.1 types state `apps._default.enabled=false` can be overridden per app. A fresh packaged hostile profile enabled both `[apps._default]` and `[apps.adobe]`; the exact completed turn's account-contained rollout showed zero connected-app functions alongside the current 7-editor/5-v1-child dynamic surface, and the guarded split/Undo/reopen/source checks passed. This bounds the observation to this runtime and thread; the complete effective tool catalog remains open |
| Per-thread Apps and configured MCP isolation | 08, 21; P2 runtime policy research; codex-app-server, security-privacy, native-app-testing | P2-07 (partial) | Main discovers configured names through pinned `codex mcp list --json`, applies per-server disabled overrides and verifies the full status inventory before and after thread start/resume. Thread requests set Apps/plugins/remote plugins false. Packaged isolated Electron with a synthetic hostile MCP config passed a real native child read; the configured server stayed disabled, host-owned Apps service was absent, and source/baseline/journal remained unchanged. Empty-table merge behavior is documented. This is bounded runtime evidence, not a complete upstream built-in-tool catalog |
| Configured MCP discovery and per-server disable verification | 08, 21; P2 runtime policy research; codex-app-server, native-app-testing | P2-07 (partial) | `tests/media/codex-mcp.test.ts` asserts the exact `mcp list --json` plus per-server override calls, successful verification, fail-closed handling for still-enabled/added/removed entries, and rejection of malformed, duplicate or excessive names. The authenticated packaged hostile-config child fixture already passed for the valid path. This does not prove a complete upstream tool catalog |
| Native standalone window | 03, 04 | P1 | Playwright Electron launch, computer-use review |
| Real reader output and reproducible isolated runtime | 20, 22; native-app-testing | P1-05, P1-06 | tests/native/orca-smoke.test.ts binds reviewed versions, package/test hashes, real settings readback, application-owned focus and seven reader-output assertions; tests/desktop/provision-orca50.py verifies archives and uses fresh source/build/install directories; retain old-reader failures and inspect monitor screenshots; installation alone is not acceptance |
| Actual project create/open and persisted five-stage shell | 03, 04, 05, 07, 17; ADR 0013; project/source/timeline/revision contracts and extended desktop_ipc schema/example | P1-03, P1-05, P1-06 | tests/media/project.test.ts, project-view.test.ts, project-store.test.ts and IPC checks cover rational source baseline, full probe/integrity validation, path-free committed DTO, failure preservation and stage-only persistence; packaged native all-five-stage baseline/source preservation, permission-denied failure, reopen, scales/security/focus and guest-only visual checks passed; accepted in docs/workflow/results/P1.json with verified public source and scoped native evidence |
| Tagged H.264 still-frame preview and source fidelity | 03, 05, 07, 16, 35, 44; media library schema; media-engine, lossless-media and native-app-testing skills | P3-02 through P3-05 (partial foundation) | `tests/media/library.test.ts` checks H.264/AAC byte equality, native dimensions, three deterministic display frames, unsupported tags, decode-order PTS, fractional ticks and synthetic variable-cadence seek gaps; `tests/media/project-runtime.test.ts` checks committed trim/stale mapping; `tests/native/h264-preview.test.ts` checks packaged Linux import, canvas-to-decoded-display pixels including an in-between seek, reopen and guest-only visible seek. Private user inputs were exercised only in an ignored isolated guest, separately from public synthetic acceptance. That earlier slice did not prove two-source mapping; the next row records its later partial implementation. No full A/V playback, master export or P3 acceptance is claimed |
| Ordered compatible two-source draft and native join preview | 03, 05, 07, 16, 30, 44; two_source_initial_project and desktop_ipc schemas/examples; media-engine, timeline-editor, native-app-testing | P3-04 (partial foundation) | `tests/media/two-source-project.test.ts`, timing/domain/IPC tests and `tests/native/two-source-project.test.ts` cover measured final packet boundary, ordered source identity, trim/undo reflow, immutable hashes, tamper rejection, packaged native picker, join pixels and reopen. After synthetic acceptance the two supplied inputs were privately imported in order through packaged Electron, reopened and visually inspected on both sides of the join, with decoded-display byte and source-hash checks. Private evidence stays ignored. Continuous A/V, AI edit on user media, master export and P3 acceptance remain open |
| Native manual trim, Undo and Redo | 03, 05, 10; P2/P6 phase prompts; desktop_ipc manual edit schema/examples; timeline-editor, simple-desktop-ui, native-app-testing | P2-05, P6-01 (partial) | `tests/media/draft-transactions.test.ts` covers stacked Redo replay after reopen, LIFO order, stale rejection and redo clearing after new work; strict IPC/project-view tests cover the path-free request and committed target. Packaged `tests/native/two-source-project.test.ts` passed synthetic trim, shared Undo, Redo before and after project reopen, exact decoded preview at the reapplied cut, a follow-up Undo, and immutable source/baseline checks. Playwright assertions and guest-only visual inspection covered the actual packaged Edit window. Earlier supplied-footage testing covers trim/Undo only; this Redo run used synthetic sources. Remaining editor tools, audio review, Magic Wand/Codex user-footage edits and export are open |
| Native interior split and fragment projection | 03, 05, 10; P2/P3 phase prompts; desktop IPC and draft transaction split schemas/examples; timeline-editor, simple-desktop-ui, native-app-testing | P2-05, P3-04, P6-01 (partial) | Strict `projects:manual-split` and shared transaction reducer retain the left ID, derive the right ID from the trusted operation, preserve half-open source order and total duration, and bound fragments to 4096. `npm run check` passed 42 foundation and 249 media tests with two skips. Packaged Linux Electron synthetic two-source Split→fragment trim→two undos→reopen passed exact decoded-display checks at source join and fragment boundary, immutable source/baseline and guest-only 200% window inspection. After fixture acceptance, both supplied recordings passed the same private native split/reopen and integrity path in order. Full A/V review, Codex/Magic Wand user-footage editing, canonical render and export remain open; no phase result is claimed |
| Native marked ripple range cut | 03, 05, 10, 31; P2/P3/P6 phase prompts; desktop IPC and draft transaction range-cut schemas/examples; timeline-editor, simple-desktop-ui, native-app-testing | P2-05, P3-04, P6-01 (partial) | Strict `projects:manual-range-cut` carries an exact head and nonempty half-open output interval. Reducer tests cover cuts across fragments/source joins, complete visible source omission with immutable inventory retained, reflow, whole-draft rejection, replay and newest Undo. `npm run check` passed 42 foundation and 255 media tests with two skips. Final packaged Linux Electron synthetic fixture passed cross-source mark/cut, exact surviving join pixels, Undo/reopen and source/baseline integrity; the corrected default-size window kept Cut range in view, and 200% bounds plus guest-only visual inspection passed. After this fixture, both supplied private recordings passed the same isolated native cross-source cut, join pixel, Undo/reopen and integrity path. An earlier below-fold native visual finding was fixed and rerun. Synchronized A/V, Codex/Magic Wand user-footage edits, full review, master export and phase acceptance remain open |
| Guarded Codex/API range-cut tool | 08, 10, 21, 30, 31, 34, 47; six-tool manifest example, Codex editor prompt and codex-app-server skill | P2-05, P2-06, P2-10 (partial) | `cut.delete_range` routes a bounded half-open output interval through the shared `ripple_delete` reducer with exact draft-head freshness and trusted Codex/API-provider origin. Focused service/MCP/provider tests, exact packaged inventory, a real authenticated Codex synthetic two-source cross-source cut/preview/Undo/reopen and a separate real Codex turn on the privately combined supplied sequence passed in isolated packaged Electron, with immutable source/baseline checks and guest-only visual inspection. Earlier private harness memory/seek failures remain failed attempts. Present read tools provide no transcript or audio selection evidence; no automatic speech cleanup or seamless join is established. Paid API editing, P2-07 confinement and full A/V/export gates remain open |
| Guarded Codex/API split tool | 08, 10, 21, 30, 31, 34, 47; six-tool manifest example, Codex/API prompts and codex-app-server skill | P2-05, P2-06, P2-10 (partial) | `cut.split` routes an exact interior clip/output-time intent through the shared split reducer with trusted Codex/API origin, immutable source and newest Undo. Service, MCP, provider-adapter and refresh tests passed. A real signed-in Codex turn in packaged isolated Electron split a synthetic two-source draft into three fragments while running; exact start/split/join decoded pixels, manual Undo, reopen, source/baseline integrity and guest-only visual inspection passed. An earlier native test asserted Undo before asynchronous settlement and failed; its corrected rerun passed. After fixture acceptance, both supplied recordings were imported in order and passed the real Codex split, exact preview, shared Undo, reopen and immutable-source checks in the isolated native window; a later Luna/high run repeated this with the current packaged runtime. Speech meaning, paid API editing, P2-07 tool confinement and full A/V/export remain open |
| Interface scale through 200% | 04, 05, 22; desktop_preferences and desktop_ipc docs/schemas/examples | P1-05, P1-06 | Five storage and seven IPC tests passed for the 200% extension; packaged native keyboard/focus and guest-only layout inspection at 200% passed; prior 150% evidence remains separate |
| Persistent interface scale, settings modal and source focus | 04, 05; docs/schemas/desktop_preferences.schema.json; docs/schemas/desktop_ipc.schema.json; matching docs/examples/desktop_preferences.example.json and docs/examples/desktop_ipc.example.json | P1-03, P1-05, P1-06 | Strict preference/IPC and persistence tests; actual packaged modal save/cancel/Escape, Ctrl+,, one-inspector restoration and source/Back focus tests at supported sizes/scales; bounded native inspection passed; P1 accepted in docs/workflow/results/P1.json |
| Real Codex App Server | 08; ADR 0002 as revised by 0014 | P2-01 through P2-07 | Official auth, model, skill, thread, native child and guarded live-edit smoke; existing bounded evidence does not complete P2 |
| Explicit OpenAI API and DeepSeek key providers | 03, 05, 08, 21, 28, 29, 31, 35, 39; ADR 0014; P2 API-provider research; typed provider and thread IPC contracts/examples | P2-08 through P2-10 (partial) | Fixed-endpoint, key-redaction, key-storage, bounded thread, shared-journal and packaged disconnected native checks exist. An isolated packaged OpenAI API key authenticated live model discovery/selection, filtered to reviewed chat/tool families, and session-only restart. Unit tests cover encrypted remembered-model restart, legacy key-only migration, live catalog revalidation, replacement clearing and redacted save failure. A separate packaged Linux GNOME Secret Service test passed protected OpenAI key/model reopen and removal. Packaged native OpenAI and DeepSeek runs used selected main-only deterministic transports to pass draft trim, stale rejection, undo, provider-failure and 429 redaction, source/baseline preservation and project reopen; their model text was synthetic. A live packaged OpenAI-key edit attempt received HTTP 429 and committed nothing. A later live packaged DeepSeek turn discovered a reviewed model, made three successful completion calls from one explicit Send, committed exactly one guarded trim, projected exact native pixels and duration, reopened the project, preserved source/baseline bytes and did not restore its session-only key. Other OS keyrings, live Gemini and complete P2 gates remain open |
| Shared Undo after authenticated provider edit | 08, 10, 21, 27; guarded transaction and native-testing skills | P2-10 (partial) | A keyless isolated packaged continuation of the live DeepSeek fixture used Edit-stage manual Undo to target the provider trim and restore original duration and pixels after decode. The strengthened paid native harness later passed the whole live edit, reopen, Undo, exact restored preview and second reopen sequence with immutable source/baseline and a removed session key. Its first full attempt failed only at a zero-sized canvas read during final decode; the corrected bounded wait passed separately. Synthetic tests cover provider errors; semantic A/V and export acceptance remain open |
| Fixed Gemini API-key provider | 03, 05, 08, 21, 28, 29, 35, 39; ADR 0015; P2 API-provider research; typed provider/thread IPC schema/example; codex-app-server, security-privacy and native-app-testing skills | P2-08 through P2-10 (partial) | Fixed Google compatibility destinations, separate main-owned key/model state, reviewed stable text/function IDs intersected with live discovery, bounded transient Gemini function-call signatures and shared journal intent are covered by unit tests. Packaged isolated Linux Settings rejected an invalid key with a separate empty Gemini state. A synthetic Gemini 3.1 Flash-Lite tool loop passed native trim/preview, exact thought-signature replay, stale rejection, shared Undo, redacted failures, immutable source/baseline and reopen; guest-only screenshot confirmed the actual selected Gemini drawer and committed preview. OpenAI/DeepSeek synthetic native regressions also passed. The live fixture harness now requires a reviewed Gemini 3 model and audits signature replay by count without writing signature bytes. No authorized Gemini key, live catalog, paid completion or live signature replay was tested; P2 remains incomplete |
| API connection failure and visible drawer recovery | 03, 05, 08, 21, 35, 39; desktop_ipc schema/example; native-app-testing | P2-08 through P2-10 (partial) | Main classifies completion 401/403 without reading the body; strict thread view persists fixed key/account-access recovery text and the user request. Focused unit tests and packaged deterministic OpenAI/DeepSeek native tests verify redaction, unchanged journal, readable alert inside the selected drawer, source/baseline preservation, shared undo and reopen. Separate 1366×768 and 200%-scale OpenAI native runs confirmed alert viewport containment and at least 80 CSS px of independently scrollable history; at 200% preview and drawer were inspected after scrolling between them. The live fixture harness now passed an authenticated DeepSeek trim, while failure recovery remains supported by separate synthetic native and unit evidence; a live Gemini turn has not run |
| Main-owned active-project read bootstrap | 08; CODEX_IN_APP_EDITOR_PROMPT; codex-app-server | P2-04, P2-05, P2-06 | Desktop Codex regression covers validated project_id/schema_version developer context on create/resume without paths, titles or draft snapshots; the first native attempt exposed missing identity, then the corrected signed-in packaged run completed read/trim/undo turns and reopened the same thread; read tools still establish current draft sequence/hash |
| Quarantined disconnect and shutdown | 08; P2 runtime policy research; codex-app-server | P2-01, P2-04, P2-07 | Preserve projector poison, publish active-turn uncertainty once, and close transport despite notification callback exceptions; the corrected package passed the bounded authenticated flow. An offline guarded-service continuation against a later real Codex native edit rejected stale sequence/hash, and a later packaged native turn proved live App Server/MCP stale-error relay with unchanged journal, source and baseline. Pinned MCP resource helpers remain exposed by the runtime and unreviewed items remain quarantined; exact owned server inventory is not a complete built-in-tool absence proof |
| Authenticated trim, undo and faithful live/history projection | 08; codex-app-server | P2-04, P2-05, P2-06 (partial) | A private packaged native synthetic run passed one Codex-origin start trim at sequence 1 (1.5s to 1.0s), observed committed controls and exact retained pixels while the turn was running, reopened the same actual thread with matching history, and used real Codex undo at sequence 2 to restore 1.5s; source/baseline and journal verified. A read-only Stop interrupted without journal change and reopened. Guest-only visual evidence confirmed drawer-to-Source-inspector selection. An offline guarded-service stale request against a later real native edit passed; live App Server/MCP stale rejection passed in a later packaged native turn; a separate strict packaged native child run passed inherited read-only policy |
| Pinned real stdio and thread runtime | 08; codex-protocol contract; P2 runtime policy research; codex_thread_view schema/example | P2-01, P2-03, P2-04, P2-07 (partial) | 207-type experimental generated closure; transport/client/thread/stream/registry/view tests cover exact requests, create/resume, bounded inline/fallback recent history, active-turn restoration, ordered streaming, interrupt, idle unsubscribe, retry, redaction and forbidden-history quarantine; isolated packaged signed-out startup verifies owned MCP names and input schemas; separate signed-in packaged run verifies the bounded edit/history/Stop flow. Child notifications remain outside parent projection. On the dynamic route, only a completed server-owned spawn with a correlated active child turn authorizes read-only summary calls; the packaged native test exercises early child-turn ordering and message isolation. The test does not establish complete effective tool confinement |
| Native Codex account/settings source | 05, 08, 39; Codex view/IPC and device-login contracts/examples; packaged runtime resolver | P2-01, P2-02, P2-03, P2-07 (partial) | Auth state/URL mapping and packaged-resource tests; packaged Linux signed-out login initiation/cancellation/reopen/scale and guest-only visual checks passed. A separate browser-equipped isolated guest opened the real OpenAI browser-login page from packaged Settings, then cancelled to signed out; this is opener evidence only. The official device-code path passed start, fixed-page opening, cancel and transient-code clearing. In another packaged live attempt the user completed device verification externally, the same native window reconciled to signed in and cleared the code, and a separate packaged reopen recovered the authoritative signed-in plan, live models and skills. Browser OAuth callback completion remains unverified. A separate seeded-account Settings run matched returned plan, model, usage windows/reset times and skills across restart; this does not substitute for the managed device-code completion evidence |
| Native child spawn, read and inherited policy | 08; P2 runtime policy research; codex-app-server; native-app-testing | P2-04, P2-07 (partial) | One current packaged Linux Electron Luna/high dynamic-route run passed a server-owned completed parent spawn and wait; `thread/read` linked its receiver ID to the child parent/source; both project and timeline summary calls completed on the child thread; and saved child policy exactly matched the parent read-only/never-approval, empty-environment configuration. An isolated early child-turn candidate was authorized only after the completed parent spawn identified the receiver. Child message events did not enter the parent projection. Source, baseline and journal stayed unchanged. The guest-only native screenshot was visually reviewed and showed the actual Electron window, single Codex drawer, synthetic preview and no browser chrome or persistent Ready badge. Pinned persisted parent turn history omits v1 collaboration events, so it is not the spawn oracle. Complete effective-tool confinement, managed browser-login completion and Windows acceptance remain open |
| Bounded authenticated forbidden-command probe | 08, 20; P2 runtime policy research; native-app-testing | P2-07 (partial) | Packaged Linux guest project and real model turn returned ready; official history held no command, file, dynamic or unrequested tool item. A fresh rerun also correlated that exact turn to a bounded private rollout with zero raw tool-search, function, other call or unknown response items. Journal/source/baseline stayed unchanged and the native window was inspected through the guest display. A separate audit resumed with the production policy. This proves observed non-use in that turn, not complete model-visible tool absence; earlier setup and bare-audit failures remain private |
| Project import and immutability | 07, 17 | P3 | hash, reopen, recovery, mutation tests |
| Screen, audio, camera recording | 06 | P4 | virtual-device sync and recovery |
| Magic Wand raw edit | 09 | P5 | meaning, undo, interruption, media QA |
| Manual simple editor | 10 | P6 | pointer, keyboard, history, revision tests |
| Automatic and manual zoom | 11 | P7 | target, edge, motion, adjustment tests |
| Speed-ups and pitch | 12, 15 | P7 | boundary, duration, pitch, A/V tests |
| Captions | 13 | P8 | timing, safe area, sidecar tests |
| B-roll and layouts | 14 | P8 | provenance, crop, fallback, render tests |
| Audio cleanup | 15 | P8 | loudness, clipping, sync, ducking tests |
| Export and verification | 18, 19 | P9 | render, decode, hash, manifest tests |
| Native isolated testing | 20; tests/desktop/README.md | P0-04 infrastructure; all product phases | Docker isolation validation, sandboxed Electron/Playwright probe and guest-only native input/capture establish infrastructure; the P2 packaged visual run observed the Codex drawer and one Source inspector after a guest click. Product phase recordings and audio review remain required |
| Screenshot update process | 26 | P10 and changes | manifest and visual regression checks |
| Cross-file sync | CHANGE_CONTROL | all | spec-sync report |
| Reproducible repository foundation | 46; docs/research/P0_FOUNDATION.md | P0-01 through P0-05 | Strict type, lint, format, unit, schema/example and phase-result-writer checks |
| Synthetic lossless boundary | 44, 46 | P0-06, P0-08 | Raw canonical no-op and edited-frame/audio encode/decode equality; unsupported-format and proxy-isolation rejection |
| Actual native media bootstrap prerequisite | 03, 05, 46; ADR 0012 | P0-04, P0-06 | Packaged product security/IPC checks; native synthetic import, immutable source hashes, library reopen, frame equality/seek and explicit unsupported-preview tests; Playwright Electron and computer-use product evidence |
| Safe source publication | 45, 46 | P0-07 | Staged path/content review, public-source audit, exact verified remote commit and CI |

| Requirement | Specification | Implementation tasks | Required proof |
|---|---|---|---|
| Lossless default and explicit smaller modes | docs/44_LOSSLESS_MEDIA_POLICY.md | P0-08, P3-03, P4-03, P9-03, P10-05 | Canonical sample comparison and proxy isolation |
| Shared draft transaction and owned MCP foundation | 08, 10, 17, 21, 30, 31; draft snapshot/transaction/checkpoint schemas and examples; guarded-tool contract | P2-05 | FFmpeg-backed tests cover trusted manual/Codex/Magic Wand origins, exact sequence/hash freshness, one bounded trim reducer, active-project summary/trim/newest-undo tools, atomic journal replay, idempotence, deterministic undo, checkpoint scope/recovery, source and baseline immutability, navigation serialization, same-head conflict, forbidden tool input and redacted errors; project-runtime and renderer-reconciliation tests cover atomic project/draft views, head-bound frame decode, stale-pixel rejection and mutation settlement refresh; direct child/broker tests cover forwarding, allowlist, bounds, errors and tamper; signed-out startup verifies owned inventory. A signed-in packaged run proves guarded Codex trim and shared-engine undo with committed native preview. A later offline guarded-service stale request against a real Codex-created native draft passed with unchanged journal/source/baseline; live App Server/MCP stale rejection passed in a later packaged native turn; effective forbidden-tool restriction remains open |
| Ordered private acceptance sources | 00, 02, 07, 19, 20, 34, 40; example prompt/checklist | P10-01, P10-03, P10-05, P10-06 | Two private hashes/probes and order retained outside Git; later native import must append source two after source one, preserve both originals, reopen, review the join, export, and verify the edited canonical master |
| Public implementation | docs/45_OPEN_SOURCE_DEVELOPMENT.md | P0-07 and every working slice | Verified remote commits and public CI |
| Individual native UI references | docs/references/manifest.json and IMPLEMENTATION_NOTES.md | P1-04, P1-06, P6-03 | Real native screenshots and interaction checks |

| Five-stage navigation and authorized live draft editing | 02, 04, 05, 08, 30, 46; ADR 0003 | P0-05, P1-03, P2-05 | tests/foundation/test_workflow_contracts.py contract checks; packaged native stage persistence and committed bounded-trim preview checks pass; private signed-in packaged synthetic run observed the real Codex trim's committed controls and pixels during the running turn and verified shared undo. Broader editing and P2 gates remain required |

## Editorial first-cut requirement mapping

P1 lifecycle and accessibility evidence additionally maps docs/03, docs/20, docs/22 and the native-electron skill to P1-01/P1-05/P1-06: `tests/native/lifecycle.test.ts`, `tests/native/accessibility.test.ts`, and the real Orca smoke/AT-SPI observer. Keep packaged test assertions, guest visual inspection and bounded screen-reader output distinct. The new checks do not establish P1 acceptance until they run and their artifacts are reviewed.

[47_EDITORIAL_FIRST_CUT.md](47_EDITORIAL_FIRST_CUT.md) and `docs/prompts/EDITORIAL_FIRST_CUT_PROMPT.md` are accepted later-feature requirements. The following evidence remains required; these rows assert neither implemented automation nor P1 completion. Typed editorial/operation/zoom/opportunity contracts and examples must be synchronized as implemented.

| Requirement | Specifications / skill routes | Tasks | Required acceptance evidence |
| --- | --- | --- | --- |
| Actual context, one transcription job, authorized scope and synchronized cut sets | 09, 13, 32, 47; magic-edit; media/recording/Codex engineers | P2-05, P4-03, P5-01 | Real job identity/polling; missing capability and independent-source rejection; protected/approved edits preserved |
| Conservative spoken cues and complete semantic cut | 09, 13, 32, 47; magic-edit | P5-03, P5-05, P10-02 | Configured and opt-in legacy aliases; quoted/ambiguous/hostile cues; final complete redo and unique context; pauses supporting demonstrations; every join, omission pass, full reread and restoration |
| Shared live pass groups and verified checkpoints | 32, 47; magic-edit; editor/Codex engineers | P2-05, P5-04, P5-06 | Current sequence/hash after structural edits; atomic persistence before announcement; abort or compensating undo; no false checkpoint/revision/export claims |
| Approved purposeful layouts | 14, 37, 47; broll-layout | P8-02, P10-02 | Property reuse, preferred-corner collision handling, screen-only return, meaningful section count, both edges/midpoint and protected-region QA |
| Selective graphics opportunities only | 14, 32, 47; broll-layout | P8-03, P10-06 | Final-cut timing/remapping; complete grounded generator-neutral prompts; proposed asset dimensions distinct from master; zero generation/import/placement without separate asset request |
| Initial graphics suggestion data validation | graphics_opportunity schema/example; domain graphics-opportunity.ts | P8-03 | tests/media/graphics-opportunity.test.ts covers detached validation, stale context, bounds, exact rate/duration, strict fields and sparse-array rejection; actual suggestion generation/UI remain unimplemented |
| Named screen/camera zoom targets | 11, 32, 47; automatic-zoom | P7-01, P7-07, P10-02 | Exact target and mode/layer evidence; readability-based scale/duration/rests; overlap/wrong-cursor/empty-target/camera-collision rejection; boundaries/midpoint/interior review |
| Final editorial QA/report and source fidelity | 32, 44, 47; native-qa/security/media engineers | P9-02, P10-03, P10-06 | Whole-video analysis scan plus precise rendered A/V; protected ranges; actual duration/counts/targets/unresolved cues/checkpoint state; analysis/master isolation and no automatic export |
| Current Luna/high tool and interruption policy | 08, 21, 30, 39; ADR 0016; codex-app-server, security-privacy, native-app-testing | P2-03, P2-04, P2-07 (partial) | App Server launch and thread create/resume disable plan, user-input, app and unrelated external tool namespaces. MCP-bound threads keep native collaboration disabled; new dynamic Codex threads enable supported v1 children at depth 1 for correlated read-only summary access. Current packaged 0.155.1 Luna/high split inventory is verified from the exact completed turn's account-contained rollout: seven guarded editor functions, five supported v1 collaboration functions only on the dynamic route, zero connected-app functions and zero other nested functions. Baseline and hostile per-app override fixtures passed split, exact frames, shared Undo, reopen and immutable inputs. The final restricted package also passed real Luna/high trim/Undo/Stop/history, a private-rollout forbidden-command audit, and the packaged child-read fixture. These bounded results do not establish complete effective-tool confinement. No Astra agent is used. |
| Host-defined tool protocol feasibility | 08, 21; P2 dynamic-tool research; codex-app-server, native-app-testing, security-privacy | P2-07 (partial) | A direct isolated official 0.155.1 App Server probe with no MCP server discovered live Luna/high, registered one namespaced dynamic tool, and completed a turn with exactly one correlated host `item/tool/call`. Its code-mode `ALL_TOOLS` inventory and exact private rollout cell showed only that tool and no other raw custom-tool name. A separate process-restart probe resumed the exact thread and completed a second owned tool call. Two earlier failed setup variants remain distinct. Existing project threads still use guarded MCP, so this does not prove product tool confinement, legacy migration, full history projection, native children, or a packaged draft edit |
| Optional guarded host-call adapter | 08, 21, 30; codex-protocol generated closure; codex-app-server, security-privacy | P2-07 (partial) | Generated `DynamicToolCallParams`/Response pinned to 0.155.1; six-name/schema parity, exact request and response bounds, stale/foreign quarantine and generic owned stream/history projection pass focused tests. A validated owned `turn/started` notification can provisionally authorize a call before the matching RPC response; unit tests reject unknown, completed and contradictory turns. A direct isolated 0.155.1 Luna/high client completed one owned read without MCP, but did not establish an early-call delivery. At this earlier checkpoint the packaged product did not yet route dynamic tools; the later mixed-route row records the product native run |
| Live host-call turn timing | P2 dynamic-tool research; native-app-testing, codex-app-server | P2-07 (partial) | Direct isolated 0.155.1 Luna/high read-only probe using the six-tool adapter: one owned host call after validated `turn/start`, zero early/foreign/mismatched calls, completed turn. Two failed older-package runs had no host call. This does not prove all scheduling orders, MCP-thread migration or packaged Electron editing |
| Durable Codex tool route | 08, 21, 30; codex thread-registry schema/example; codex-app-server, security-privacy, native-app-testing | P2-07 (partial) | Registry v2 tags MCP or dynamic; strict v1 decode retains MCP without read-time rewrite; unit tests prove atomic later migration, mismatch/corruption rejection, dynamic start/resume and cross-route host-call quarantine. Packaged Luna/high MCP edit/Undo/reopen/interruption passed, and a synthetic v1 file reopened the same thread for an owned read without mutation; a later product write preserved its ID in v2. A second full edit harness failed after its correct trim and remains a failure. Product still uses MCP; dynamic draft editing remains open |
| Packaged mixed Codex tool routes | 08, 21, 30; route-specific in-app prompt; codex-app-server, security-privacy, native-app-testing | P2-07 (partial) | New conversations use six host-defined tools and stored MCP bindings retain MCP. Focused route-switch tests and packaged isolated Luna/high synthetic dynamic trim during turn, committed preview, same-thread/history reopen, real Codex Undo, restored exact pixels, Stop and source/baseline integrity passed with guest-only native visual inspection. The same package reopened a v1 MCP conversation for an owned read without rewriting the binding. A dynamic negative-command probe had no forbidden invocation in that turn. A copied-profile legacy attempt failed because the baseline embeds absolute paths; the in-place guest rerun passed. Full effective-tool confinement and P2 acceptance remain open |
| Live stale-draft relay on both routes | 08, 21, 30; P2 dynamic-tool research; codex-app-server, security-privacy, native-app-testing | P2-07 (partial) | Route-aware native continuation from a real Codex trim/Undo fixture passed direct guarded-service rejection and one correlated host-defined `cut_trim_edge` call with obsolete sequence/hash. The authoritative completed turn carried the safe unsuccessful stale-draft result; current draft, both journal records, original, managed source and baseline were unchanged. The adapted one-edit MCP fixture also passed a live stale call without changing its draft, journal or immutable inputs. This narrows live error-relay evidence, not complete effective-tool confinement |
| Dynamic owned inventory, app override and two-source split | 08, 21, 30; P2 dynamic-tool research; codex-app-server, native-app-testing, security-privacy | P2-05, P2-07 (partial) | The current packaged Luna/high dynamic route requires seven guarded editor functions, five supported v1 collaboration functions, and zero connected-app or other nested functions. Its inventory evidence matches the exact `exec` `custom_tool_call` and paired output from the completed turn's account-contained private rollout. Fresh-profile baseline and hostile `[apps._default].enabled = true` / `[apps.adobe].enabled = true` runs passed live inventory, committed split, exact frames, shared Undo, reopen and immutable inputs. Earlier six-tool/zero-unowned counts describe the prior catalog and route state. Prior synthetic and private supplied-footage split runs remain separate bounded editing evidence. This does not establish a permanent effective-tool catalog or P2 acceptance |
| Atomic confirmed range batch | 08, 10, 21, 30, 31, 34, 47; draft-transaction schema; seven-tool manifest/example; in-app/API prompts; timeline-editor, codex-app-server, security-privacy | P2-05 (partial) | `cut.delete_ranges` accepts 2–16 descending disjoint half-open output ranges under one exact head, commits one two-or-more-operation journal transaction, and shares newest Undo. Reducer, service, MCP/schema and dynamic parity tests passed. A packaged Luna/high host-defined turn passed on synthetic and the two private recordings in order: committed draft and exact edited-join preview, manual Undo, reopen, immutable original/managed sources and baseline. The supplied guest native window was visually inspected. A separate fresh packaged Luna/high split turn reported exactly seven owned and zero unowned nested tools, then passed guarded split/Undo/reopen. No transcript-based range selection, listened A/V join QA, verified checkpoint, permanent tool confinement or final master is established. |
| Authenticated dynamic batch read-before-edit | 08, 21, 30; Codex thread history and guarded-tool contracts; native-app-testing, codex-app-server | P2-05, P2-06 (partial) | Fresh-profile packaged Electron synthetic runs match the exact completed App Server turn and require successful bounded direct `dynamicToolCall` outputs bound to the active project, both summary reads before one mutation, and current sequence/hash fields. They passed committed-during-turn state, shared Undo, reopen, exact preview, immutable sources/baseline and a guest-only visual inspection. A separate supplied-footage structural batch passed the same edit/reopen checks. One reused-profile mismatched-project read failed before mutation and remains recorded. Audio listening, semantic editing, Magic Wand and lossless export remain unverified. |

P1 acceptance is recorded in `docs/workflow/results/P1.json` against verified public revision `33d3fdc1d87787322c658fad88a1c4fa9787f2d1`. Its evidence and limitations govern the P1 rows above. P2-P10 and the adapted editorial runtime remain incomplete.
