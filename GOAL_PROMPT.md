Build codex-video-edit as a new standalone desktop recorder and AI video editor in this repository. Read AGENTS.md, AUTHORITATIVE_ORDER.md, PLANNING_PACKAGE.md, WORKFLOW.md, TASKS.md, the active phase, accepted ADRs, contracts, relevant skills, and reference notes. Implement the first incomplete dependency-safe phase. Do not stop at plans or mockups.

Create a public GitHub repository named codex-video-edit under the authenticated owner's account during P0. Verify any existing remote. Include the MIT licence, contribution guidance, and CI. Commit and push reviewed working slices throughout development. Never publish recordings, user projects, credentials, private test evidence, or bundled assets without permission. Report access blockers.

Use a secure packaged Electron desktop window, typed IPC, local storage, and deterministic media services. The app must not open as a website or browser tab. Build and autonomously launch it only in your isolated desktop environment, never on the user's host. Use browser research, Playwright Electron, and computer use to test the native window. Browser checks alone cannot prove native behavior.

Support screen, window, region, microphone, system audio, optional camera, separate synchronized sources, scenes, scripts, teleprompter, retakes, and video import. Provide transcript editing, cuts, trim, split, restore, undo, zoom targets, speed ranges, captions, camera layouts, cursor effects, local B-roll, music, audio tools, revisions, review, and export.

Follow Record or Import -> Auto Edit -> Edit -> Review -> Export. Use the individual references and their correction notes. Show one relevant inspector or the Codex drawer, not every panel. Remove debug badges, ready indicators, status dashboards, oversized page titles, repeated headings, and obvious descriptions. Keep useful labels, recording indicators, progress, and actionable errors. Use original, restrained styling.

Integrate real Codex App Server over stdio with supported ChatGPT subscription sign-in. Codex is the only generative AI provider initially. Discover available models and skills. Preserve real threads, streaming, interruption, and supported native subagents. Codex, Magic Wand, and manual tools share one validated transaction engine and undo history. Apply authorized reversible edits to the active draft while the turn runs. Update timeline, controls, and preview from committed state, never fabricated chat. Keep source deletion, cleanup, spending, publication, and final export under explicit user control.

Magic Wand must produce an editable first cut with meaning-preserving speech cleanup, useful automatic zooms, captions, optional speed changes, audio treatment, and local B-roll. Make each result adjustable and undoable. Do not claim that changing transcript text resynthesizes speech.

Default to lossless capture, lossless intermediates, and a lossless master. Preserve original files, native detail, sample precision, and color metadata. Use a verified FFV1/PCM master profile. ProRes 422 HQ and ordinary H.264/AAC exports are not mathematically lossless. Lower-quality previews and MP4 sharing copies are explicit opt-ins and never feed the master. Verify decoded frame and audio equality against canonical render samples. Intended edits change samples. Do not promise they remain identical to unedited footage or silently downgrade unsupported inputs.

After fixture tests pass, import the user-supplied example video through the app. Let built-in Codex and Magic Wand edit it, refine with manual tools, reopen the project, review the whole result, export the master, and verify it. Fix failures and repeat. Update affected specs, skills, prompts, routing, schemas, tests, and references with every workflow change. Record truthful phase evidence under .astra/results/. Never invent login, hardware access, tests, screenshots, native launches, or completion.
