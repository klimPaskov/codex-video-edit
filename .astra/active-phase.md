# Active phase

- Phase ID: `P2`
- Task IDs: `P2-01` through `P2-07`
- Prompt: `.astra/phases/P2_REAL_CODEX.md`
- Required skills: `codex-app-server`, `security-privacy`, `native-app-testing`, `spec-sync`; apply `timeline-editor` to the shared transaction foundation and `open-source-development` to publication.
- Required result: `.astra/results/P2.json`

P0 and P1 are accepted in their result records. P1 binds to verified public main revision `33d3fdc1d87787322c658fad88a1c4fa9787f2d1` after PR #10 Windows/Linux checks passed. Its private evidence covers the actual packaged local Electron shell, immutable synthetic import, project/baseline identity, persisted five-stage navigation, failed-save preservation, bounded lifecycle recovery, 100-200% scale, keyboard and guest-only visual inspection. The real Orca 50.2/AT-SPI 2.56.8 smoke passed seven controls under natural activation. Retained failures and Linux-only limitations are explicit in `.astra/results/P1.json`; no later feature is accepted by navigation alone.

Implement real Codex app-server over stdio with the supported ChatGPT-managed sign-in and exact generated protocol contract. Discover models and skills at runtime; preserve actual project threads, streaming, interruption and supported server approvals/subagents. No fake production transport or hardcoded model catalog. Official Codex 0.142.3 is now installed in the isolated guest, with exact generated protocol types and a real signed-out bootstrap/skill-discovery test. Packaged signed-out account/settings controls now pass real login-initiation/cancellation/reopen and guest-only visual checks; the test suppressed external browser launching. Managed sign-in completion, authenticated model discovery and all edit gates remain unverified. Continue the native integration and report actual authentication access blockers without copying host credentials or inventing a login.

Apply ADR 0013 before the real fixture edit: implement one shared transaction engine with expected sequence/hash checks, durable journal, atomic persistence before notification, deterministic undo and complete-transaction recovery. Manual tools, Codex and later Magic Wand reuse it. Preserve sources and the immutable baseline; stage navigation is separate from draft history. Apply the editorial pass-group and truthful checkpoint requirements in `docs/47_EDITORIAL_FIRST_CUT.md` and its prompt. The supplied editorial reference was adapted as requirements; its runtime is not implemented yet.

Product builds, launches, screenshots and input stay inside isolated Docker guests. Never launch or control the app on the user's host. Use Playwright Electron and the guarded guest X11/XTEST/x11grab helper, with no host mounts, devices, credentials or viewer. Keep all private media, projects and evidence out of Git. The user-example video remains unimported and unedited. No audio listening, continuous A/V playback, Windows capture/device/installer, real Codex edit or full export workflow is accepted. P2-P10 remain required.
