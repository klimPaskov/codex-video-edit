# P0 implementation progress

Status: accepted foundation. P0-01 through P0-08 are complete under [the guarded result](../results/P0.json), bound to published revision `8fe7c4bcbe8511ac8772c043ff023b15075ad823`. P1 is now active; this history preserves the foundation scope and limits.

## Verified locally

- `npm run check` passed on 2026-09-05: strict TypeScript, ESLint, Prettier, 26 Python foundation tests, 17 TypeScript tests, 26 schema/example pairs, 20 reference images and 10 negative quality-contract checks. Publication review also added regression tests for staged-content auditing, portable source integrity, immutable evidence, cancellation at publication, and exclusive inspector/drawer state.
- Exact unchanged and edited canonical FFV1/PCM round trips passed for BGRA/s16, gbrp16/f32, yuv444p10/s24, yuv444p16/s32 and yuva444p16/f64. Source hashes remained unchanged; proxy, unsupported precision/HDR and failed equality paths reject promotion.
- The bounded encoder accepts canonical raw files up to 64 MiB each, BT.709 and mono/stereo. Rational frame rate and sample counts are verified; exact container packet timing, production compositing and capture throughput are not.
- The installed Codex 0.142.3 metadata generator resolved seven required request methods and managed ChatGPT login. Generated schemas and hashes remain private local evidence. This did not start a server session, sign in, or invoke a model.
- The authenticated GitHub owner is klimPaskov. The public repository `klimPaskov/codex-video-edit` was created and its ownership/visibility verified. The reviewed foundation was published as `fc7ce9301c3007e632cfcb5f277b44f3e4f0e70a`; `git ls-remote origin refs/heads/main` returned the same identity. [Foundation CI run](https://github.com/klimPaskov/codex-video-edit/actions/runs/33924115195) tracks Windows and Linux headless checks, separately from native acceptance.
- The user-supplied example exists. Its location, byte length and original hash are recorded only in ignored `.astra/private/user-example.json`. It has not yet been imported, edited or exported through the application.

The first public CI run passed on Linux and failed Windows formatting because JavaScript/TypeScript files checked out as CRLF. Explicit LF attributes now cover those source files. Corrective [PR #1](https://github.com/klimPaskov/codex-video-edit/pull/1) merged as `a46c4a54fd7a1e3e14dc4d71211235093b73886e`. [Main CI run](https://github.com/klimPaskov/codex-video-edit/actions/runs/33924657687) passed on Windows and Linux. This source checkout correction is separate from media/native acceptance.

## Isolated desktop infrastructure

Docker 28.5.2 is now running. A dedicated container provides an unprivileged Linux desktop with no host mounts or devices, dropped capabilities, restricted seccomp, and authenticated loopback-only VNC. The Electron 44.2 / Playwright 1.63.0 compatibility probe passed with Chromium sandboxing enabled; a fresh `probe-setup` followed by `probe` passed again. Computer use inspected native Xmessage and Electron windows through TigerVNC and confirmed input with visible responses. A user Escape interrupted the later computer-use close step, so no clean computer-use close is claimed. Private native evidence stays out of Git.

This infrastructure slice covers P0-04 and P0-07 without completing them. The current user instruction authorizes provisioning and the native viewer on the host; the product itself must build and run only inside the guest. The reviewed infrastructure merged through [PR #2](https://github.com/klimPaskov/codex-video-edit/pull/2) at `e37102ed289bf476d61f9f7d4a5f30b558346ec3` with Windows/Linux CI passing. The compatibility probe is not an editor implementation. Linux display/input evidence cannot prove Windows capture or installer behavior, and VNC does not provide audio listening or high-precision master review.

## Actual native media bootstrap

ADR 0012 brings the minimum actual shell, typed IPC, immutable media import and frame transport into P0-04/P0-06 so P0 can satisfy its actual-product native gate without treating the infrastructure probe as an editor. The actual packaged Electron 44.2 product passed isolated Linux tests for its secure local custom protocol, validated IPC and foreign-window denial, immutable synthetic FFV1/PCM import, exact native-size two-frame transport/seek, and library reopen. Linux renderer `/proc` evidence confirmed sandboxing. Separate master verification confirmed decoded canonical sample equality. Computer use through native TigerVNC selected the source card and pressed Next frame, visibly changing red at zero to green at 0.500 seconds. These are product observations, distinct from the earlier probe. A stronger same-runtime check compared complete opaque nonuniform frame buffers across seek and reopen using distinct source frames. Arbitrary alpha and display-compositing fidelity remain unverified. Private artifacts remain private. Reviewed source merged through [PR #3](https://github.com/klimPaskov/codex-video-edit/pull/3) at `8fe7c4bcbe8511ac8772c043ff023b15075ad823` after Windows/Linux [push checks](https://github.com/klimPaskov/codex-video-edit/actions/runs/33952499182) and [PR checks](https://github.com/klimPaskov/codex-video-edit/actions/runs/33952514882) passed.

The append-only managed library is distinct from projects and drafts. Its initial frame path supports native-dimension BGRA full-range GBR BT.709 SDR only within explicit size limits; other supported imports remain preserved with preview explicitly unavailable. Frame inspection/seek introduces no resampling or lossy proxy, but it is not audio playback, editing, or the final master-export workflow. The full later-phase and user-video workflow remains required.

## Remaining acceptance

The bounded actual bootstrap has package, Playwright Electron and computer-use evidence. Its source publication and P0 acceptance audit passed; the guarded result records all eight P0 tasks. Audio listening, Windows capture/device/installer behavior, capture fixtures, real signed-in Codex editing, complete project/draft/review/export workflows, and the user-example acceptance run remain unverified. These remain later-phase requirements and are not accepted by the P0 result.

## Spec sync

Changed: five-stage workflow and separate Edit/Review contracts; authorized reversible tool confirmation policy; phase-result evidence gate and example; development validation/publication scripts and CI; current dependency/protocol research; related ADR, docs, tests, skills, prompts, routing and traceability.

The desktop infrastructure slice also updates native test provisioning and probe instructions, isolation checks, the narrow host infrastructure exception in ADR 0006 and testing guidance, the native-testing skill, active phase, and traceability. It replaces stale unavailable statements with observed scope-limited evidence. The guarded P0 result now records foundation acceptance without completing later phases.

The actual native bootstrap adds ADR 0012 and synchronizes P0 tasks/phase prompt, architecture and screen scope, native-testing skill, traceability and active phase. The project schema's `workflow_step` now includes `edit` instead of `qa`, with a regression check, matching the five-stage workflow. Historical project values need the same migration as UI state: old editor `review` becomes `edit`, and old quality-review `qa` becomes `review`. A future project loader must apply this mapping; no loader or runtime migration exists yet. Draft/timeline operations and AI routes remain unchanged. Product native evidence is recorded separately from the probe.

Intentionally unchanged: immutable source and timeline operation schemas; reference image bytes and provenance manifest; capture and production master requirements; explicit user control over final export, deletion, cleanup, spending and publication. Headless encoder checks do not waive native or final media acceptance.
