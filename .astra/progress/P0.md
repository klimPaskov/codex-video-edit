# P0 implementation progress

Status: partial. P0-01 through P0-08 remain the active phase scope. This is a progress record, not an accepted phase result.

## Verified locally

- `npm run check` passed on 2026-09-05: strict TypeScript, ESLint, Prettier, Python foundation tests, media/process tests, 24 schema/example pairs, 20 reference images and 10 negative quality-contract checks. Publication review also added regression tests for staged-content auditing, portable source integrity, immutable evidence, cancellation at publication, and exclusive inspector/drawer state.
- Exact unchanged and edited canonical FFV1/PCM round trips passed for BGRA/s16, gbrp16/f32, yuv444p10/s24, yuv444p16/s32 and yuva444p16/f64. Source hashes remained unchanged; proxy, unsupported precision/HDR and failed equality paths reject promotion.
- The bounded encoder accepts canonical raw files up to 64 MiB each, BT.709 and mono/stereo. Rational frame rate and sample counts are verified; exact container packet timing, production compositing and capture throughput are not.
- The installed Codex 0.142.3 metadata generator resolved seven required request methods and managed ChatGPT login. Generated schemas and hashes remain private local evidence. This did not start a server session, sign in, or invoke a model.
- The authenticated GitHub owner is klimPaskov. The public repository `klimPaskov/codex-video-edit` was created and its ownership/visibility verified. The reviewed foundation was published as `fc7ce9301c3007e632cfcb5f277b44f3e4f0e70a`; `git ls-remote origin refs/heads/main` returned the same identity. [Foundation CI run](https://github.com/klimPaskov/codex-video-edit/actions/runs/33924115195) tracks Windows and Linux headless checks, separately from native acceptance.
- The user-supplied example exists. Its location, byte length and original hash are recorded only in ignored `.astra/private/user-example.json`. It has not yet been imported, edited or exported through the application.

The first public CI run passed on Linux and failed Windows formatting because JavaScript/TypeScript files checked out as CRLF. Explicit LF attributes now cover those source files. Corrective [PR #1](https://github.com/klimPaskov/codex-video-edit/pull/1) merged as `a46c4a54fd7a1e3e14dc4d71211235093b73886e`. [Main CI run](https://github.com/klimPaskov/codex-video-edit/actions/runs/33924657687) passed on Windows and Linux. This source checkout correction is separate from media/native acceptance.

## Isolated desktop infrastructure

Docker 28.5.2 is now running. A dedicated container provides an unprivileged Linux desktop with no host mounts or devices, dropped capabilities, restricted seccomp, and authenticated loopback-only VNC. The Electron 44.2 / Playwright 1.63.0 compatibility probe passed with Chromium sandboxing enabled; a fresh `probe-setup` followed by `probe` passed again. Computer use inspected native Xmessage and Electron windows through TigerVNC and confirmed input with visible responses. A user Escape interrupted the later computer-use close step, so no clean computer-use close is claimed. Private native evidence stays out of Git.

This infrastructure slice covers P0-04 and P0-07 without completing them. The current user instruction authorizes provisioning and the native viewer on the host; the product itself must build and run only inside the guest. The setup changes are uncommitted pending tests, integration review, and publication. The compatibility probe is not an editor implementation. Linux display/input evidence cannot prove Windows capture or installer behavior, and VNC does not provide audio listening or high-precision master review.

## Remaining acceptance

No product Electron app has been built or launched. The isolated desktop and compatibility probe remove the previous infrastructure blocker. Product Playwright Electron and computer-use acceptance, capture fixtures, real signed-in Codex editing, application import/reopen/review/export, and the full user-example acceptance run remain unverified. No `.astra/results/P0.json` is written while acceptance is incomplete.

## Spec sync

Changed: five-stage workflow and separate Edit/Review contracts; authorized reversible tool confirmation policy; phase-result evidence gate and example; development validation/publication scripts and CI; current dependency/protocol research; related ADR, docs, tests, skills, prompts, routing and traceability.

The desktop infrastructure slice also updates native test provisioning and probe instructions, isolation checks, the narrow host infrastructure exception in ADR 0006 and testing guidance, the native-testing skill, active phase, and traceability. It replaces stale unavailable statements with observed scope-limited evidence. P0 remains incomplete; no task or phase result is marked accepted.

Intentionally unchanged: immutable source and timeline operation schemas; reference image bytes and provenance manifest; capture and production master requirements; explicit user control over final export, deletion, cleanup, spending and publication. Headless encoder checks do not waive native or final media acceptance.
