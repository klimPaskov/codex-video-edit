# Isolated native desktop

This P0 test environment runs Linux native windows inside Docker. All native input and screenshots now stay inside Docker under the latest user instruction. Never control the host PC or launch Electron directly on it. This environment is not Windows capture, installer, audio-listening or product acceptance evidence.

## Provision

With Docker's Linux engine running:

```sh
python scripts/desktop_environment.py build
python scripts/desktop_environment.py start
python scripts/desktop_environment.py check
```

The launcher creates `codex-video-edit-desktop`. It fails on an existing name instead of deleting it. The container has a private display, PID/IPC namespaces and shared memory, runs as UID 1000, drops capabilities, prevents privilege escalation, and mounts no host files, sockets or devices. The reviewed [seccomp profile](SECCOMP.md) permits Chromium's nested sandbox. Do not replace it with privileged mode or disable the Chromium sandbox.

The build context is only this directory; `.dockerignore` includes only the Dockerfile and display entry point. Private evidence and user footage must never enter a build context. Stop a test session with `docker stop codex-video-edit-desktop`; retain failed environments/evidence until cleanup is explicitly authorized.

## Restart recovery

The display startup script recovers only display 99's stale lock and socket inside the private container namespace. It refuses recovery when the recorded process is alive or the display responds. An abrupt stop/start of the empty test container must pass `python scripts/desktop_environment.py check` again before product testing. The empty-guest abrupt kill/start recovery test passed, with the previous container retained and source evidence private. Retain failed containers and evidence; rebuilding the image does not update an existing container's startup script.

## Guest-only native observation and input

The current visual testing route is `tests/desktop/guest-input.py`, copied as reviewed source into the guest and executed there with Python. It uses the guest's existing X11/XTEST libraries via ctypes for native input and FFmpeg x11grab for screenshots. Runtime guards require Linux, `/.dockerenv`, UID 1000, `DISPLAY=:99`, and the expected 1440×900 guest display. It neither controls nor captures the host desktop.

Actions are `capture`, `click <x> <y>`, and `key <permitted-key>`. Inspect the most recent guest screenshot before choosing coordinates. The permitted keys are Tab, Shift_L+Tab, Escape, Return, Up, Down, Home, End and Control_L+comma. Each action produces a fresh private screenshot and JSON record under `/home/node/evidence/visual/` with the action, display, hash and `hostInput: false`. Inspect the result before taking another action; a successful command alone is not a visual pass.

The current path captured the guest desktop, closed its probe, opened an actual product source, opened Settings with Ctrl+,, selected/saved 200%, opened Source details and advanced the actual frame to 0:00.500. Native 200% tests passed separately. This does not establish audio listening, full accessibility, arbitrary-alpha/display precision, or Windows acceptance.

Earlier tests used an authenticated loopback TigerVNC viewer. That is historical evidence only. The user's later stop interrupted the host-viewer 200% attempt; it is not a pass. Do not launch or operate a host viewer unless the user explicitly requests it again. Retain private viewer files and old evidence without using them as current host-control authorization. The existing VNC endpoint must remain loopback-only and must not be exposed to the network.

## Sandboxed Electron compatibility

The test image also installs Orca, AT-SPI and the Python accessibility bindings. These are test dependencies only. Run the Orca harness in one private `dbus-run-session` so Electron, the screen reader and observer share the session bus. Disable speech and physical braille while checking actual braille-monitor output. Do not connect host audio, displays, devices or accessibility services. Preserve the previous container and its evidence when changing the image.

New guest-only environments publish no host ports. The validator also recognizes the previously reviewed loopback VNC binding for retained historical containers, but no viewer is launched or controlled. Multiple isolated guests can therefore be tested without touching the host desktop or sharing displays.

### Codex runtime bootstrap

For the P2 bootstrap, install the reviewed `@openai/codex@0.155.1` package into a dedicated private guest prefix with `npm install --ignore-scripts --save-exact --prefix <guest-prefix> @openai/codex@0.155.1`. Verify package integrity, the native executable and same-version code-mode host; retain their hashes and generated protocol outputs. Do not copy host account directories or runtime configuration; the only credential-transfer exception is the explicit session authorization in ADR 0006 below. `tests/native/codex-runtime.test.ts <absolute-native-codex-executable>` requires the isolated guest and creates fresh private account/context directories. It tests two real stdio connections, signed-out account state, local fixture-skill discovery, model sign-in gating and close/reconnect exclusion. It records source/runtime provenance before startup. This test does not launch Electron, authenticate, invoke a model, or edit media and cannot satisfy those phase gates.

For this session only, the user explicitly authorized copying the official runtime credential file directly into the private app-owned guest account with mode 600. The host file was unchanged at transfer; no mounts, configuration, history or secret output were used. Keep this private test setup separate from the signed-out harness and from production managed sign-in. No renderer token input or general credential-import feature is introduced. The packaged probe reports connected/signed_in and runtime models, skills and usage, not browser-login completion.

The private synthetic authenticated native flow now passes in packaged Electron with the real signed-in Codex runtime. Run `node tests/native/codex-authenticated.test.ts <packaged-executable> <private-config-root>` only in the isolated guest. The passing run discovered one model and five skills, persisted a runtime-validated selection, committed one Codex-origin start trim at sequence 1 (1.5 to 1.0 seconds), and observed committed controls and exact retained pixels before the turn finished. The same actual thread/history reopened; a real Codex undo appended sequence 2 and restored the 1.5-second draft. Source/baseline bytes and the journal verified. A read-only turn was stopped while running without a journal change and reopened on the same thread. Guest-only capture separately verified the packaged drawer and its switch to the Source inspector after a guest click, with no host input. Keep these result, provenance and screenshot hashes private. Earlier failed native attempts remain failed evidence.

For the guarded AI range cut, run `node tests/native/codex-range-cut.test.ts <packaged-executable> <private-config-root> [<video-1> <video-2>] [--inspect]` only in the isolated guest. Without video arguments it creates two synthetic one-second sources and asks real signed-in Codex to cut a range spanning their join; it verifies a committed `ripple_delete` while the turn runs, exact surviving join pixels, manual shared Undo, reopen and immutable inputs. The synthetic fixture passed first. With the two supplied videos privately staged in the guest, the same packaged path passed a two-second cross-source cut, exact decoded join frame, Undo/reopen and original/managed source and baseline integrity; `--inspect` enabled guest-only visual inspection. The private draft is restored after Undo. An initial private attempt exhausted the Node harness heap by transferring full-resolution canvas arrays, and another asserted before the reopened 4K seek settled; both remain failed attempts. The corrected harness hashes pixels inside the secure renderer and waits for seek and frame readiness. Neither test proves a meaning-preserving editorial cut, continuous A/V playback, Magic Wand or export. Keep private input paths, account state, model text, media hashes and screenshots out of Git.

For guarded AI split, run `node tests/native/codex-split.test.ts <packaged-executable> <private-config-root> [--inspect]` only in the isolated guest. It creates two synthetic one-second sources, imports them through the packaged app, and asks real signed-in Codex for `cut.split` at 500,000 output microseconds in the first clip. The passing run observed a committed three-fragment map during the turn, exact decoded start/split/source-join pixels, manual newest Undo, reopen and unchanged original/managed source and baseline bytes. Guest-only computer-use capture visually inspected both the live Codex drawer and reopened Edit window. The first run failed a harness assertion before asynchronous Undo settled; the corrected wait-based rerun passed. This synthetic split does not establish speech-edit judgment, paid API editing, or master export. Keep its guest account, journal, hashes and screenshots ignored and private.

For the current mixed Codex route, run `node tests/native/codex-authenticated.test.ts <packaged-executable> <fresh-private-config-root> --require-luna-high --require-dynamic --inspect` in the isolated guest. New project bindings must be `dynamic`; the real Luna/high turn must commit through the seven guarded host-defined tools and pass trim/Undo/reopen/Stop with exact preview and immutable source/baseline. Use `tests/desktop/guest-input.py capture` only on the guest X11 display while the native inspection hold is active, then create its `inspection.done` marker. The same package passed `codex-thread-route-migration.test.ts` against a pre-existing MCP project and `codex-forbidden-policy.test.ts` against a new dynamic project. The first legacy attempt copied a config to a new root and failed because the baseline stores absolute paths; use the original guest config with a backed-up registry and restore it afterward. Keep account state, project IDs, screenshots and bounded result files private. One negative command turn does not establish complete tool unavailability.

For dynamic live stale rejection, pass the resulting private authenticated fixture evidence, its original guest config root and that packaged executable to `node tests/native/codex-stale-committed.test.ts <evidence> <config-root> <packaged-executable>`. The dynamic fixture has a committed Codex trim and committed Codex Undo; the test sends the earlier head through one real host `cut_trim_edge` call, inspects its unsuccessful authoritative item and checks both journal files, current draft, source and baseline remain unchanged. The earlier one-edit MCP fixture remains supported. Keep private result files and screenshots ignored.

`tests/native/codex-authenticated-settings.test.ts <packaged-executable> <private-config-root> [--inspect]` checks signed-in packaged Settings twice across restart. It compares visible plan, model, usage/reset and skill labels to the live bounded IPC state, then supports guest-only visual input while the native modal remains open. A passing run does not prove managed browser-login completion or future account limits. Keep account screenshots and usage evidence private.

The signed-in trim/undo flow does not prove managed browser-login completion or complete absence of unapproved built-in tools. `tests/native/codex-native-subagent.test.ts <packaged-executable> <private-config-root> [--inspect]` exercises the packaged dynamic Codex route and requires one completed server-owned parent spawn and wait, receiver ID correlation through `thread/read`, completed child-owned project and timeline summary reads, inherited read-only/never-approval policy, and unchanged source, baseline and journal. On 0.155.1 the parent spawn starts with no receiver ID; its completed item carries the child ID. The client buffers an untrusted early child-turn candidate and authorizes it only after a validated server-owned spawn event supplies the matching receiver; a bare child start broadcast is insufficient. Child turn/message notifications stay out of the parent projection. The test reads bounded authoritative turn history and private parent/child rollout metadata; a model claim or generic drawer activity is not evidence. `--inspect` holds the real native window for guest-only capture; retain the screenshot inside the isolated guest and review it there. The current synthetic pass's guest screenshot was visually reviewed and showed the actual Electron window with a single Codex drawer and synthetic preview. User-supplied media, audio listening, Windows hardware/capture/installer and export remain outside this child run. Keep credentials, account identifiers, private locations, transcripts and raw turns out of public evidence.

`tests/native/codex-forbidden-policy.test.ts <packaged-executable> <private-config-root> [--inspect]` is a bounded authenticated command-policy probe, not a complete tool-catalog introspection. Select a live Codex model in packaged Settings before opening a conversation. A fresh rerun passed with a ready native thread, completed saved turn, no command/file/dynamic or unrequested tool item, and unchanged synthetic project bytes. It also correlated the exact turn to a bounded account-confined private rollout and found zero raw tool-search, function or other call/unknown response items; persisted `thread/turns/list` alone cannot reveal all of these. The separate audit resumes with the production fixed policy; a bare audit resume was an invalid comparison because it reapplied defaults. The model's statement that shell was prohibited or unavailable is classified as a claim. The test records `effectiveToolUnavailabilityProven: false`; do not mark the full P2 restriction gate complete from it. The two setup failures before the passing rerun and all guest-only visual captures remain private.

For remembered API-model acceptance, first build the ordinary `codex-video-edit-desktop:p0` image, then build `tests/desktop/keyring.Dockerfile` as `codex-video-edit-desktop:p2-keyring`. Create a separate guest using `scripts.desktop_environment.start_arguments()` with only name/image changed and validate its inspection; retain UID 1000, restricted seccomp, dropped capabilities, no mounts/devices/ports and guest display. Transfer a reviewed Git source archive, run guest `npm ci`, and package Electron there. Transfer an already authorized OpenAI API test credential only through stdin into a mode-600 file under the guest's private `test-results/`. Run `dbus-run-session -- sh tests/desktop/run-keyring-model.sh <packaged-executable> <private-key-file> [--inspect]` in the guest. The wrapper starts a test-owned GNOME Secret Service with a private random password; the native test checks real `gnome_libsecret`, live model discovery, encrypted key/model bytes, persisted selection on packaged reopen, empty key field and explicit removal. The private key input is unlinked before the app opens; the protected key file is removed in the test or failure cleanup. `--inspect` holds the reopened native window for `guest-input.py capture` and visual review. This path makes no paid completion and does not exercise DeepSeek. Keep the keyring files, screenshots, catalog details and credential outside Git.

`node tests/native/api-provider-draft-fixture.test.ts <packaged-executable> [--deepseek|--gemini] [--inspect]` runs in the ordinary isolated guest with no real API key. Without a provider flag it tests OpenAI API; the flags select DeepSeek or Gemini API. The deterministic `fetch` replacement lives in Electron main only after packaged startup and accepts only the selected provider's fixed model/completion destinations and a synthetic key, so no external completion or charge occurs. Each run imports a generated FFV1/PCM project through the native UI and tests committed live trim/preview, stale rejection, shared undo, redacted provider failure and reopen. The Gemini 3 synthetic loop also checks exact transient thought-signature replay and no persisted signature. `--inspect` holds the trimmed native window for `guest-input.py capture` and review. Keep screenshots and all run results ignored/private; earlier timing failures do not become passes. This is synthetic protocol evidence, not authenticated provider acceptance.

Generate `app-server generate-ts --experimental` and `app-server generate-json-schema --experimental` using that same pinned executable in the guest. Transfer only the generated text to private evidence, then normalize the reviewed TypeScript dependency closure with `scripts/sync_codex_types.py`; `--check` verifies all 207 retained files against their original pinned hashes. Request builders compile against the generated inputs, and response decoders enforce the supported security-sensitive fields. Preserve the complete generated output privately so later protocol changes can be compared without publishing machine-local evidence.

`npm run check:protocol` also generates the pinned experimental JSON schema and requires definitions for every consumed account, catalog, thread-history and turn request, including `thread/resume` and `thread/turns/list`. It is a metadata check only; it does not start app-server, read an account or prove runtime acceptance.

### Pinned Orca comparison

The Debian reader remains available for reproducing its failures. Build a separate dependency image for the reviewed Orca 50.2/AT-SPI2 2.56.8 comparison:

```sh
docker build -t codex-video-edit-desktop:p1-accessibility tests/desktop
docker build -f tests/desktop/orca50.Dockerfile -t codex-video-edit-desktop:p1-orca50-build tests/desktop
```

Create a distinct guest using `scripts.desktop_environment.start_arguments()`, changing only its name and image, and validate the resulting inspection with `validate_inspection()`. Preserve existing guests. Do not add mounts, devices, published ports or privileges. Transfer the reviewed provisioner and test sources with the same allowlisted source-transfer boundary as the product tests.

Inside that guest, run `python3 tests/desktop/provision-orca50.py`. It verifies pinned upstream archive hashes and extracts, builds and installs into a fresh invocation directory. It retains previous runs and prints the new prefix. Installation is separate from product or accessibility acceptance.

In the guest test workspace, set `ORCA_TEST_PREFIX` to that emitted prefix and `PACKAGED_EXECUTABLE` to the absolute reviewed package path, then run:

```sh
env ORCA_EXECUTABLE="$ORCA_TEST_PREFIX/bin/orca" \
  LD_LIBRARY_PATH="$ORCA_TEST_PREFIX/lib" \
  GI_TYPELIB_PATH="$ORCA_TEST_PREFIX/lib/girepository-1.0" \
  XDG_DATA_DIRS="$ORCA_TEST_PREFIX/share:/usr/local/share:/usr/share" \
  dbus-run-session -- node tests/native/orca-smoke.test.ts "$PACKAGED_EXECUTABLE"
```

The harness accepts only reviewed reader versions. Orca 50.2 uses its live D-Bus service for readiness and runtime setting readback; speech and physical braille must be off and the monitor on before app testing. All seven control checks still require actual reader output and matching application-owned focus events. Record package, reader and test provenance before startup. A monitor screenshot may show a subsequent reader mode announcement; inspect it and do not mislabel it as the earlier control announcement.

Copy only the probe source, install its pinned test dependencies inside the container, and run:

```sh
python scripts/desktop_environment.py probe-setup
python scripts/desktop_environment.py probe
```

The setup transfers an allowlist of probe files through tar stdin as UID 1000. It needs no root ownership changes or host mounts. Electron 44's npm package exposes an explicit binary installer; installing the package alone did not provide the executable in this environment.

The probe requires Docker, UID 1000 and the isolated display. Playwright explicitly sets `chromiumSandbox: true`; its Linux default disables sandboxing. Assertions check local file loading, isolated preload, sandbox state, lack of renderer Node access, input response and launch arguments. Evidence remains under the container's `/home/node/evidence/`. Inspect Electron visually as well. This compatibility probe is not a product mockup and cannot complete application acceptance.

Source transfer for later application tests must use an explicit reviewed Git archive or selected files. Never mount the repository root, user home, Docker socket, authentication directories or host display sockets. The supplied video may be copied separately into a private project only when the relevant fixture tests pass.

### Packaged Codex account/settings slice

The guest desktop image includes Chromium and `xdg-utils` as an unprivileged guest-local browser handler for managed sign-in opener tests. Keep the existing container isolation flags, no mounts or host ports, and preserve an authenticated guest when making a separate browser-test container.

For the real opener path, run `node tests/native/codex-login-opener.test.ts <packaged-executable> [--inspect]` in that separate browser-equipped isolated guest with a fresh signed-out account. It checks the packaged window, guest Chromium start on the validated OpenAI authorization route, cancellation and signed-out return. `--inspect` holds before cancellation for `guest-input.py capture` and visual review of the guest-only screenshot. A bounded run showed the loaded OpenAI login page over the native app and exited its test-owned browser/opener cleanly. This proves opening and cancellation, not managed-login completion. Keep OAuth URLs, codes, browser profiles and screenshots private; use no host browser or port forwarding.

For the alternative official device-code route, run `node tests/native/codex-device-login.test.ts <packaged-executable> [--inspect]` in the same browser-equipped guest with a fresh signed-out account. The ordinary run checks that the packaged Settings action displays a transient code and fixed verification page, the page opens in guest Chromium, and Cancel sign-in returns to signed out, clears the code and cleans up test-owned browser processes. An inspected live run also observed the user finish verification externally: the same native app reconciled to signed in and cleared the code. A separate packaged reopen with that private app-owned account confirmed authoritative signed-in account, plan, runtime models and skills. The inspected test's cancellation assertion failed when sign-in completed before cancellation; retain that failure rather than relabel it as a cancel pass. Managed device-code completion is verified for this bounded runtime; browser OAuth callback completion remains unverified. Keep one-time codes, account data, browser profiles and screenshots private. Do not open the product or browser on the user's host.

The guest desktop build now requires the locked official Codex platform package installed by guest `npm ci`. It copies the reviewed Linux x64 `vendor/x86_64-unknown-linux-musl/bin/codex` into packaged resources/codex outside ASAR, together with its Apache licence and version/hash manifest. No build or runtime launch is permitted on the user host. The main resolver has no environment-selected executable fallback; test absent/tampered resources as actionable failures.

The protocol closure now includes 207 generated types covering account/settings, the consumed experimental thread and turn lifecycle, bounded recent-turn history, streamed items, server approvals, and owned MCP status. Unit tests require inline history or exactly one newest-first `thread/turns/list` fallback, strict page and item bounds, chronological redacted projection, active-turn restoration, interrupt continuity, activity-state agreement, and quarantine of forbidden history. Appearance/Codex settings sections and real client actions passed packaged signed-out native checks with guest-only visual inspection at 100% and 200%. Run `node tests/native/codex-settings.test.ts <packaged-executable> [--inspect]` in the guest; the inspection flag retains the native window until the private inspection.done signal or its deadline. This test suppresses external browser launching explicitly while exercising the actual official login-start/cancel methods. The separate `tests/native/desktop.test.ts` opens the compact project Codex drawer and verifies its signed-out error without inserting a sample conversation. A signed-out browser-login start/cancel test can validate runtime URL handling and cancellation without authenticating. Keep the URL, login IDs, codes and raw errors out of screenshots and evidence; record bounded outcomes only. Never treat that test as managed-login success, authenticated catalog discovery, persisted thread history, a model turn, or an edit. Browser opening for native tests must target only a browser inside the same isolated guest; no host viewer or port forwarding. The credential seed exception above does not authorize other host access.

The packaged build carries the reviewed MCP child outside ASAR with a size/SHA manifest. Main starts a token-authenticated local broker and configures the app-server with exactly the seven owned tools. Signed-out `mcpServerStatus/list` validation must confirm the expected server name/version, exact tool names and input schemas, and empty resources/templates before project conversations are enabled. Unit tests exercise the child protocol, broker authentication and bounds, fixed error mapping, deep POSIX application-data paths and package tamper rejection. These signed-out checks alone do not establish authenticated tool use, built-in-tool absence under a live model, or native subagent inheritance; separate signed-in fixture tests cover bounded tool use and one native child read path.

The earlier product native suite also closed the packaged app, committed a real fixture trim through the production draft transaction store, and reopened the visible project. It verified the shortened project duration and seek range, mapped output time zero to the retained source offset, then opened the same Source library item at source time zero. That run proved packaged reopen and project-frame mapping for the supported single-clip reducer. The later authenticated native flow above separately passed live event delivery from a real Codex tool.

### Live API-provider edit gate

`tests/native/api-provider-authenticated-edit.test.ts` exercises a user-started provider turn in packaged Electron on a synthetic FFV1/PCM project. Run it only in the UID-1000, DISPLAY `:99` isolated guest after placing an authorized key in `test-results/private-openai.key`, `test-results/private-deepseek.key`, or `test-results/private-gemini.key` there with mode 600. Invoke `node tests/native/api-provider-authenticated-edit.test.ts <packaged-executable> <absolute-guest-test-results/private-openai.key>` for OpenAI, or use the matching private path and append `--deepseek` or `--gemini`. The harness reads and unlinks that file before launching the product, selects a live-supported model through Settings, clicks Send, checks the shared journal and exact rendered frame, reopens the project, and records only HTTP status codes for any Chat Completions response in ignored private evidence. The Gemini route requires a reviewed Gemini 3 model and records only signature counts and exact replay agreement, never signature bytes. It can make a billed call. Never pass a key as an argument, commit it, mount host credentials or use the host desktop. A run that returns HTTP 429 without a draft transaction fails this acceptance gate; model-list success alone is insufficient. Two live OpenAI attempts returned 429, so pause paid retries until account state changes. One user-authorized live DeepSeek fixture turn passed guarded trim, native preview and reopen; no Gemini key has been supplied. The synthetic packaged provider fixture separately checks redacted failure and shared undo behavior without external completion calls.

After the live edit and first reopen, the harness enters Edit, applies manual Undo through the shared journal, waits for exact restored pixels and duration, and reopens again. A complete authorized DeepSeek run passed that whole path in packaged isolated Electron; its preceding attempt failed at the final reopen because a zero-sized decoding canvas threw before the pixel wait could retry. The harness now treats zero dimensions as a retryable assertion and waits for decoded pixels at each reopen. Keep both runs and the earlier keyless continuation separate. A screenshot captured before decode can still show a transient Reading frame message.

The deterministic native fixture also accepts `--compact` to resize the actual packaged window to 1366×768 before import. Its screenshot probe scans the preview region for the retained synthetic green frame, rather than relying on one absolute screen pixel after resizing. Both provider routes check that recovery text fits the drawer and remains in the viewport; the compact OpenAI run additionally checks a usable conversation scroll area. Guest-only screenshots and full results stay ignored/private. A first compact run failed because the older fixed screenshot pixel landed outside the resized preview; its failure remains separate from the passing revised run.

For the experimental host-defined alternative, run `node tests/native/codex-dynamic-tools-probe.test.ts <absolute-guest-packaged-resources/codex/codex> <absolute-guest-test-results/private-profile-root>` only in the UID-1000 isolated guest. The private root must contain a mode-600 `account/auth.json` copied under the user's existing authorization and an empty `context/`; never pass or print credential bytes, mount the host account, or commit that root. The harness starts the official pinned App Server without MCP, checks the live Luna/high catalog, registers one namespaced read-only dynamic tool, validates the single correlated host request, audits a bounded `ALL_TOOLS` inventory and the matching private rollout code cell, and writes counts only to ignored evidence. Earlier plain-function and bare-name mismatch attempts were failures. This is a direct App Server protocol probe, not Playwright Electron, a guarded draft edit, existing-thread migration, or P2-07 acceptance.

The adjacent `codex-dynamic-tools-resume.test.ts` uses the same private-root setup and official binary but requires a fresh root for each run. It calls one host-defined tool, closes App Server, resumes that exact thread in a new process, and requires a second owned call with no foreign host request. It writes bounded counts only under the ignored root. It does not exercise Electron, legacy MCP-backed threads or full history projection.

For registry compatibility, first run `codex-authenticated.test.ts` on a fresh isolated packaged synthetic project with Luna/high and inspect the actual guest window. `codex-thread-route-migration.test.ts <packaged-executable> <private-config-root>` then requires exactly one existing v2 MCP binding, retains its private bytes, rewrites only that guest fixture to v1, reopens the same packaged conversation, requires an owned read-only turn, and verifies the registry stays byte-identical. A later packaged project conversation should write v2 with both MCP bindings and the original thread hash. The tested full MCP fixture passed; the v1 reopen/read and later ID preservation passed. A second full authenticated fixture failed after a correct trim at a post-turn assertion, so do not count its entire path. Keep private registry, media, account, journal and screenshots ignored; this does not prove dynamic-tool draft editing.

`codex-dynamic-tools-timing.test.ts` is a direct read-only App Server probe of the optional six-tool adapter. Run it in a fresh UID-1000 no-mount guest root with `account/auth.json` mode 600 and empty `context/`, passing the absolute official 0.155.1 native executable whose matching `codex-code-mode-host` is present beside it. It requires a validated `turn/start` response before one correlated owned summary call, zero early/foreign/mismatched calls, and a completed turn. An older package without the host produced two no-call failures; retain those separately. Keep result files and rollout private. This does not establish universal request ordering, packaged Electron behavior, or MCP-thread migration.

`codex-dynamic-client-correlation.test.ts` uses the same fresh private-root shape and pinned executable but exercises `CodexClient` and its project-thread registry without MCP. It requires live Luna/high discovery, a dynamic binding, exactly one owned summary read and a completed turn, and writes only counts under the private root. The first run printed a pass but an uncleared test timeout kept Node open; the corrected rerun exited successfully. Unit tests cover calls before the RPC response after a validated owned start notification and reject unknown, terminal or contradictory turns. This direct test does not establish that an early host call occurred, launch packaged Electron, mutate the draft or prove effective full tool confinement.

Use `--scale-200` for a separate packaged OpenAI run that saves the real 200% interface setting before the synthetic turn. The fixture scrolls the native preview into view for frame capture and the drawer scrolls to the changed recovery message. At this scale the preview and entire conversation do not fit simultaneously in the test window, so inspect each visible state separately. An initial run failed because the conversation history collapsed below the 80 px usability gate; the corrected minimum-height scroll area and full native rerun passed. Keep both outcomes distinct.

For dynamic split and bounded nested inventory, run `node tests/native/codex-split.test.ts <packaged-executable> <private-config-root> [<guest-video-1> <guest-video-2>] --require-luna --require-dynamic --probe-surface [--hostile-app-config] [--inspect]` only in the isolated guest. The dynamic route requires seven guarded editor tools, five supported `multi_agent_v1` functions, and zero connected-app or other nested tools. The fixture verifies the code-mode `custom_tool_call` and its correlated output in the exact account-contained rollout; the assistant's final text alone is not inventory evidence. `--hostile-app-config` seeds `[apps._default].enabled = true` and `[apps.adobe].enabled = true` in the fresh app-owned Codex home. The synthetic baseline and hostile-config runs passed the live surface check, committed split during the turn, exact split/join frames, shared Undo, reopen, and immutable inputs. Earlier six-tool and no-agent inventory counts describe previous route/catalog revisions. Keep all rollout data, media, account files, answers and screenshots inside the guest and out of Git. This is a bounded turn result, not a permanent effective allowlist.

The current catalog adds `cut.delete_ranges` as a seventh guarded tool. The split inventory fixture now expects seven owned nested names; its preceding six-tool pass remains historical. Run `node tests/native/codex-range-cut.test.ts <packaged-executable> <private-config-root> [<guest-video-1> <guest-video-2>] --batch [--inspect]` to check a real Luna/high dynamic host call with two disjoint descending ranges and one committed transaction. Synthetic and supplied-footage runs passed edited-join preview, manual Undo, reopen, immutable inputs and guest-only inspection of the supplied native window. This does not certify the audible join or automatic speech selection.

The range-cut harness selects the newly combined project through Home after import and waits for its duration and Edit controls before sending the live turn. Large supplied media can take longer than a default visibility wait. Three final-build harness attempts failed before the edit on project focus or stage readiness; the corrected run passed. Keep those failed private artifacts separate from the passing evidence.
The current packaged child harness also writes a synthetic hostile `mcp_servers.untrusted_test` entry into its fresh app-owned profile before launching Electron. The bridge discovers it with the pinned `mcp list --json` command and applies a per-server disabled override. The test audits the process arguments and active-thread status, requires `runtimeStatus: disabled` with no tools or resources, then continues the correlated read-only child test. `thread/start` and `thread/resume` explicitly disable Apps, plugins and remote plugins to suppress the authenticated host-owned Apps service. This remains one bounded config fixture, not proof of a complete effective tool catalog. Never copy real user config, MCP credentials or server output into the fixture.
The focused `tests/media/codex-mcp.test.ts` unit cases verify that the application calls `mcp list --json`, issues one explicit disabled override for each returned server, and checks that the resulting list preserves every name with `enabled: false`. Keep retained-enabled, added-server, removed-server, malformed-name, duplicate-name and excessive-count cases as fail-closed regressions. These unit tests do not replace the packaged hostile-config native child test.
