# Skill index

The implementation agent loads only skills relevant to the active task, plus `orchestrate-implementation` and `spec-sync` for every phase.

Skills are templates and may be refined when the implementation exposes a better contract. Any refinement must update routing, tests, and traceability in the same change.
